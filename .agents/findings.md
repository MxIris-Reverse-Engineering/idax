# Findings and Learnings (Live)

Entries below summarize key findings to preserve as implementation guardrails.
Format note: use a numbered list with one concrete technical finding per item; keep wording concise and include impact/mitigation only when it materially helps.

1. API naming inconsistency is the biggest onboarding barrier.
2. Implicit sentinels (`BADADDR`, `BADSEL`, magic ints) create silent failures.
3. Encoded flags/mixed bitfields are powerful but hard to reason about quickly.
4. Multiple equivalent SDK API paths differ subtly in semantics and side effects.
5. Pointer validity/lifecycle semantics need strong encapsulation in ergonomic wrappers.
6. Type and decompiler domains are high-power/high-complexity; need progressive API layering.
7. Debugger/UI domains need typed abstractions to prevent vararg misuse bugs.
8. Fully opaque wrappers need comprehensive coverage to avoid forcing raw SDK fallback.
9. Public API simplicity must preserve capability; advanced options must remain in structured form.
10. Migration docs are as critical as API design for adoption.
11. Interface-level API sketches must be present (not just summaries) to avoid implementation ambiguity.
12. C++23 + SDK `pro.h` incompatibility: `std::is_pod<T>` used without `#include <type_traits>`. Fix: include `<type_traits>` before `<pro.h>` in bridge header.
13. SDK segment API: `segment_t::perm` uses `SEGPERM_READ/WRITE/EXEC` (not `SFL_*`). Visibility via `is_visible_segm()` (not `is_hidden_segtype()`).
14. SDK float types require `BTF_FLOAT` (=`BT_FLOAT|BTMT_FLOAT`) and `BTF_DOUBLE` (=`BT_FLOAT|BTMT_DOUBLE`), not raw `BT_FLOAT`/`BTMT_DOUBLE`.
15. Private member access: use `friend struct XxxAccess` with static `populate()` in impl file. Anonymous namespace helpers cannot be friends.
16. **CRITICAL**: SDK stub dylibs vs real IDA dylibs have mismatched symbol exports. Stub `libidalib.dylib` exports symbols (e.g. `qvector_reserve`) the real one doesn't—only real `libida.dylib` does. macOS two-level namespace causes null-pointer crashes. **Fix**: link against real IDA dylibs, not SDK stubs.
17. CMake: `libidax.a` uses custom `idasdk_headers` INTERFACE target (SDK includes + `__EA64__` + platform settings). Consumers bring own `idasdk::plugin`/`idasdk::idalib`. For idalib tests, link real IDA dylibs to avoid namespace mismatches.
18. Graph API: `create_interactive_graph()` returns nullptr in idalib/headless. Graph uses standalone adjacency-list for programmatic use; only `show_graph()` needs UI. `qflow_chart_t` works in all modes.
19. SDK graph: `FC_PREDS` renamed to `FC_RESERVED`. Predecessors built by default; `FC_NOPREDS` to disable. `insert_simple_nodes()` takes `intvec_t&` (reference, not pointer).
20. SDK chooser: `chooser_t::choose()` returns ssize_t (-1=no selection, -2=empty, -3=already exists). `CH_KEEP` prevents deletion on widget close. Column widths encode `CHCOL_*` format flags in high bits.
21. SDK loader: `loader_failure()` does longjmp, never returns. No C++ base class for loaders (unlike `procmod_t`). Wrapper bridges C function pointers to C++ virtual methods via global instance pointer.
22. Hex-Rays ctree: `apply_to()`/`apply_to_exprs()` dispatch through `HEXDSP` runtime function pointers (no link-time dep). `CV_POST` enables leave_*() callbacks. `CV_PRUNE` via `prune_now()` skips children. `citem_t::is_expr()` returns `op <= cot_last` (69). `treeitems` populated after `get_pseudocode()`, maps line indices to `citem_t*`. `cfunc_t::hdrlines` is offset between treeitems indices and pseudocode line numbers.
23. `get_widget_title()` takes `(qstring *buf, TWidget *widget)` — NOT single-arg returning `const char*`. Changed from older SDKs.
24. Debugger notification API: mixed `va_list` signatures. Most events pass `const debug_event_t*`, but `dbg_bpt`/`dbg_trace` pass `(thid_t, ea_t, ...)` directly. Wrappers must decode per-event arg layouts.
25. `switch_info_t` encodes element sizes via `SWI_J32/SWI_JSIZE` and `SWI_V32/SWI_VSIZE` bit-pairs, not explicit byte fields. Expose normalized byte-size fields in wrapper structs.
26. IDB event payloads are `va_list`-backed, consumable only once. For multi-subscriber routing, decode once into normalized event object, then fan out.
27. `get_strlit_contents()` supports `len = size_t(-1)` auto-length: uses existing strlit item size or `get_max_strlit_length(...)`. Enables robust string extraction without prior data-definition calls.
28. Snapshot APIs in `loader.hpp`: `build_snapshot_tree()` returns synthetic root whose `children` are top-level snapshots. `update_snapshot_attributes(nullptr, root, attr, SSUF_DESC)` updates current DB snapshot description.
29. Custom fixup registration: `register_custom_fixup()`/`find_custom_fixup()`/`unregister_custom_fixup()` returns type ids in `FIXUP_CUSTOM` range (0 on duplicate/missing). Wrappers return typed IDs, map duplicates to conflict errors.
30. DB transfer: `file2base(li, pos, ea1, ea2, patchable)` requires open `linput_t*` + explicit close. `mem2base(ptr, ea1, ea2, fpos)` returns 1 on success, accepts `fpos=-1` for no file offset.
31. SDK bridge internals (`sdk_bridge.hpp`) in iostream-heavy tests collide with `fpro.h` stdio macro remaps (`stdout` -> `dont_use_stdout`). Keep string checks in integration-level tests or avoid iostream in bridge TUs.
32. Comment API: `append_cmt` success doesn't guarantee appended text round-trips via `get_cmt` as strict suffix. Tests should assert append success + core content presence, not strict suffix matching.
33. Netnode blob ops at index 0 can trigger `std::length_error: vector` crashes in idalib. Use non-zero indices (100+) for blob/alt/sup ops; document safe ranges.
34. `FunctionIterator::operator*()` returns by value (not reference); range-for must use `auto f` not `auto& f`. Constructs `Function` value from internal SDK state each dereference. Same for `FixupIterator`.
35. `DecompiledFunction` is move-only (`cfuncptr_t` is refcounted). `std::expected<DecompiledFunction, Error>` also non-copyable. Test macros using `auto _r = (expr)` must be replaced with reference-based checks.
36. P9.1 Audit: polarity clash (`Segment::visible()` vs `Function::is_hidden()`), subscription naming stutter (`debugger_unsubscribe` in `ida::debugger`), duplicate binary pattern search in `data`/`search`. Fix: unified positive polarity (`is_visible()`), removed stutter, documented dual-path.
37. P9.1 Audit: ~200+ `ea` params renamed to `address`, `set_op_*` to `set_operand_*`, `del_*` to `remove_*`, `idx`/`cmt` abbreviations expanded in public interfaces.
38. P9.1 Audit: `Plugin::run()` returned `bool` not `Status`; `Processor::analyze/emulate/output_operand` returned raw `int`; `line_to_address()` returned `BadAddress` as success; UI dialog cancellation was `SdkFailure` not `Validation`. All fixed.
39. P9.1 Audit: opaque boundary confirmed zero HIGH violations (no SDK types leak into public headers). MEDIUM: `Chooser::impl()`/`Graph::impl()` unnecessarily public, `xref::Reference::raw_type` exposed raw SDK codes. Fixed: `impl()` private, `raw_type` replaced with typed `ReferenceType` enum.
40. macOS linker warnings: IDA 9.3 dylibs built for macOS 12.0 while objects target 11.0. Warning-only; runtime stable. Keep linking real dylibs (required for symbol correctness).
41. CPack output dir drifts with arbitrary working directories. Fix: invoke with `-B <build-dir>` to pin artifact location.
42. Plugin surface gap (`entropyx/ida-port`): missing dockable custom widget hosting (`create_empty_widget`/`display_widget`/`close_widget`), HT_VIEW/UI notification coverage, `jumpto`, segment-type introspection. Add opaque dock-widget APIs, expanded event routing, `ui::jump_to`, `segment::Segment::type()`/`set_type()`.
43. Title-only widget callbacks insufficient for complex multi-panel plugins—titles aren't stable identities, no per-instance lifecycle tracking. Surface opaque widget handles in notifications.
44. Plugin authoring gap: `make_plugin_descriptor()` referenced but no public export helper exists. Add explicit descriptor/export helper bridging `Plugin` subclasses to IDA entrypoints.
45. SDK dock constants: `WOPN_DP_FLOATING` (not `WOPN_DP_FLOAT`). Defined in `kernwin.hpp` as `DP_*` shifts by `WOPN_DP_SHIFT`. `WOPN_RESTORE` restores size/position. `display_widget()` takes `(TWidget*, uint32 flags)`.
46. `view_curpos` event: no `va_list` payload—get position via `get_screen_ea()`. Differs from `ui_screen_ea_changed` which passes `(new_ea, prev_ea)` in `va_list`.
47. Widget identity: `TWidget*` stable for widget lifetime. Handle-based subscriptions compare `TWidget*` pointers. Opaque `Widget` stores `void*` + monotonic `uint64_t` id for cross-callback identity.
48. `plugin_t PLUGIN` static init: must use char arrays (not `std::string::c_str()`) to avoid cross-TU init ordering. Static char buffers populated at `idax_plugin_init_()` time. `IDAX_PLUGIN` macro registers factory via `make_plugin_export()`; `plugin_t PLUGIN` lives in `plugin.cpp` (compiled into `libidax.a`).
49. Segment type constants: SDK `SEG_NORM(0)`–`SEG_IMEM(12)`. Wrapper `segment::Type` maps all 12 values plus aliases: `Import`=`SEG_IMP=4`, `InternalMemory`=`SEG_IMEM=12`, `Group`=`SEG_GRP=6`. `segment_t::type` is `uchar`.
50. entropyx portability: dock widget lifecycle present, but Qt plugins still need underlying host container for `QWidget` embedding (entropyx casts `TWidget*` to `QWidget*`). `ida::ui::Widget` is opaque, no container attachment. Add `ui::with_widget_host(Widget&, callback)` with `void*` host pointer.
51. Widget host bridge: scoped callback (`with_widget_host`) over raw getter reduces accidental long-lived toolkit pointer storage. Host pointer type remains `void*` (`WidgetHost`) for SDK/Qt opacity.
52. `action_activation_ctx_t` carries many SDK pointers. Normalize only stable high-value fields (action id, widget title/type, current address/value, selection/xtrn bits, register name) into SDK-free structs.
53. Generic UI/VIEW routing needs token-family partitioning: UI (`< 1<<62`), VIEW (`[1<<62, 1<<63)`), composite (`>= 1<<63`) for safe unsubscribe of composite subscriptions.
54. SDK parity audit: broad domain coverage across all namespaces, but depth uneven (`partial` vs full SDK breadth). Closing parity needs matrix-driven checklist with per-domain closure criteria.
55. Diagnostics counters: plain shared struct creates data-race risk under concurrent logging/assertion. Use atomic counter fields and snapshot reads.
56. Compile-only parity drift: when headers evolve quickly, compile-only tests can lag. Expand `api_surface_parity_test.cpp` with header changes, including overload disambiguation.
57. `create_float`/`create_double` may fail at specific addresses in real DBs. Treat float/double define checks as conditional capability probes; assert category on failure.
58. `open_database()` in idalib performs loader selection internally, so `LoadIntent` (`Binary`/`NonBinary`) maps to same open path. Keep explicit intent API, wire to dedicated paths when possible.
59. SDK segment comments: `get_segment_cmt`/`set_segment_cmt` operate on `const segment_t*`. `set_segment_cmt` returns `void`. Validate target segment first; treat set as best-effort.
60. `set_entry_forwarder(ord, "")` can fail for some ordinals/DBs in idalib. Expose explicit `clear_forwarder()` returning `SdkFailure`; tests use set/read/restore patterns.
61. SDK search: `find_*` helpers already skip start address; `SEARCH_NEXT` mainly meaningful for lower-level text/binary search. Keep typed options uniform; validate with integration tests.
62. SDK action detach helpers return only success/failure, no absent-attachment distinction. Map detach failures to `NotFound` with action/widget context.
63. Loader callback context: load/reload/archive extraction spread across raw callback args and bitflags (`ACCEPT_*`, `NEF_*`). Expose typed request structs and `LoadFlags` encode/decode helpers.
64. Processor output: existing modules rely on side-effect callbacks; advanced ports need structured text assembly. Add `OutputContext` and context-driven hooks with fallback defaults (non-breaking).
65. SDK netnode existence: `exist(const netnode&)` is hidden-friend resolved via ADL. Qualifying as `::exist(...)` fails to compile. Call `exist(nn)` unqualified.
66. Debugger request queue: `request_*` APIs enqueue, need `run_requests()` to dispatch; direct `step_*`/`run_to`/`suspend_process` execute immediately. Mixing styles without flush causes no-op behavior. Expose explicit request helpers + `is_request_running()`/`run_requests()`.
67. SDK custom viewer lifetime: `create_custom_viewer()` relies on caller-provided line buffer/place objects remaining valid for widget lifetime. Store per-viewer state in wrapper-managed lifetime storage; erase on close.
68. Graph layout in headless is behavioral (stateful contract), not geometric rendering. Persist selected `Layout` in `Graph`, expose `current_layout()`, validate via deterministic integration checks.
69. Decompiler lvar retype persistence: uses `modify_user_lvar_info(..., MLI_TYPE, ...)` with stable locator. In-memory type tweaks alone are insufficient. Route through saved-user-info updates; add refresh + re-decompile checks.
70. Cross-cutting/event parity closure can use intentional-abstraction documentation when full raw SDK mirroring is counter to wrapper goals. Keep `partial` with rationale + expansion triggers.
71. Linux compile-only: GCC 13.3.0 passes on Ubuntu 24.04; Clang 18.1.3 fails with missing `std::expected` even with `-std=c++23`.
72. Linux Clang libc++ fallback: `-stdlib=libc++` avoids `std::expected` gap but fails during SDK header inclusion—`pro.h` remaps `snprintf` -> `dont_use_snprintf`, colliding with libc++ internals.
73. GitHub-hosted cross-platform validation: `compile-only` and `unit` profiles work without licensed IDA runtime by checking out `ida-sdk` with `IDADIR` unset; integration tests auto-skipped.
74. IDA SDK checkout layout varies (`<sdk>/ida-cmake/`, `<sdk>/cmake/`, submodule-backed). May need recursive submodule fetch. Resolve layout explicitly; support all known bootstrap locations.
75. CI submodule policy: both project and SDK checkouts should use recursive submodule fetch. Set `submodules: recursive` on both checkout steps.
76. GitHub Actions macOS labels change over time. Keep active labels (currently `macos-14`); reintroduce x86_64 via supported labels or self-hosted runners.
77. CTest on multi-config generators (Visual Studio): requires explicit `-C <config>` at test time. Always pass `--config` to `cmake --build` and `-C` to `ctest`.
78. SDK `pro.h` stdio remaps (`snprintf -> dont_use_snprintf`) collide with newer libc++ internals. Include key C++ headers before `pro.h` in bridge: `<functional>`, `<locale>`, `<vector>`, `<type_traits>`.
79. Example addon coverage: enable `IDAX_BUILD_EXAMPLES=ON` and `IDAX_BUILD_EXAMPLE_ADDONS=ON` in CI to catch module-authoring compile regressions without runtime execution.
80. JBC procmod gap: `ida::processor::analyze(Address)` returns only instruction size, no typed operand metadata (`o_near`/`o_mem`/`specflag`). Full ports must re-decode in multiple callbacks. Add optional typed analyze-result operand model.
81. JBC lifecycle gap: no wrapper for `set_default_sreg_value`. Add default-segment-register seeding helper.
82. JBC output gap: `OutputContext` is text-only (no token/color channels, no mnemonic callback parity). Extend with token-category output primitives and mnemonic/operand formatting hooks.
83. CI log audit: grep for `Complete job name`, `validation profile '<profile>' complete`, `100% tests passed` sentinels for quick validation.
84. JBC parity closed: `ida::processor` now includes `AnalyzeDetails`/`AnalyzeOperand` + `analyze_with_details`, tokenized output (`OutputTokenKind`/`OutputToken` + `OutputContext::tokens()`), mnemonic hook (`output_mnemonic_with_context`). `ida::segment` has default segment-register seeding helpers.
85. ida-qtform port: `ida::ui::with_widget_host()` sufficient for Qt panel embedding without raw `TWidget*`.
86. ida-qtform parity: added markup-only `ida::ui::ask_form(std::string_view)` for form preview/test without raw SDK varargs. Add typed argument binding APIs later if needed.
87. idalib-dump parity: decompiler microcode exposed via `DecompiledFunction::microcode()` and `microcode_lines()`.
88. idalib-dump gap: no headless plugin-load policy controls (`--no-plugins`, allowlist). Add database/session open options.
89. idalib-dump parity: decompile failures expose structured details via `DecompileFailure` and `decompile(address, &failure)` (failure address + description).
90. idalib-dump gap: no public Lumina facade. Add `ida::lumina` or document as intentional non-goal.
91. README drift risk: absolute coverage wording, stale surface counts, non-pinned packaging commands can diverge. Keep aligned with `docs/` artifacts.
92. idalib-dump parity: headless plugin-load policy via `RuntimeOptions` + `PluginLoadPolicy` (`disable_user_plugins`, allowlist with `*`/`?`). Reproduces `--no-plugins`/selective `--plugin`.
93. DB metadata: SDK file-type from two sources (`get_file_type_name` vs `INF_FILE_FORMAT_NAME`/`get_loader_format_name`). Expose both with explicit `NotFound` for missing loader-format.
94. Lumina parity: pull/push exposed via `ida::lumina` (`pull`, `push`, typed `BatchResult`/`OperationCode`, feature selection).
95. Lumina runtime: `close_server_connection2`/`close_server_connections` declared in SDK but not link-exported. Keep close wrappers as `Unsupported` until portable close path confirmed.
96. ida2py gap: no first-class user-name enumeration API. Add `ida::name` iterators (`all`, `all_user_defined`) with range/filter options.
97. ida2py gap: `TypeInfo` lacks pointer/array decomposition (pointee type, array element type, array length). Add decomposition helpers + typedef resolution.
98. ida2py gap: no generic typed-value data facade consuming `TypeInfo` for recursive materialization. Consider `ida::data::read_typed`/`write_typed`.
99. ida2py gap: decompiler expression views lack typed call subexpressions (callee + argument accessors). Add typed call-expression accessors in visitor views.
100. ida2py gap: no Appcall/executor abstraction or extension hook for external engines (e.g. angr). Add debugger execution facade + pluggable executor interface.
101. Host runtime caveat: idalib tool examples exit with signal 11 in this environment. Only build/CLI-help validation available; functional checks need known-good idalib host.
102. ida2py parity: `ida::name` now has typed inventory helpers (`all`, `all_user_defined`) backed by SDK nlist enumeration.
103. ida2py parity: `TypeInfo` now includes `is_typedef`, `pointee_type`, `array_element_type`, `array_length`, `resolve_typedef`.
104. ida2py parity: `ExpressionView` now includes `call_callee`, `call_argument(index)` alongside `call_argument_count`.
105. ida2py parity: `ida::data` now includes `read_typed`/`write_typed` with `TypedValue`/`TypedValueKind`, recursive array support, byte-array/string write paths.
106. ida2py parity: `ida::debugger` now includes Appcall + pluggable executor (`AppcallRequest`/`AppcallValue`, `appcall`, `cleanup_appcall`, `AppcallExecutor`, `register_executor`, `appcall_with_executor`).
107. Matrix drift risk: validation automation didn't propagate `IDAX_BUILD_EXAMPLE_TOOLS`. Plumb through scripts and CI workflow.
108. Appcall smoke: fixture `ref4` validated safely by calling `int ref4(int *p)` with `p = NULL`, exercises full request/type/argument/return bridging.
109. Tool-example runtime-linking: `ida_add_idalib` can bind to SDK stubs causing two-level namespace crashes. Prefer real IDA dylibs; stub fallback only when runtime libs unavailable.
110. Appcall host nuance: with runtime-linked tools, `--appcall-smoke` fails cleanly with `dbg_appcall` error 1552 (exit 1) instead of crashing. Remaining gap is debugger backend/session readiness.
111. Linux Clang C++23: Clang 18 reports `__cpp_concepts=201907` so `std::expected` stays disabled; Clang 19 reports `202002` and passes. Use Clang 19+ for Linux evidence.
112. Linux SDK artifacts: current checkout lacks `x64_linux_clang_64` runtime libs. Addon/tool targets fail under Linux Clang when build toggles on. Keep toggles OFF for Clang container evidence.
113. Appcall launch: `ida2py_port --appcall-smoke` tries multi-path debuggee launch (relative/absolute/filename+cwd). Host failures resolve to explicit `start_process failed (-1)`. Blocked by debugger backend.
114. Lumina validation: host reports successful `pull`/`push` smoke (`requested=1, succeeded=1, failed=0`). Non-close Lumina runtime validated.
115. lifter port audit: idax decompiler is read-oriented only. No write-path hooks: microcode filter registration, IR emission/mutation, maturity callbacks, `FUNC_OUTLINE` + caller cache invalidation. Full lifter migration blocked after plugin-shell/action porting.
116. lifter parity: added maturity subscriptions (`on_maturity_changed`/`unsubscribe`/`ScopedSubscription`) and outline/cache helpers (`function::is_outlined`/`set_outlined`, `decompiler::mark_dirty`/`mark_dirty_with_callers`). Remaining blocker: microcode write-path + raw decompiler-view handles.
117. lifter parity: baseline microcode-filter hooks added (`register_microcode_filter`, `unregister_microcode_filter`, `MicrocodeContext`, `MicrocodeApplyResult`, `ScopedMicrocodeFilter`). Full IR mutation (`m_call`/`m_ldx`/typed mops) remains unimplemented.
118. lifter parity: `MicrocodeContext` now includes operand/load-store and emit helpers (`load_operand_register`, `load_effective_address_register`, `store_operand_register`, `emit_move_register`, `emit_load_memory_register`, `emit_store_memory_register`, `emit_helper_call`). Advanced typed IR (callinfo/typed mops/helper-call args) still blocked.
119. lifter parity: typed helper-call argument builders added (`MicrocodeValueKind`/`MicrocodeValue`, `emit_helper_call_with_arguments`, `emit_helper_call_with_arguments_to_register`) for integer widths 1/2/4/8. Full parity needs UDT/vector args, callinfo controls, typed mop builders.
120. lifter parity: call option shaping via `MicrocodeCallOptions` + `MicrocodeCallingConvention` (`emit_helper_call_with_arguments_and_options`, `..._to_register_and_options`). Advanced callinfo/tmop depth (non-integer args, reg/stack location, return modeling) still open.
121. lifter parity: scalar FP immediates (`Float32Immediate`, `Float64Immediate`) and explicit-location hinting (`mark_explicit_locations`) added. Vector/UDT and deeper callinfo/tmop controls remain open.
122. lifter parity: `MicrocodeValueLocation` (register/stack offset) for argument-location hints. Auto-promoted when hints present.
123. lifter parity: register-pair and register-with-offset location forms added with validation/error mapping.
124. lifter parity: static-address placement (`set_ea`) with `BadAddress` validation added.
125. lifter parity: scattered/multi-part placement via `MicrocodeLocationPart` + `Scattered` kind with per-part validation (offset/size/kind constraints).
126. lifter parity: byte-array view modeling (`MicrocodeValueKind::ByteArray`) with explicit location requirements.
127. lifter parity: register-relative placement (`ALOC_RREL` via `consume_rrel`) with base-register validation.
128. lifter parity: vector view modeling (`MicrocodeValueKind::Vector`) with typed element width/count/sign/floating controls + explicit location enforcement.
129. lifter parity: declaration-driven type views (`MicrocodeValueKind::TypeDeclarationView`) parsed via `parse_decl` with explicit-location enforcement.
130. callinfo-shaping: `mark_dead_return_registers`, `mark_spoiled_lists_optimized`, `mark_synthetic_has_call`, `mark_has_format_string` mapped to `FCI_DEAD`/`FCI_SPLOK`/`FCI_HASCALL`/`FCI_HASFMT`.
131. callinfo-shaping: scalar field hints (`callee_address`, `solid_argument_count`, `call_stack_pointer_delta`, `stack_arguments_top`) mapped to `mcallinfo_t` fields with validation.
132. ActionContext host bridge: opaque handles (`widget_handle`, `focused_widget_handle`, `decompiler_view_handle`) and scoped callbacks (`with_widget_host`, `with_decompiler_view_host`).
133. Appcall launch fallback: adding `--wait` hold-mode args doesn't change host outcome (`start_process failed (-1)`). Blocker is debugger backend, not fixture timing.
134. Appcall attach fallback: `attach_process` returns `-4` across all permutations. Host blocked at attach readiness too. Gather pass evidence on debugger-capable host.
135. callinfo-shaping: `return_type_declaration` parsed via `parse_decl`, applied to `mcallinfo_t::return_type`. Invalid declarations fail with `Validation`.
136. lifter source-audit: dominant gap is generic microcode instruction authoring (opcode+operand construction, callinfo/tmop depth, deterministic insertion policy). Ad hoc helper-call expansion insufficient. Prioritize generic typed instruction builder/emitter.
137. lifter parity: baseline generic typed instruction emitter added (`MicrocodeOpcode`, `MicrocodeOperandKind`, `MicrocodeOperand`, `MicrocodeInstruction`, `emit_instruction`, `emit_instructions`). Covers `mov/add/xdu/ldx/stx/fadd/fsub/fmul/fdiv/i2f/f2f/nop`.
138. SDK microblock insertion: `mblock_t::insert_into_block(new, existing)` inserts after `existing`; `nullptr` inserts at beginning. Expose constrained policy enums (`Tail`/`Beginning`/`BeforeTail`) without raw block internals.
139. callinfo-shaping: `function_role` and `return_location` semantic hints with typed validation/mapping.
140. Helper-call placement: `MicrocodeCallOptions::insert_policy` reuses `MicrocodeInsertPolicy` (`Tail`/`Beginning`/`BeforeTail`).
141. Microcode-filter runtime stability: aggressive callinfo hints in hardening filters can trigger `INTERR: 50765`. Keep integration coverage validation-focused; heavy emission stress for dedicated scenarios.
142. Helper-call typed-return: register-return now accepts declaration-driven return typing with size-match validation and UDT marking for wider destinations.
143. Helper-call typed-argument: register args now accept declaration-driven typing with parse validation, size-match enforcement, and integer-width fallback.
144. Helper-call argument-metadata: optional `argument_name`, `argument_flags`, `MicrocodeArgumentFlag` mapped to `mcallarg_t::name`/`flags` with unsupported-bit validation and `FAI_RETPTR -> FAI_HIDDEN` normalization.
145. lifter probe: `lifter_port_plugin.cpp` installs working VMX/AVX subset via idax microcode-filter APIs. No-op `vzeroupper`, helper-call lowering for `vmxon/vmxoff/vmcall/vmlaunch/vmresume/vmptrld/vmptrst/vmclear/vmread/vmwrite/invept/invvpid/vmfunc`.
146. AVX temporary-register: `MicrocodeContext::allocate_temporary_register(byte_width)` mirrors `mba->alloc_kreg`.
147. Helper-call callinfo defaults: `solid_argument_count` now inferred from provided argument list when omitted.
148. Helper-call auto-stack placement: additive `auto_stack_start_offset`/`auto_stack_alignment` controls with validation (non-negative start, power-of-two positive alignment).
149. lifter AVX scalar subset: lowers `vaddss/vsubss/vmulss/vdivss`, `vaddsd/vsubsd/vmulsd/vdivsd`, `vcvtss2sd`, `vcvtsd2ss` through typed emission (`FloatAdd/FloatSub/FloatMul/FloatDiv/FloatToFloat`).
150. Instruction `Operand` exposes typed values but no rendered text helpers. AVX lowering assumes XMM-width destination copy. Expand operand-width introspection if broader vector-width lowering needed.
151. lifter AVX scalar expansion: `vminss/vmaxss/vminsd/vmaxsd`, `vsqrtss/vsqrtsd`, `vmovss/vmovsd` using typed emission + helper-call return lowering.
152. AVX scalar move memory-destination: load destination register before checking memory-destination creates unnecessary failure. Handle memory-dest stores first (`store_operand_register`), then resolve register-target paths.
153. AVX packed subset: `vaddps/vsubps/vmulps/vdivps`, `vaddpd/vsubpd/vmulpd/vdivpd`, `vmov*` packed moves through typed emission + store-aware handling.
154. Packed-width inference: `ida::instruction::operand_text(address, index)` heuristics (`xmm`/`ymm`/`zmm`, `*word` tokens) enable width-aware lowering without SDK internals.
155. Helper-call typed-return fallback: packed destination widths exceeding integer scalar widths need byte-array `tinfo_t` fallback when no explicit return declaration supplied.
156. Packed conversion subset: `vcvtps2pd`/`vcvtpd2ps`, `vcvtdq2ps`/`vcvtudq2ps`, `vcvtdq2pd`/`vcvtudq2pd` via `FloatToFloat`/`IntegerToFloat` emission with operand-text width heuristics.
157. Many float-int packed conversions (`vcvt*2dq/udq/qq/uqq`, truncating) don't map to current typed opcodes; use helper-call fallback.
158. `vaddsub*`/`vhadd*`/`vhsub*` need lane-level semantics beyond `FloatAdd`/`FloatSub`. Use helper-call lowering.
159. Helper-fallback packed families (bitwise/permute/blend) widened by collecting mixed register/immediate operands and forwarding as typed helper-call arguments.
160. Packed logic/permute/blend: no direct typed opcodes; helper-call fallback remains practical path.
161. Packed shift/rotate (`vps*`, `vprol*`, `vpror*`): mixed register/immediate shapes not directly expressible. Helper-call fallback.
162. Variadic helper fallback robustness: unsupported operand shapes degrade to `NotHandled` not hard errors, keeping decompiler stable while coverage grows.
163. Variadic helper memory-operand: when register extraction fails for source, attempt effective-address extraction and pass typed pointer argument.
164. Packed compare destination: mask-register destinations not representable in current register-load helpers. Treat unsupported compare destinations as no-op handling.
165. Typed packed bitwise/shift opcodes added: `BitwiseAnd`/`BitwiseOr`/`BitwiseXor`/`ShiftLeft`/`ShiftRightLogical`/`ShiftRightArithmetic`. Helper fallback kept for unsupported forms (`andn`, rotate, exotic).
166. Typed packed integer add/sub: `MicrocodeOpcode::Subtract` added. `vpadd*`/`vpsub*` direct register/immediate forms routed through typed emission. Helper fallback for mixed/unsupported.
167. Packed integer operand-shape: typed emission doesn't cover memory-source or saturating variants. Route saturating/memory forms through variadic helper fallback.
168. Packed integer multiply: typed direct multiply for `vpmulld`/`vpmullq`. Other variants (`vpmullw`/`vpmuludq`/`vpmaddwd`) have lane-specific semantics—use helper-call fallback.
169. Packed binary operand count: two-operand encodings can be missed if destination not treated as implicit left operand. Treat operand 0 as both dest and left source for two-operand forms.
170. Advanced callinfo list-shaping: register-list and visible-memory controls exposed. Passthrough registers must be subset of spoiled. Validate subset semantics; return `Validation` on mismatch.
171. Declaration-driven vector element typing: `type_declaration` parsed as element type. Element-size/count/total-width constraints validated together. Derive missing count from total width when possible; reject mismatched shapes.
172. Rich typed mop-builder: `RegisterPair`/`GlobalAddress`/`StackVariable`/`HelperReference` operand/value kinds added.
173. Block-reference typed operand: `MicrocodeOperandKind::BlockReference` + validated `block_index` without raw microblock exposure.
174. Nested-instruction typed operand: `MicrocodeOperandKind::NestedInstruction` + `nested_instruction` payload with recursive validation/depth limiting.
175. Local-variable typed operand: `MicrocodeOperandKind::LocalVariable` with `local_variable_index`/`local_variable_offset` validated before `_make_lvar(...)` mapping.
176. Local-variable rewrite safety: needs context-aware availability checks. Expose `MicrocodeContext::local_variable_count()` and gate usage on `count > 0` with no-op fallback.
177. Local-variable rewrite consistency: centralize local-variable self-move emission in one helper; reuse across rewrite sites (`vzeroupper`, `vmxoff`).
178. Debugger backend activation: backend discovery/loading should be explicit (`available_backends` + `load_backend`). Expose in `ida::debugger`; auto-load in `ida2py_port` before launch.
179. Appcall host (macOS): with `arm_mac` backend, `start_process` returns 0 but state stays `NoProcess`; attach returns `-1`, still `NoProcess`. Blocked by backend/session readiness, not wrapper API coverage.
180. Appcall queued-request timing: `request_start`/`request_attach` report success while state still `NoProcess` immediately after single flush. Perform bounded multi-cycle request draining with settle delays.
181. Microcode placement parity: low-level emit helpers (`emit_noop`, `emit_move_register`, `emit_load_memory_register`, `emit_store_memory_register`) default to tail insertion. Add policy-aware variants; route all emits through shared reposition logic.
182. Wide-operand emit: AVX/VMX wider register/memory flows may need UDT operand marking. Add optional `mark_user_defined_type` controls to low-level move/load/store helpers.
183. Store-operand UDT: `store_operand_register` writeback for wide/non-scalar flows needs explicit UDT marking on source mop. Add `store_operand_register(..., mark_user_defined_type)` overload.
184. Immediate typed-argument declaration: `UnsignedImmediate`/`SignedImmediate` now consume optional `type_declaration` with parse/size validation and declaration-width inference when byte width omitted.
185. Callinfo pass/spoil coherence: `passthrough_registers` contradictory without `spoiled_registers` superset. Enforce subset semantics whenever passthrough registers present.
186. Callinfo coherence testing: success-path helper-call emissions in filter hardening can trigger decompiler `INTERR`. Prefer validation-first probes (e.g., post-subset bad-visible-memory checks) for deterministic assertions.
187. SDK operand width metadata: `op_t::dtype` + `get_dtype_size(...)` provide structured operand byte widths; fallback register-name inference is only needed when processors omit operand dtype detail.
188. Compare/mask destination handling: helper-call return can be lowered deterministically by routing through temporary register + operand writeback (`store_operand_register`) instead of no-op tolerance.
189. Microcode rewrite lifecycle: tracking last-emitted instruction plus block instruction-count query enables additive remove/rewrite workflows without exposing raw microblock internals.
190. Lifter width heuristics: structured `instruction::Operand` metadata (`byte_width`, `register_name`, `register_category`) removes dependence on `operand_text()` parsing for AVX width decisions.
191. Microblock index lifecycle: `has_instruction_at_index`/`remove_instruction_at_index` provide deterministic, SDK-opaque mutation targeting beyond tracked-last-emitted-only flows.
192. Typed helper-call tmop shaping: helper-call argument model can carry `BlockReference`/`NestedInstruction` values for richer callarg mop authoring without raw `mop_t` exposure.
193. Typed decompiler-view edit sessions: deriving stable function identity from opaque host handles (`view_from_host`) enables reusable rename/retype/comment/save/refresh workflows without exposing `vdui_t`/`cfunc_t`.
194. Decompiler variable-edit error categories can vary by backend/runtime (`NotFound` vs `SdkFailure`) for missing locals. Tests should assert failure semantics unless category is contractually stable.
195. Integration tests that persist decompiler edits can mutate fixture `.i64` files. Prefer non-persisting validation probes (or explicit fixture restore) for deterministic worktree hygiene.
196. AVX/VMX helper-return routing: prefer `emit_helper_call_with_arguments_to_micro_operand_and_options` for register and direct-memory (`MemoryDirect` -> `GlobalAddress`) destinations; keep operand-index writeback as fallback for unresolved destination shapes.
197. Integration hardening can safely exercise helper-return micro-operand success routes by targeting temporary-register and current-address `GlobalAddress` destinations, then removing emitted instructions to keep mutation checks deterministic.
198. Helper-return destination routing can reduce operand-index writeback fallback further by treating any memory operand with a resolved `target_address` as a typed `GlobalAddress` micro-operand destination (not only `MemoryDirect`).
199. Lifter helper-call depth can progress safely by adding semantic call-role hints (`SseCompare4`/`SseCompare8` for `vcmp*`) plus `argument_name` metadata on helper arguments; this enriches callinfo/tmop intent without aggressive side-effect flags.
200. Additive callinfo enrichment scales cleanly when semantic roles also cover rotate helper families (`RotateLeft`/`RotateRight`) and `argument_name` metadata is applied consistently across variadic, VMX, and explicit scalar/packed helper-call paths.
201. Declaration-driven return typing can be applied incrementally to stable helper-return families (integer-width `vmread` register destinations and scalar float/double helper returns) to improve callinfo fidelity without broad vector-type assumptions.
202. Register-destination helper flows can safely carry explicit callinfo `return_location` hints when mapped to the same destination register used for helper-return writeback; this composes with declaration-driven return typing.
203. Callinfo hardening should probe both positive and negative hint paths: success/backend-failure tolerance for micro/register destination routes, plus validation checks for negative register return locations and return-type-size mismatches.
204. Compare helper operand-index writeback fallback should be constrained to unresolved destination shapes only (mask-register destinations and memory destinations lacking resolvable target addresses), while resolved destinations prefer typed micro-operand routing.
205. Callinfo hardening coverage should include cross-route validation checks (`to_micro_operand`, `to_register`, `to_operand`) for invalid return-location register ids and return-type-size mismatches to prevent contract drift between helper emission APIs.
206. When compare destinations are register-shaped but `load_operand_register(0)` fails, attempting a typed micro-operand register route using structured `Operand::register_id()` before operand-writeback fallback can recover additional handled cases while preserving unresolved-shape gating.
207. For compare helper flows with resolved memory destinations, static-address `return_location` hints can be applied to typed `GlobalAddress` micro-destination routes; if a backend rejects the hint as validation, retrying without the hint preserves stable behavior.
208. Callinfo hardening can extend positive/negative location validation to global-destination micro routes by asserting success-or-backend-failure for valid static-address hints and explicit `Validation` for `BadAddress` static return locations.
209. Static-address return-location validation should be asserted across helper emission routes including `to_operand`; `BadAddress` static-location hints must fail with `Validation` consistently.
210. Compare helper register-destination micro routes benefit from the same validation-safe retry pattern as resolved-memory routes: if explicit register `return_location` hints are rejected with validation, retrying with no location hint preserves stable handling.
211. Callinfo hardening should validate return-type-size mismatch behavior on global-destination routes as well, including `to_operand` checks, to keep type-size contracts consistent across helper emission APIs.
212. For unresolved compare destinations, an intermediate helper-to-temporary-register route plus operand store writeback can be attempted before direct `to_operand` helper fallback, reducing reliance on the direct operand route while preserving degraded-path stability.
213. Compare helper micro-routes can use a three-step validation-safe retry ladder (full typed options with location+declaration -> reduced options without location -> base compare options without declaration/location) to preserve semantic richness first while degrading safely when backends reject advanced callinfo hints.
214. Direct `to_operand` compare fallback can also use validation-safe retry with base compare options (dropping declaration/location hints) to reduce avoidable validation failures on backend variance while preserving unresolved-shape degraded handling.
215. After all degraded compare `to_operand` retries are exhausted, treating residual `Validation` failures as non-fatal (`NotHandled`) improves backend variance tolerance while keeping hard SDK/internal failures explicit.

216. Compare helper stability improves when all destination routes (resolved-memory micro, register micro, temporary-register bridge, and degraded `to_operand`) retry with base compare options on validation rejection, and temporary-register writeback treats `Validation`/`NotFound` store failures as degradable while preserving hard SDK/internal failure signaling.

217. Direct register-destination compare helper routes can use the same validation-safe retry ladder (location+declaration hints -> declaration-only -> base compare options), and residual validation rejection should degrade to not-handled while preserving hard SDK/internal failures.

218. In temporary-register compare fallback paths, once `store_operand_register` `Validation`/`NotFound` is intentionally degraded, any subsequent category checks must guard on `!status` before reading `.error()`; reading `.error()` on a successful `std::expected` is invalid and can destabilize fallback flow.

219. Compare helper fallback handling is more backend-robust when residual `NotFound` outcomes are treated like other degradable non-hard categories on degraded/direct-destination routes (return not-handled), while preserving only `SdkFailure`/`Internal` as hard failure signals.

220. The compare-helper temporary-register bridge can use typed `_to_micro_operand` destination routing instead of `_to_register` because the allocated temporary register id is known and can be expressed as a `MicrocodeOperand` with `kind = Register`. This eliminates the last non-typed helper-call destination in the lifter probe; all remaining operand-writeback sites are genuinely irreducible (unresolved mask-register/memory shapes for compare destinations, and legitimate memory-store operations for vmov memory destinations).

221. The original lifter's `clear_upper()` function — called at 112 sites across handler files — is a deliberate no-op. The comment states "AVX semantics handle this automatically." The only real zero-extension gap is `vmovd`/`vmovq` which uses explicit `m_xdu` (zero-extend unsigned) to widen 4/8-byte GPR/memory values into XMM/YMM-width registers. The port's previous routing through `lift_packed_helper_variadic` for these instructions was semantically incorrect — it emitted opaque helper calls instead of native microcode, losing type inference and value-tracking information.

222. AVX-512 opmask information is stored in `insn.Op6` (`insn.ops[5]`) with the operand type `o_reg` or `o_kreg` and register number relative to `R_k0`. The EVEX.z (zero-masking) bit is in `Op6.specflag2 & 0x04` via the `evex_flags` macro. Because `reg2mreg()` returns `mr_none` for k-registers, the original lifter encodes the k-register number as a negative `mreg_t` and passes it as an unsigned immediate constant in helper-call arguments — the decompiler cannot natively represent k-registers in microcode. The idax wrapper exposes this through `MicrocodeContext::has_opmask()`, `is_zero_masking()`, and `opmask_register_number()` without requiring Intel-specific headers in user code.

223. SSE passthrough instructions (`vcomiss`, `vcomisd`, `vucomiss`, `vucomisd`, `vpextrb/w/d/q`, `vcvttss2si`, `vcvttsd2si`, `vcvtsd2si`, `vcvtsi2ss`, `vcvtsi2sd`) are returned to IDA's native handling by the original lifter via `MERR_INSN`. The idax port equivalent is returning `false` from `match()` so the filter does not claim the instruction.

224. K-register manipulation instructions (`kmov*`, `kadd*`, `kand*`, `kor*`, `kxor*`, `kxnor*`, `knot*`, `kshift*`, `kunpck*`, `ktest*`) and instructions with mask-register destinations are lifted as NOPs by the original lifter. This is a pragmatic choice: the decompiler's microcode cannot represent k-register operations natively, so the lifter acknowledges the instruction without trying to model its k-register semantics.

225. AVX-512 opmask masking must be wired uniformly across ALL helper-call paths (compare, store-like, scalar min/max/sqrt, packed sqrt/addsub/min/max, helper-fallback conversions, and variadic helpers), not just the normal variadic path. For native microcode emission paths (typed binary add/sub/mul/etc, typed conversion, typed moves, typed packed FP math), the correct approach when masking is present is to skip the native emission and fall through to the helper-call path, because native microcode instructions cannot represent per-element masking. The helper-call path then applies masking by suffixing `_mask`/`_maskz` to the helper name and appending merge-source register and mask register number arguments.

226. The original lifter uses `get_type_robust(size, is_int, is_double)` → `get_vector_type(size, is_int, is_double)` to resolve named vector types (`__m128`, `__m128i`, `__m128d`, `__m256`, `__m256i`, `__m256d`, `__m512`, `__m512i`, `__m512d`) from the DB's type library for ALL helper-call return types and arguments. The type lookup first tries `tinfo_t::get_named_type()` then falls back to synthesizing an anonymous UDT struct with a byte-array member. The idax port achieves the same effect by setting `return_type_declaration` to the corresponding type name string (e.g., `"__m256i"`), which the wrapper passes to `parse_decl` for resolution against the same type library. For scalar sizes (1-8 bytes), integer/floating declaration strings are equivalent. For wider sizes (16/32/64 bytes), the named vector type declarations produce proper `__m*` types in decompiler output instead of anonymous byte-array structs. A `vector_type_declaration(byte_width, is_integer, is_double)` helper unifies scalar and vector type name selection in the port, mirroring the original's `get_type_robust` signature.

227. A comprehensive cross-reference audit of the original lifter's 14 distinct SDK mutation pattern categories (`cdg.emit`, `alloc_kreg/free_kreg`, `store_operand_hack`, `load_operand_udt`, `emit_zmm_load`, `emit_vector_store`, `AVXIntrinsic`, `AvxOpLoader`, `mop_t` construction, `minsn_t` post-processing, `load_operand/load_effective_address`, `MaskInfo`, misc utilities) confirmed that 13 of 14 are fully covered by idax wrapper APIs actively used in the port (~2,700 lines, 300+ mnemonics). The remaining pattern (post-emit instruction field mutation) has functional equivalence via remove+re-emit lifecycle helpers. No new wrapper APIs are required for lifter-class microcode transformation ports. Key quantitative evidence: 26 helper-call emission sites, 7 typed instruction emission sites, 37 operand load sites, 4 effective address loads, 4 operand writeback sites, 11 UDT marking references.

228. The lifter port now has separate "Mark as inline" and "Mark as outline" context-sensitive actions matching the original's dual-action design in `inline_component.cpp`. The original uses `action_state_t` (`AST_ENABLE_FOR_WIDGET`/`AST_DISABLE_FOR_WIDGET`) in the `update()` callback to show only the relevant action based on the function's `FUNC_OUTLINE` flag state. The idax port achieves this via `enabled_with_context` lambdas that query `ida::function::is_outlined()` — "Mark as inline" is enabled only when `FUNC_OUTLINE` is NOT set, "Mark as outline" only when it IS set. Both handlers call `ida::decompiler::mark_dirty_with_callers()` after toggling the flag, matching the original's `mark_callers_dirty` + `vu->refresh_view(true)` pattern.

229. The lifter port now has a debug-printing toggle action with maturity-driven disassembly/microcode dumps matching the original's `hexrays_debug_callback` in `avx_lifter.cpp`. The original installs a `hxe_maturity` callback that prints disassembly at `MMAT_GENERATED` and microcode at `MMAT_PREOPTIMIZED`/`MMAT_LOCOPT`. The idax port uses `ida::decompiler::on_maturity_changed()` with `ScopedSubscription` and maps `Maturity::Built` (=`MMAT_GENERATED`), `Maturity::Trans1` (=`MMAT_PREOPTIMIZED`), and `Maturity::Nice` (=`MMAT_LOCOPT`). The subscription is installed/removed dynamically via `toggle_debug_printing()` with the debug state tracked in `PortState::debug_printing`.

230. The 32-bit YMM skip guard has been ported from the original lifter's `match()`. The original checks `inf_is_64bit()` and skips YMM (256-bit) operands to avoid Hex-Rays `INTERR 50920` ("Temporary registers cannot cross block boundaries"). The idax port uses `ida::function::at(address)->bitness() == 64` (with `ida::segment::at()` fallback) as there is no direct `inf_is_64bit()` wrapper, and checks `Operand::byte_width() == 32` for YMM detection instead of the original's `op.dtype == dt_byte32`.

231. The SDK global `PH.id` (processor_t::id via `get_ph()`) returns the active processor module ID (PLFM_* constant). `PLFM_386` is `0` (Intel x86/x64), not 15 as might be assumed. The original lifter uses `PH.id != PLFM_386` in both AVX and VMX component availability checks as a crash guard — IDA crashes when interacting with AVX/VMX in non-x86 processor modes. Added `ida::database::processor_id()` wrapping `PH.id` and `ida::database::processor_name()` wrapping `inf_get_procname()` to idax. Implementation lives in `address.cpp` (not `database.cpp`) to avoid pulling idalib-only symbols (`init_library`/`open_database`/`close_database`) into plugin link units.

232. The IDA SDK redefines bare `snprintf` to `dont_use_snprintf` via a macro in `pro.h:965`, forcing use of `qsnprintf` in SDK-linked code. However, `std::snprintf` (the fully-qualified C++ name) is unaffected by this macro and can be used safely in example/plugin code that doesn't include SDK headers directly. In idax `src/` files (which include SDK headers), `qsnprintf` must be used. In example plugins (which include only `<ida/idax.hpp>`), `std::snprintf` is the portable alternative.

233. `cfunc_t::get_pseudocode()` returns `const strvec_t&` — a read-only reference. To modify pseudocode lines (e.g., inserting color tags), access `cfunc->sv` directly (the public member), not the const getter. The idax wrapper's `set_raw_line()` implements this by indexing into `cfunc->sv[line_index].line`.

234. The SDK's `qrefcnt_t<cfunc_t>` (typedef `cfuncptr_t`) does not provide a `.get()` method like `std::shared_ptr`. To obtain a raw `cfunc_t*`, use `&*ptr` (dereference then take address) or `ptr.operator->()`.

235. Color enum values in the SDK (`color_t` constants in `lines.hpp`) use specific byte values that must be matched exactly in any wrapper enum: `COLOR_DEFAULT=0x01`, `COLOR_KEYWORD=0x20`, `COLOR_REG=0x21`, `COLOR_IMPNAME=0x17`, `COLOR_LIBNAME=0x18`, `COLOR_NUMBER=0x0C`, `COLOR_STRING=0x14`, `COLOR_CHAR=0x15`, etc. Getting these wrong produces garbled colored output in IDA's pseudocode view.

236. Widget type enum values must match SDK `BWN_*` constants in `kernwin.hpp`: `BWN_EXPORTS=0`, `BWN_IMPORTS=1`, `BWN_NAMES=2`, `BWN_FUNCS=3`, `BWN_STRINGS=5`, `BWN_DISASM=27`, `BWN_PSEUDOCODE=46`, etc. These are NOT sequential or intuitive — they follow internal SDK widget registration order.

237. `attach_dynamic_action_to_popup` uses `DYNACTION_DESC_LITERAL` macro which takes 5 arguments: `(label, handler, shortcut, tooltip, icon)`. This is NOT the same as `ACTION_DESC_LITERAL_OWNER` (8 args). The dynamic action variant is for temporary popup-only actions that don't need global registration.

238. Hexrays callback event signatures (accessed via `va_arg` in the event bridge): `hxe_func_printed` receives `(cfunc_t*)`, `hxe_curpos` receives `(vdui_t*)`, `hxe_create_hint` receives `(vdui_t*, qstring*, int*)` and the callback should return 1 to show the hint or 0 to skip, `hxe_refresh_pseudocode` receives `(vdui_t*)`. All events return `int` (0 = continue processing, non-zero = handled for most events).

239. `ui_finish_populating_widget_popup` notification receives `(TWidget*, TPopupMenu*, const action_activation_ctx_t*)` via `va_arg`. The `TPopupMenu*` is needed for `attach_dynamic_action_to_popup()` calls. The `action_activation_ctx_t` provides widget context including the widget pointer and type.

240. The `IDAX_PLUGIN(ClassName)` macro is mandatory for all IDA plugin source files using the idax wrapper. It expands to the `_PLUGIN` export symbol that IDA's plugin loader searches for when loading `.dylib`/`.dll`/`.so` files. Without it, the dylib compiles and links but contains zero exported functions — IDA silently ignores it. This was found when 5 example plugins (`action_plugin.cpp`, `decompiler_plugin.cpp`, `deep_analysis_plugin.cpp`, `event_monitor_plugin.cpp`, `storage_metadata_plugin.cpp`) produced empty dylibs despite compiling cleanly.

241. When `libidax.a` is linked into a plugin `.dylib`, the linker resolves symbols at the object-file granularity. If any function from a `.cpp.o` file is referenced, ALL symbols in that object file must be resolvable. `database.cpp` contained both plugin-safe query APIs (`input_file_path`, `image_base`, `input_md5`, etc.) and idalib-only lifecycle APIs (`init`, `open`, `close`). The idalib-only functions reference `init_library`, `open_database`, `close_database`, `enable_console_messages` which are NOT in `libida.dylib` — they're only in `libidalib.dylib`. Splitting into `database.cpp` (queries) + `database_lifecycle.cpp` (lifecycle) isolates the idalib-only symbols so plugins that only use query APIs don't pull them in. `save_database` IS available in `libida.dylib`, so `save()` stays in the plugin-safe `database.cpp`.

242. DrawIDA port audit: idax plugin export is fixed to `PLUGIN_MULTI` through `IDAX_PLUGIN` and does not expose per-plugin flag customization (`PLUGIN_KEEP`/`PLUGIN_UNL`/etc.). This is a low-severity ergonomic surface gap for ports that want explicit raw-SDK flag control, but it is non-blocking for DrawIDA.

243. DrawIDA port audit: `ida::ui::with_widget_host()` intentionally returns opaque `void*` host handles. Qt-based ports must manually cast to `QWidget*` in plugin code to mount custom controls. Capability is present, but this is a recurring low-severity ergonomics gap for Qt-heavy ports.

244. Documentation drift risk: adding a new real-world port artifact (like `examples/plugin/abyss_port_plugin.cpp`) can leave user-facing docs stale unless the update explicitly touches all index surfaces together (`README.md` parity/doc tables, `docs/api_reference.md`, `docs/namespace_topology.md`, `docs/sdk_domain_coverage_matrix.md`, quickstart/example guides, and a dedicated port audit doc). Treat this as a required closeout checklist item for future ports.

245. Plugin export flag ergonomics closure: `ida::plugin::ExportFlags` + `IDAX_PLUGIN_WITH_FLAGS(...)` adds per-plugin export control (`modifies_database`, `requests_redraw`, `segment_scoped`, `unload_after_run`, `hidden`, `debugger_only`, `processor_specific`, `load_at_startup`, `extra_raw_flags`) while preserving idax's mandatory `PLUGIN_MULTI` bridge model.

246. Qt host ergonomics closure: `ida::ui::widget_host_as<T>()` + `ida::ui::with_widget_host_as<T>()` provide typed host access wrappers over `widget_host`/`with_widget_host`, eliminating repetitive `void*` casts in Qt plugin ports.

247. Dedicated Qt addon wiring can be made non-fragile by using `ida_add_plugin(TYPE QT QT_COMPONENTS Core Gui Widgets ...)`: when Qt is unavailable the target is skipped with explicit `build_qt` guidance, and when Qt is present the plugin builds as a normal addon target.

248. Qt header portability nuance (Qt6/Homebrew): `qkeyevent.h` is not present in framework headers; `QKeyEvent`/`QMouseEvent` are provided via `qevent.h`. Using `qevent.h` avoids target-specific include breakage in Qt plugin sources.

249. IDA SDK 9.3 does not expose legacy `get_struc_id()`/`get_struc()` helpers in the public bridge used by idax; struct-offset operand helpers should resolve type IDs through `get_named_type_tid()` and then call `op_stroff`/`op_based_stroff`.

250. DriverBuddy migration validates that `ida::database::import_modules`, `ida::function::all`, `ida::xref::code_refs_to`, `ida::search::text`, and `ida::instruction::decode` are sufficient to port core Windows-driver triage workflows (driver classification, dispatch discovery, IOCTL decode) without raw SDK fallback.

251. WDF dispatch-table labeling can be implemented in idax without raw SDK structs by materializing a `TypeInfo::create_struct()` schema and applying it at the discovered table address (`type::apply_named_type` + `name::force_set`) after locating the `KmdfLibrary` marker and dereferencing metadata pointers.

252. DriverBuddy parity still has non-blocking ergonomic gaps: no one-call standard-type bootstrap equivalent to `Til2Idb(-1, name)`, no public struct-offset path readback (`get_stroff_path`) wrapper, and no minimal `add_hotkey` convenience API separate from action registration.

253. idapcode port required first-class database metadata wrappers for architecture selection parity: `address_bitness` (16/32/64), `is_big_endian`, and `abi_name` in addition to processor ID/name. Wrapping these SDK globals in `ida::database` keeps plugin code SDK-opaque while enabling deterministic Sleigh spec routing.

254. Sleigh support helper semantics: `sleigh::FindSpecFile` expects search roots at the specfiles root and internally appends `Ghidra/Processors/<arch>/data/languages/<file>`. Passing a language subdirectory path directly will fail lookup.

255. Sleigh as a dependency is heavy because configuring from source fetches/patches Ghidra; integrating it behind an idapcode-specific opt-in build flag avoids regressing default idax configure/build cycles.

256. Even with processor ID + bitness + endianness + ABI, exact language-profile selection remains partial for some families (notably ARM profile/revision variants). A richer normalized processor-profile wrapper would eliminate residual best-effort heuristics in ports like idapcode.

257. On this host, runtime integration tests that initialize idalib can still fail with `init_library failed` even when `IDADIR`/`DYLD_LIBRARY_PATH` are explicitly set; build-level validation can proceed, but runtime evidence remains host/license-gated.

258. The `init_library` startup failure for idax smoke flows is reproducible when `IDADIR` points to an SDK source tree (for example `<ida-sdk-source>`) rather than a full IDA runtime root. On this host, `idax_smoke_test` succeeds with no env overrides because the binary already carries `LC_RPATH` to `<ida-runtime>`; explicit `IDADIR`/`DYLD_LIBRARY_PATH` to that same runtime root also succeeds.

259. **Superseded by F394:** The earlier user-supplied set asserted that `ida::database::ProcessorId` should track a current SDK range through `PLFM_MCORE = 77`. Exhaustive installed/current SDK verification did not support that terminal constant; retain this entry only as historical provenance for the now-corrected enum expansion.

260. Runtime plugin-load policy paths are now host-validated via `idax_idalib_dump_port`: both `--no-plugins` and allowlist mode (`--plugin "*.dylib"`) initialize/open/analyze successfully against the fixture binary, confirming `RuntimeOptions::plugin_policy` is non-blocking in this runtime profile.

261. Bidirectional navigation sync between a custom p-code viewer and linear disassembly can be implemented in idax without new wrapper APIs by combining `ui::on_cursor_changed` (viewer -> linear), `ui::on_screen_ea_changed` (linear -> viewer), `ui::on_view_activated`/`on_view_deactivated`, and `ui::custom_viewer_jump_to_line` with a reentrancy guard.

262. For robust click-to-address mapping in a p-code custom viewer, prefixing every rendered line (including emitted p-code op lines and error lines) with a canonical address token enables stable cursor-line address parsing even when the cursor is not on the instruction header line.

263. Cross-function follow in the idapcode viewer is achieved by detecting when `screen_ea` leaves the currently mapped function range, resolving `function::at(new_ea)`, and rebuilding the existing custom viewer content in-place for the new function rather than opening additional viewers.

264. Scroll-follow behavior for custom viewers can be approximated without new SDK wrappers by adding a lightweight UI timer that polls `custom_viewer_current_line(mouse=true/false)` and applies `jump_to` when the parsed line address changes.

265. The idapcode shortcut was changed from `Ctrl-Alt-S` to `Ctrl-Alt-Shift-P` to avoid common keybinding collisions with SigMaker workflows while keeping the action discoverable for p-code usage.

266. `ida::ui::set_custom_viewer_lines` must preserve the original `CustomViewerState` object address. Replacing the stored `unique_ptr` invalidates internal pointers handed to `create_custom_viewer` (`min`/`max`/`cur`/`lines`) and can cause EXC_BAD_ACCESS during render/model updates; mutating the existing state in-place fixes the crash.

267. For C ABI arrays that return owning opaque handles (e.g., `IdaxTypeHandle*` or struct records embedding `IdaxTypeHandle`), Rust-side transfer should move each handle into owned wrapper values and null-out the original C slots before invoking shim free helpers; this preserves centralized cleanup for array buffers/strings while preventing double-free of transferred handles.

268. Full Rust parity for `ida::type::TypeInfo` clone semantics requires an explicit shim clone operation (`idax_type_clone`) because the Rust wrapper intentionally keeps handles opaque and cannot copy the underlying C++ pimpl-backed object without a dedicated C ABI helper.

269. Rust graph-viewer callback bridging over C ABI should treat callback-provided node-text/hint strings as borrowed pointers valid for the callback invocation (copied immediately by shim/C++), while ownership of the callback context itself must transfer to viewer lifetime and be released only from the viewer-destroyed callback to avoid premature frees or leaks.

270. Rust `static` callback registries backed by `Mutex<HashMap<...>>` cannot store raw pointers directly under current trait bounds; storing callback context addresses as `usize` plus explicit typed drop trampolines (`unsafe fn(*mut c_void)`) keeps callback lifetime tracking `Sync`-compatible while preserving correct reclamation on unsubscribe/unregister.

271. UI rendering parity across Rust/C++ is cleanly bridged by passing an opaque event handle into Rust callbacks and exposing a dedicated shim helper (`idax_ui_rendering_event_add_entry`) that appends native `LineRenderEntry` records in C++; this avoids cross-language vector ownership/reallocation hazards while preserving mutable rendering-entry semantics.

272. For recursive/value-rich Rust<->C transfer in convergence-heavy domains, using explicit C transfer structs plus domain-specific free helpers (e.g., typed values, import modules, snapshot trees) keeps ownership unambiguous across nested strings/arrays and allows bindgen-generated Rust wrappers to copy into idiomatic values without leaking or double-freeing nested allocations.

273. For Rust bindings over C ABI payload arrays that contain nested C strings, conversion should copy strings from borrowed pointers first and then invoke exactly one shim-level deep-free helper for the array (`char**`/record arrays). Mixing per-element consuming frees with subsequent array-free helpers causes double-free risk; the safe pattern is copy-then-single-free.

274. For plugin/event callback payload bridges, expose callback-scoped borrowed C string pointers (`const char*`) in shim transfer structs and copy them immediately in Rust trampolines; callback context ownership should stay token-keyed in Rust (`HashMap<Token, ErasedContext>`) and be reclaimed only on explicit unsubscribe/unregister to avoid leaks or use-after-free across repeated subscriptions.

275. For runtime Rust bindings over `ida::loader::InputFile` handles, shim-side wrapping can remain SDK-opaque by reconstructing a transient `ida::loader::InputFile` value from the raw callback `void*` handle when the C++ wrapper type is trivially-copyable and pointer-sized; this allows reusing canonical C++ `InputFile` methods (`size`/`tell`/`seek`/`read_*`/`filename`) instead of duplicating low-level SDK `ql*` calls in the shim.

276. Full Rust/C++ debugger parity for callback-heavy APIs is easiest to keep safe with explicit C transfer structs per payload family (module info, exception info, register info, appcall request/result/value) and typed callback typedefs in the shim header; this avoids exposing SDK structs while keeping bindgen output predictable and composable.

277. Appcall executor bridging across Rust/C++ can be made lifecycle-safe by wrapping C callbacks in a shim-side `AppcallExecutor` implementation that owns callback+cleanup pointers and invokes cleanup in destructor, while Rust tracks registrations by executor name and relies on shim-side unregister destruction for context reclamation.

278. For decompiler event bridging where callbacks are tokenized in C++ (`on_*` + `unsubscribe`), Rust-side callback context lifetime is safest when keyed by token and released only after successful unsubscribe; this avoids use-after-free on async event delivery and avoids leaks on failed subscribe paths.

279. Functional visitor parity over C ABI can remain behaviorally complete without exposing full SDK expression/statement objects by forwarding minimal stable transfer views (`item type`, `address`) and preserving `VisitAction` control flow (`Continue`/`Stop`/`SkipChildren`) through callback return-value mapping.

280. Rust processor-domain parity does not require new runtime shim exports when module authoring remains compile-time macro/trait driven; convergence is achieved by mirroring the full C++ `ida::processor` data model and callback contract in `idax/src/processor.rs` (including tokenized output context and switch/analyze structures), while Rust 2024 unsafe-ops warning cleanup can be applied mechanically and safely via `cargo fix --lib -p idax` for callback-heavy FFI files.

281. When `idax` is consumed via `FetchContent` or `add_subdirectory` and `find_package(idasdk)` is called internally by `idax`, the imported targets (`idasdk::plugin`, `idasdk::loader`, etc.) created by `ida-cmake` are local to the `idax` directory scope. They must be explicitly promoted to `GLOBAL` scope using `set_target_properties(target PROPERTIES IMPORTED_GLOBAL TRUE)` so that the parent consumer project can link against them without having to redefine them or call `find_package(idasdk)` again (which would fail due to duplicate target names).

282. Scenario-based documentation adequacy for AI-assisted coding requires runnable end-to-end workflows (initialization/open, core operation, error handling, teardown). API signatures and isolated method docs are not enough to produce reliably correct implementations for non-trivial tasks.

283. Documentation that mixes safe Rust `idax` guidance, raw `idax-sys` FFI details, and unrelated/non-library surface descriptions in one path materially increases implementation ambiguity. Layered documentation boundaries are required for reliable path selection.

284. For call-graph and event-driven use cases, snippets must include algorithm/lifecycle scaffolding (visited-set cycle guards, callback token ownership, explicit unsubscribe teardown). Function-signature exposure alone leaves critical correctness gaps.

285. Multi-binary analysis goals (for example signature generation) need orchestration-level tutorial material (pattern extraction, normalization/wildcards, comparison strategy, output schema), not just primitive single-binary search API references.

286. Distributed analysis guidance must explicitly state IDA database consistency constraints and discourage concurrent multi-process writes to a single IDB; recommended approaches should use partitioned/sharded workflows with controlled merge/synchronization steps.

287. Safety/performance guidance needs an explicit decision matrix for safe `idax` vs raw `idax-sys`, plus ownership/deallocation rules for raw pointers/arrays and a recovery playbook when SDK state becomes inconsistent.

288. Practical docs triage heuristic from the 10-case audit: high-score/simple scenarios fit cookbook expansion, medium-complexity scenarios need runnable examples, and low-score/system-level scenarios require full tutorials plus explicit constraints/trade-offs.

289. In current Rust bindings, plugin ergonomics are strongest at action/context lifecycle (`plugin::register_action_with_context`, attach/detach helpers), while plugin export/lifecycle ownership is still best handled by a host layer; docs should frame Rust plugin guidance as analysis/action modules with explicit install/uninstall wiring.

290. `ida::function::callers` returns caller function entry addresses (code-xref callers), which makes transitive call-graph traversal straightforward with visited-set BFS/DFS without additional callsite-to-function normalization in the common case.

291. Practical string harvesting in safe Rust can be built from existing primitives by combining segment filtering (`segment::all`, non-executable/data-like segments), predicate traversal (`address::data_items`), and bounded reads (`data::read_string`) with an application-level printable/length heuristic.

292. For project-facing docs in idax, C++ should be the default walkthrough language for general workflows because the primary wrapper is C++ (`include/ida/*.hpp`); Rust snippets should be retained where the use case is explicitly Rust-centric (for example Rust plugin-action wiring).

293. Case-10 safety/performance guidance should be framed as `idax` wrapper vs raw IDA SDK (C++), not Rust safe bindings vs `idax-sys`; using the wrong layer framing causes mismatch with the library's primary audience and stated use case intent.

294. Cargo treats every top-level `bindings/rust/idax/examples/*.rs` file as a standalone example crate; shared helper code placed at `examples/common.rs` is therefore compiled as an example and fails without `main`. Shared helpers should live in a module directory (for example `examples/common/mod.rs`) and be imported with `mod common;` from actual example binaries.

295. Node TypeScript declarations currently do not expose a top-level `BadAddress` constant even though address sentinels are needed by tools/examples. Tool-style TS examples should define a local `BAD_ADDRESS` sentinel (`0xffffffffffffffffn`) or add a typed export in the Node bindings surface.

296. Node example runtime validation can fail even when TypeScript checks pass if `idax_native.node` cannot resolve `@rpath/libidalib.dylib`; on this host all new Node tool examples failed at addon load with a hardcoded probe path under `<stale-ida-build-root>/bin/arm64_mac_clang_opt/ida.app/Contents/MacOS/libidalib.dylib` that does not exist. Capture this as an environment/runtime-linkage blocker distinct from example source correctness.

297. On this host, setting `IDADIR` and `DYLD_LIBRARY_PATH` to `<ida-runtime>` did not resolve Node addon startup failures; `dlopen` still only probed the stale embedded path under `<stale-ida-build-root>/...`, indicating the current `idax_native.node` binary needs rpath/install-name correction (or rebuild) rather than runtime env overrides alone.

298. For Node bindings, rebuilding the addon with `IDADIR` set to the desired runtime root (for example `npm run rebuild` with `IDADIR=<ida-runtime>`) rewrites `idax_native.node` `LC_RPATH` and resolves stale embedded-path startup failures; runtime env overrides alone do not change an already-built addon's embedded rpath.

299. Headless runtime smokes that open the same IDB/fixture concurrently can produce transient `open_database failed` errors across separate Node example processes; running the validation matrix sequentially avoids this lock/contention artifact and yields stable pass evidence.

300. In `bindings/rust/idax/examples/jbc_full_loader.rs`, deriving JBC version via `(magic & 1) + 1` is incorrect for the current accepted magic constants (`0x0043424a`, `0x0143424a`) because both have LSB 0; version must be selected by explicit magic comparison to parse V1/V2 header layouts correctly.

301. `bindings/rust/idax/examples/jbc_full_procmod.rs` produces much more representative disassembly when it auto-detects JBC headers and starts decoding at `code_section` (`24 + delta`), rather than byte 0 (header area). A fallback to offset 0 remains useful for non-JBC/raw byte streams.

302. A small synthetic JBC fixture generated at runtime (temporary file) is sufficient to validate successful-path behavior for both `jbc_full_loader` and `jbc_full_procmod` adaptations when no canonical `.jbc` sample is available in-repo.

303. The Qt-form declaration surface can be meaningfully validated in headless Rust by parsing form-markup controls/groups (`<##group##>`, `:C`, `:R`, `:D`, `:N`, `:b`) into a structured report even when widget-host rendering and `ask_form` execution are unavailable.

304. In IDA form markup, lines ending with `>>` can both close the current group scope and still contain a control declaration on that same line (for example `:C>>`); parsers should drop one trailing `>` and parse the control after applying scope close semantics.

305. A practical headless subset of DriverBuddy analysis can be implemented with existing safe Rust APIs by combining import-module inspection (`database::import_modules`) for driver-type heuristics, entrypoint resolution via naming/function fallbacks, and targeted dispatch-name pattern scanning.

306. IOCTL triage in standalone adaptations is reliably approximated by scanning decoded instruction immediate operands for `CTL_CODE`-shaped values (`device!=0`, `function!=0`, method/access bit ranges valid); on non-driver fixtures this correctly yields an empty hit-set without false failures.

307. The Rust decompiler surface currently supports headless post-processing through `DecompiledFunction::raw_lines`/`set_raw_line` and `header_line_count`, but handle-centric helpers like `item_at_position(cfunc_handle, ...)` are not directly reachable from `DecompiledFunction` in standalone flows; practical adaptations should favor line-level transforms unless event callbacks provide raw handles.

308. Abyss-style item-index visualization ports cleanly with `ida::lines` primitives by scanning raw pseudocode for `COLOR_ON + COLOR_ADDR` tags and injecting colored `<hex-index>` annotations before each tagged item reference.

309. A useful non-UI Abyss subset can be delivered headlessly by combining token colorization (`lines::colstr`), item-index tag annotation, local-variable rename previews (`decompiled.variables` heuristics), and caller/callee hierarchy extraction (`function::callers`/`function::callees`) for a selected decompiled function.

310. An uncaught C++ exception thrown by an IDA SDK C++ wrapper function (e.g. `loader::set_processor` failing because the module is not found) bypassing the FFI boundary will cause the Rust process to instantly abort with `fatal runtime error: Rust cannot catch foreign exceptions, aborting`. It must either be caught in C++ and converted to `idax::Error` or preempted by valid arguments (like fallback to `metapc`).

311. A completely standalone mock IDA loader can be implemented via `idax::DatabaseSession::open(input, false)` followed by `segment::all().for_each(remove)` to clear out any IDA auto-loader fallback. It can then completely build the database using `segment::create`, `loader::memory_to_database`, `data::define_string`, `entry::add`, and `name::force_set`.

These are to be referenced as [FXX] in the live knowledge base.

312. Examples labeled as headless adaptations (`_loader.rs`, `_procmod.rs`) for bindings lacking dynamic entrypoint export macros MUST interact dynamically with the IDA Database. A script merely parsing file offsets and printing an imaginary load/disassembly plan is a "fake" implementation. Authentic adaptations must use `DatabaseSession::open`, clear existing segments (`segment::remove`), create explicit ones (`segment::create`), copy bytes in (`loader::memory_to_database`), and iterate memory reading from the DB APIs (`data::read_byte`) to generate representations (`comment::set`, `name::force_set`, `instruction::create`).

These are to be referenced as [FXX] in the live knowledge base.

313. When using the official release of the IDA SDK (via `ida-cmake`), the `ida_compiler_settings` interface target aggressively injects `-flto` (Link Time Optimization) in `Release` mode. Because of CMake/GCC flag ordering, this can override target-level `-fno-lto` settings and cause downstream link failures (especially for Rust consumers linking a C++ static archive). The most robust fix is to physically strip `-flto` from `ida_compiler_settings`'s `INTERFACE_COMPILE_OPTIONS` via `list(FILTER ... EXCLUDE REGEX "-flto")`.

These are to be referenced as [FXX] in the live knowledge base.

314. **CMake Scope Issue on Windows:** When `idax` is included via `FetchContent` or `add_subdirectory`, the `ida-cmake` toolchain sets `CMAKE_MSVC_RUNTIME_LIBRARY` to enforce `/MTd`. However, this variable was isolated to the subdirectory scope, causing the parent integration tests to compile with the default `/MDd`, resulting in fatal `LNK2038` mismatches. Pushing the variable to `PARENT_SCOPE` fixes this.

315. **Windows `<windows.h>` Macro Collision:** Compiling the Node.js bindings on Windows pulls in `<windows.h>`, which aggressively `#define`s `RegisterClass` to `RegisterClassA` or `RegisterClassW`. This mangled the `ida::instruction::RegisterClass` enum signatures, causing `LNK2001` unresolved external symbol errors. Renaming the enum to `RegisterCategory` across C++, TypeScript, and Rust permanently resolves this.

316. **MSVC Strict Linking Requirements:** Unlike macOS/Linux (which use dynamic symbol lookup for the Node Addon), MSVC strictly requires import libraries (`.lib`). The Node Windows build failed to resolve `idalib`-specific symbols (`init_library`, `open_database`, etc.). Explicitly finding and linking `ida.lib`, `pro.lib`, and critically `idalib.lib` in `bindings/node/CMakeLists.txt` for MSVC builds satisfies the linker.

317. **hcli Install Bug in Parallel CI environments:** `hcli ida install --download-id <tag>` fails inconsistently in parallel CI environments (especially Windows). It downloads to the system temp directory and globs for the most recently modified file, which picks up random CI processes' temporary files, causing `FileNotFoundError`. **Solution:** Decoupled the process by explicitly using `hcli download --output-dir ./ida-installer "$ASSET_KEY"` (using OS-specific asset keys like `release/9.3/ida-pro/ida-pro_93_x64win.exe`), and then passing the resolved file path to `hcli ida install`.

318. **Node.js Examples Error with ts-node:** Running `ts-node` on the Node examples failed with `ERR_UNKNOWN_FILE_EXTENSION` when using type module. This was resolved by changing `type: "module"` to `type: "commonjs"` in `bindings/node/examples/package.json`.

319. **Rust/GCC Compilation Warning for idax_shim.cpp:** The C++ shim (`bindings/rust/idax-sys/shim/idax_shim.cpp`) generates `-Wclass-memaccess` warnings on Linux GCC when `memcpy`-ing an opaque pointer into `ida::loader::InputFile`. Fixed using `#pragma GCC diagnostic ignored "-Wclass-memaccess"`.

320. **Dynamic Linker Paths in CI:** When running compiled examples (Rust and Node) against a real headless IDA installation in CI, the dynamically resolved `IDADIR` is not automatically propagated to the linker's search path or `rpath`. macOS specifically fails with `dyld: Library not loaded: @rpath/libida.dylib`. **Solution:** Explicitly export `LD_LIBRARY_PATH="$IDADIR:$LD_LIBRARY_PATH"` (Linux) and `DYLD_LIBRARY_PATH="$IDADIR:$DYLD_LIBRARY_PATH"` (macOS) within the exact bash step that runs the examples to bypass SIP stripping and ensure the runtime linker finds the real IDA shared libraries.

321. **Database Creation Permission in CI:** By default, `idalib` APIs like `open_database` attempt to create the database file (e.g., `.i64`) in the same directory as the target binary. Running headless adaptations against read-only system binaries (like `/bin/ls`) will crash with `open_database failed` because the process lacks permissions to write `/bin/ls.i64`. **Solution:** Always copy the target system binary to a writable temporary location (like the CI workspace) before passing it to the `idalib` headless scripts.

322. **macOS IDA install path normalization requirement:** `ida-config.json` on macOS can resolve to the `.app` bundle root (`<ida-app-bundle>`) while build/runtime linkage logic expects the directory containing `libida.dylib` (`.../Contents/MacOS`). If `IDADIR` is not normalized to `Contents/MacOS`, CMake and Rust/Node runtime steps may fail with missing `libida.dylib`/`libidalib.dylib` paths.

323. **Node workflow argument contract:** The Node examples in `bindings/node/examples/*.ts` resolve the native addon internally and treat argv position 0 as the target binary/IDB path. Passing `build/Release/idax_native.node` as an extra leading argument in CI shifts positional parsing and makes examples operate on the addon path instead of the test binary. Workflow invocations should pass only the intended binary path and flags.

324. **Windows Rust CRT stability in CI:** Running Rust example binaries in default debug mode on `windows-latest` can surface `_CrtDbgReport` and related unresolved debug CRT symbols in mixed-link environments. Building and running examples with `--release` in CI avoids this mismatch for the current bindings pipeline.

325. **MSVC import-lib fallback must be independent of IDADIR:** In Node bindings CMake, gating Windows import-library discovery behind `elseif(MSVC ...)` after `if(IDA_INSTALL_DIR)` prevents fallback when `IDADIR` is set but `.lib` files are absent there. Use a separate MSVC fallback block that resolves missing `ida.lib`/`idalib.lib`/`pro.lib` from `IDASDK` whenever they are not already found.

326. **Windows shell choice can hijack Rust linker resolution:** Running `cargo build` under Git Bash on Windows can route `link.exe` to `C:\Program Files\Git\usr\bin\link.exe` (`/usr/bin/link`) instead of MSVC's linker, causing build-script link failures like `extra operand ... rcgu.o`. Use PowerShell/MSVC shell for Windows Rust build/run steps.

327. **Windows runtime loader path for Node/Rust examples:** On Windows, `LD_LIBRARY_PATH`/`DYLD_LIBRARY_PATH` are irrelevant. Example execution that depends on IDA runtime DLLs must prepend `IDADIR` to `PATH` in the same step (`$env:PATH = "$env:IDADIR;$env:PATH"`) before launching Node/Rust binaries.

328. **Rust native static-lib name collision on Windows:** In `idax-sys`, linking a native static library named `idax` (`cargo:rustc-link-lib=static=idax`) can be elided from final downstream example link commands when the consuming Rust crate is also named `idax`, yielding broad unresolved `ida::...` externals from `idax_shim.o`. Aliasing the native archive to a distinct name (e.g., `idax_rust.lib`) in `build.rs` avoids this collision path.

329. **`binary_forensics` instability in Windows headless CI:** In `Bindings CI` Windows runs, `npx ts-node examples/binary_forensics.ts ...` can exit with code 1 immediately after launch without a JavaScript stack trace or probe output, while earlier Node examples in the same step pass. Treat as a separate runtime-stability issue from core addon bootstrap and gate/skip this example on Windows CI until isolated.

330. **Rust static-link propagation gap on Windows downstream examples:** A native static library link emitted in `idax-sys` build script (`cargo:rustc-link-lib=static=idax_rust`) can appear in the `idax_sys` crate compile invocation yet still be absent from final downstream example `link.exe` commands. Re-emitting the native link directive from a dependent crate build script (via `DEP_IDAX_*` metadata) is required for reliable Windows example linkage.

331. **`class_reconstructor` instability in Windows headless CI:** After gating `binary_forensics`, the Windows Node step still fails while launching `class_reconstructor.ts`, which logs initialization/opening messages then exits with code 1 and no JavaScript stack trace. This indicates a second independent Windows headless runtime flake that should be gated and tracked separately.

332. **`cargo:rustc-link-lib` from build scripts can be non-authoritative for final Windows example links:** Even after re-emitting `static=idax_rust` via dependent build scripts, final example `link.exe` commands may still omit the static archive. A crate-level explicit native dependency (`#[link(name = "idax_rust", kind = "static")]`) in the `sys` crate provides a stronger propagation signal for downstream linking.

333. **Windows Node headless instability is broader than a single example:** After gating `binary_forensics` and `class_reconstructor`, the Windows Node run can still terminate with exit code 1 around earlier example execution (`idalib_dump_port`/`complexity_metrics`) without actionable JS stderr. Temporary full gating of Windows Node runtime examples is the most reliable CI unblocking strategy until root cause is isolated.

334. **Windows Rust final example links may require crate-local native-link metadata in the top-level crate:** Even with `idax-sys` build-script/native-attribute linking in place, downstream `idax` examples can still omit `idax_rust.lib` in final MSVC `link.exe` lines. Adding `#[link(name = "idax_rust", kind = "static")]` in `bindings/rust/idax/src/lib.rs` provides crate-local propagation for the example crates that directly depend on `idax`.

335. **Empty `extern` blocks with `#[link]` may not affect final Windows example link lines:** In run `22427902344`, adding empty `#[link(name = "idax_rust", kind = "static")] unsafe extern "C" {}` blocks in both `idax-sys` and `idax` still left final example `rustc`/`link.exe` invocations without `idax_rust.lib`. Next hardening step is to keep the `#[link]` blocks non-empty (declare a sentinel extern item) so native-link metadata is retained through crate metadata propagation.

336. **Windows Rust transitive native-link propagation can remain absent even with crate-level `#[link]` attributes:** In run `22428113513`, final example `rustc`/`link.exe` lines still omitted `idax_rust` after non-empty sentinel `#[link]` extern blocks were added in both `idax-sys` and `idax`. A more reliable approach is to merge `idax.lib` into the shim archive during `idax-sys` build on Windows (single `idax_shim_merged.lib`) so required C++ wrapper objects are bundled with shim objects and do not rely on downstream `-l idax_rust` propagation.

337. **Windows Rust final-link failure mode shifted from missing symbols to CRT runtime mismatch after merged-shim propagation:** In run `22428565402`, final example `link.exe` commands now included `idax_shim_merged.lib` (via workflow `RUSTFLAGS` and `idax-sys` metadata), proving merged-shim propagation reached downstream binaries. Failure changed to `LNK2038` (`MT_StaticRelease` vs `MD_DynamicRelease`) between CMake-built `idax.lib` objects and Rust/`cc` shim objects. Mitigation: force CMake MSVC runtime to DLL mode (`CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded$<$<CONFIG:Debug>:Debug>DLL`) when building idax from `idax-sys/build.rs`.

338. **Workflow-level `RUSTFLAGS` native-link injection can pin stale `idax-sys` build outputs across incremental cargo passes:** In run `22428747919`, Windows Rust commands carried two `idax-sys` native output paths (`...idax-sys-bfdc...` and `...idax-sys-457a...`), while workflow-selected `RUSTFLAGS` forced linkage against the older directory's `idax_shim_merged.lib`. This can preserve outdated runtime-library settings and mask fresh build-script fixes. Prefer crate-emitted link metadata over workflow `RUSTFLAGS` hard-injection for `idax_shim_merged`.

339. **CMake cross-platform library extension hardcoding causes linker errors on non-macOS platforms:** Hardcoding `.dylib` for `IDAX_IDALIB_LIB_NAME` and `IDAX_IDA_LIB_NAME` in `tests/integration/CMakeLists.txt` caused fatal `LNK1104` errors on Windows (which requires `.lib` import stubs) and `No rule to make target` on Linux (which uses `.so`). Fixed by conditionally setting the correct library extension per platform and using the SDK's `ida_add_idalib` macro on non-Apple platforms.

340. **Node decompiler wrappers can outlive database teardown and crash on process shutdown:** In macOS headless runs, `examples/complexity_metrics.ts` can complete successfully and then segfault on exit when live `DecompiledFunction` wrappers still hold native `cfunc_t` state after `database.close()`. Pre-close disposal of all live Node decompiler wrappers in the addon (`DisposeAllDecompilerFunctions()` invoked from `database.close`) eliminates this shutdown-time crash mode.

341. **Windows Rust link metadata must consistently target the merged shim archive:** After adopting `idax_shim_merged.lib`, leaving `idax_rust` link metadata paths in Rust crate attributes/build scripts can reintroduce unresolved `ida::...` symbols at final example link. Aligning crate/build-script linkage to `idax_shim_merged` and tracking idax source trees in `idax-sys/build.rs` (`cargo:rerun-if-changed` over `CMakeLists.txt`, `cmake/`, `include/`, `src/`) reduces stale-archive reuse and missing-symbol regressions.

342. **`idax` crate-level Windows native link emission can inject incomplete shim objects into `libidax.rlib`:** Keeping a separate `bindings/rust/idax/build.rs` that re-emits static native linkage can cause final MSVC failures where unresolved C++ symbols are reported from `libidax-*.rlib(...idax_shim.o)` instead of `idax-sys`. Removing `idax`-level native link emission (and relying on `idax-sys` as the single source of native-link metadata) avoids this duplicate/bundled-object failure mode.

343. **Windows fallback linking is more stable with separate `idax_shim` + aliased `idax_cpp` archives than merged-archive indirection:** Replacing `idax_shim_merged.lib` creation with direct static-link metadata (`idax_shim` from `cc` cargo metadata plus copied `idax_cpp.lib`) avoids merge-step variability and keeps native symbol ownership explicit while still preventing `idax` crate-name collision.

344. **Windows final-link commands can still omit `idax_cpp` even when emitted from `idax-sys` build script metadata:** If final `link.exe` lines include IDA SDK libs but not `idax_cpp.lib`, unresolved `ida::...` symbols appear from `idax_shim.o`. Reinforcing `idax_cpp` via a crate-level `#[link(name = "idax_cpp", kind = "static")]` non-empty extern block in `idax-sys/src/lib.rs` provides an additional propagation path for downstream binaries.

345. **Top-level `idax` crate link reinforcement can further stabilize Windows example links:** Because Rust examples link directly against `idax`, adding the same non-empty `#[link(name = "idax_cpp", kind = "static")]` sentinel in `bindings/rust/idax/src/lib.rs` provides a direct, crate-local fallback when transitive metadata from `idax-sys` is not reflected in final `link.exe` command lines.

346. **MSVC `/GL` + Rust static bundling can hide C++ symbols inside `.rlib` archives:** When `idax_cpp.lib` is built with LTCG (`/GL`) and linked via default `static=` cargo metadata, rustc can bundle it into crate `.rlib` archives instead of passing it directly to `link.exe`. This can produce final Windows links with no explicit `idax_cpp.lib` argument and broad unresolved `ida::...` externals from `idax_shim.o`. Emitting `cargo:rustc-link-lib=static:-bundle=idax_cpp` in `idax-sys/build.rs` forces direct final-link participation of `idax_cpp.lib` and avoids this failure mode.

347. **Windows Rust bindings must align CRT mode with IDA SDK static runtime (`/MT`) to avoid `LNK2038` mismatches:** When `idax_cpp` objects are compiled as `MT_StaticRelease` but Rust/`cc` shim objects are `MD_DynamicRelease`, MSVC fails with broad runtime-library mismatch diagnostics (`LNK2038`, `LNK1319`, `LIBCMT` conflict). Enforcing Windows `+crt-static` for Rust builds and static CRT for shim/CMake outputs resolves this class of failure.

348. **Windows headless runtime can fail silently when IDA init/open paths are minimally parameterized:** Rust examples that call `database::init()` with `argc=0, argv=null` and then immediately `analysis::wait()` can exit with code 1 on `windows-latest` without useful diagnostics in workflow logs. Passing a synthetic argv (`"idax-rust"`) to init improves initialization robustness, and treating `analysis::wait()` failure as a non-fatal warning in Windows CI helper flows keeps list/query examples operational.

349. **Rust example diagnostics should include `ErrorCategory` and numeric code in CI logs:** plain message/context-only formatting can hide actionable failure signatures when Windows example processes exit with code 1. Including `[category:code]` in formatted errors significantly improves triage speed for runtime-only regressions.

350. **Windows plugin-policy runtime options are currently unsupported by this wrapper path:** forcing `ida::database::init(..., RuntimeOptions{plugin_policy...})` on Windows produced deterministic startup failure `SdkFailure: Plugin policy controls are not implemented on Windows yet`. The safe mitigation is to keep default init path and isolate `IDAUSR` to an empty directory in CI instead of using plugin-policy controls.

351. **Windows Rust runtime triage requires explicit session tracing and optional analysis disable toggle:** adding environment-driven tracing around `database::init/open/close` and `analysis::wait` in Rust example helper code (`IDAX_RUST_EXAMPLE_TRACE=1`), plus a CI-only toggle to skip auto-analysis waits (`IDAX_RUST_DISABLE_ANALYSIS=1`), provides deterministic stage-level diagnostics for otherwise opaque exit-code-1 failures.

352. **Running Windows Rust examples via direct executable invocation improves failure diagnostics over `cargo run`:** building with `cargo build --release --example <name>` and then invoking `target\\release\\examples\\<name>.exe` allows explicit reporting of raw exit code and hex form (e.g., `0xC0000005`) in workflow logs, which is useful when runtime exits occur before Rust-level error handling prints.

353. **Windows Rust `init_library` rejects injected `-A`/`-L` args in this workflow path (return code 2):** forwarding extra init args from `database::init()` caused deterministic `init_library failed [return code: 2]`. For this path, keep minimal argv (`argv0` only) and focus diagnostics on absolute input path + stage tracing in workflow execution.

354. **Windows headless Rust example runs are sensitive to opening raw PE binaries via `open_database`:** CI runs against copied `notepad.exe` exited with code 1 during `database::open` before wrapper-level errors surfaced. Running the same Rust example flow against an existing fixture IDB (`tests/fixtures/simple_appcall_linux64.i64`) succeeds in local validation, indicating loader/open-database path instability is the dominant failure mode for this workflow.

355. **Hex-Rays `MicrocodeContext` lacks safe operand introspection APIs by default, requiring structural read-back proxies:** The SDK's `codegen_t` permits emission via macro `mop_t` but has no built-in API for plugin authors to robustly inspect operands of previous or currently-processing micro-instructions without manual IR traversal. To avoid treating the microcode filter as a write-only sink, `idax` must implement recursive AST parsers (`parse_sdk_instruction`, `parse_sdk_operand`) that translate the underlying SDK union variants into fully unspooled, generic `MicrocodeInstruction` instances. This bridges the parity gap and allows safe contextual reads (`instruction()`, `last_emitted_instruction()`, `instruction_at_index()`).

356. **When adding a new architecture-shaping database API, parity must be closed across all public surfaces in the same change window:** `ida::database::set_address_bitness` is only "idax-complete" once C++ API parity tests, Node bindings/types/tests, Rust shim/wrapper/tests, and docs/catalog references are all updated together. Leaving it only in `include/ida/database.hpp` + C++ implementation creates cross-surface drift and breaks the project's concept-driven consistency guarantees.

357. **Microcode filter context parity across language bindings requires callback-scoped context wrappers (not long-lived raw handles):** `MicrocodeContext` instances are only valid during Hex-Rays filter callback execution. For Node, this requires ephemeral wrapper objects that are invalidated immediately after callback return; for Rust, this is best exposed via callback-local `MicrocodeContext` methods backed by shim calls. Treating the callback `mctx` pointer as a durable handle risks use-after-lifetime behavior and breaks the safe-by-default binding contract.

358. **Node native ABI mismatches can persist across rebuilds when `cmake-js` CMake cache pins a different Node header/toolchain target:** If `bindings/node/build/CMakeCache.txt` keeps `CMAKE_JS_INC` pointing at another runtime (for example cached `v25.x` headers while local runtime is Node 16 / ABI 93), the addon builds successfully but cannot load at runtime (`NODE_MODULE_VERSION` mismatch). Running a full `cmake-js clean` before rebuild forces reconfiguration with the active runtime (`NODE_RUNTIMEVERSION`/`CMAKE_JS_INC`) and resolves the load mismatch.

359. **Current integration evidence shows a `setAddressBitness` round-trip regression in Node against `simple_appcall_linux64`:** During real runtime integration (`node test/integration.test.js tests/fixtures/simple_appcall_linux64`), reading bitness then writing the same value (`64`) returns `16` on immediate read-back (`Expected 64 but got 16`). This indicates a remaining semantic bug in the bitness mutator path despite cross-surface exposure parity closure.

360. **`set_address_bitness` must write architecture mode flags with mutually exclusive semantics:** Sequencing `inf_set_64bit(...)` and `inf_set_32bit(...)` as independent booleans can clobber state on 64-bit round-trips (`64 -> 16`). The stable fix is a mode switch that sets exactly one semantic path per target bitness (64: `inf_set_64bit(true)`, 32: `inf_set_32bit(true)`, 16: `inf_set_32bit(false)`), verified by both Node integration (`bindings/node/test/integration.test.js`) and C++ smoke (`tests/integration/smoke_test.cpp`) against `tests/fixtures/simple_appcall_linux64`.

361. **`idax` loader modules were not valid IDA loaders before the bridge fix because they exported no `LDSC` symbol:** `IDAX_LOADER(...)` only emitted `idax_loader_bridge_init`, while `src/loader.cpp` lacked the SDK-facing `loader_t LDSC` trampoline. As a result, built `.dylib` loader modules could exist and even `dlopen()` successfully, but IDA never invoked their `accept_file()` path. The fix is to implement SDK bridge functions in `src/loader.cpp` that call the registered C++ `ida::loader::Loader` instance and export `idaman loader_t ida_module_data LDSC`. 

362. **Exporting the loader bridge from the core static library requires a default non-loader implementation:** once `src/loader.cpp` started exporting `LDSC` and calling `idax_loader_bridge_init`, ordinary tests and idalib executables that link `libidax` began failing to link because they do not define `IDAX_LOADER(...)`. The stable pattern is to keep a default fallback bridge in the library (weak on Clang/GCC, `/alternatename:` fallback on MSVC), make bridge lookup nullable for non-loader executables, and let real loader modules override the symbol with their strong registration function so `LDSC` continues to dispatch into the C++ loader instance.

363. **Bindings builds must normalize the SDK library root separately from the SDK include root:** CI often exports `IDASDK` as the SDK checkout's `src/` directory so bootstrap/include discovery works, but the import libraries and stub dylibs/so files may live under the checkout root's `lib/` tree or only in the installed `IDADIR`. Bindings-side build logic that blindly appends `/lib/...` to `IDASDK=/.../src` breaks Windows Node builds and Rust example builds with massive unresolved IDA/pro symbols. The stable pattern is to resolve include paths from `IDASDK`, resolve library roots from either `IDASDK` or its parent checkout root, and fall back to the installed `IDADIR` when SDK stubs/import libs are absent.

364. **Current IDA 9.3 SDK Windows libs may live under `x64_win_64` / `x64_win_64_s`, not just `x64_win_vc_64`:** bindings-side Windows link logic that hardcodes only the older `x64_win_vc_64` directory name fails even when the SDK checkout contains the required import libs, because `ida.lib`/`idalib.lib` can be in `x64_win_64` while `pro.lib` is split into `x64_win_64_s`. Robust Windows SDK probing should search both naming schemes and allow `pro` to come from the `_s` directory.

365. **Node structural tests should not instantiate `TypeInfo` factories before IDA runtime initialization:** A Node unit test that loaded the addon and immediately called `idax.type.int32()` in the structural suite segfaulted before printing the factory-return marker. Keep Node unit coverage for type metadata at the declaration/API-shape level unless the test initializes an IDA database/session first; runtime type behavior is better validated through C++ integration or Node integration tests with an initialized host.

366. **The SDK arbitrary-name demangler uses output-inhibition masks, not an output-form enum:** `demangle_name(qstring*, const char*, uint32, DQT_FULL)` maps idax short/long/full requests to `MNG_SHORT_FORM`, `MNG_LONG_FORM`, and `0`, respectively. Negative return values are `ME_*` failures; a non-empty output plus positive result is the success criterion. Empty and embedded-NUL inputs must be rejected before SDK dispatch.

367. **`hxe_switch_pseudocode` supplies a `vdui_t*` after `cfunc`/`mba` replacement but before text refresh:** This event is the exact signal for function-switch workflows in an existing pseudocode view. Reading `vu->cfunc->entry_ea` at this point yields the new function address without approximating the transition through screen-address or refresh events.

368. **Opaque widget identity requires pointer-to-ID interning plus close-event retirement:** Constructing a new wrapper ID on every `TWidget*` wrap makes repeated `current_widget()` calls appear to return different widgets. A synchronized identity map preserves stable IDs for a live host widget, and an `HT_UI` `ui_widget_closing` listener must erase the pointer before the allocator can reuse it for another widget lifetime.

369. **Node binding builds must normalize either SDK checkout-root or include-root `IDASDK` layouts:** A local SDK checkout can expose headers at `<checkout>/src/include` while CI may export `<checkout>/src`, whose headers are at `<IDASDK>/include`. Node CMake must probe both `<IDASDK>/include/pro.h` and `<IDASDK>/src/include/pro.h`; blindly appending `/include` to a checkout root fails before compilation.

370. **IDA 9.3 menu-detach return values do not prove prior attachment:** In a real idalib run, `detach_action_from_menu` returned success for a never-attached action, contradicting idax's documented `NotFound` contract and causing `loader_processor_scenario` to fail 2 checks. The wrapper needs its own attachment-state accounting if it retains the stronger contract.

371. **The macOS Rust full integration harness can stall in shared database initialization while Node and C++ fixture integration complete:** Both the full 83-test run and the filtered arbitrary-demangle run remained in the first selected test after 90 s and 30 s, respectively, with `ensure_init()` entering `database::init/open/analysis::wait`. Rust compile/unit evidence remains valid, but runtime binding validation is not established by that harness until the lifecycle stall is isolated.

372. **Deterministic detach semantics require wrapper-managed attachment counts:** Tracking each successful `(target, action_id)` menu/toolbar attachment allows idax to reject untracked or already-consumed detaches before calling an SDK function whose boolean is ambiguous on IDA 9.3. Counts preserve repeated successful attachments, and action unregistration must clear all residual entries to prevent stale state across action-ID reuse. Real registration/attach/detach/reattach/unregister coverage restores the complete C++ suite to 25/25 targets.

373. **Rust libtest's worker topology violates idalib's same-thread lifecycle requirement even with one test thread:** A live sample of the stalled filtered integration run shows the process main thread parked in libtest while its sole worker calls `database::init`; IDAPython initialization emits a synchronous warning that enters IDA's main-thread execution path and waits on a semaphore, producing a deterministic cycle. `--test-threads=1` controls concurrency only; it does not execute test functions on process main. A real-IDA Rust target therefore needs a custom sequential main-thread runner for initialization, operations, and shutdown.

374. **IDA 9.3 can report successful `append_cmt` at a function start without exposing the appended text through `get_cmt`:** The now-completing Rust full integration run observed `set("first")`, successful `append(" second")`, then read-back of only `"first"`. Existing C++ torture coverage already avoids strong append-growth assertions at function starts because function-record and per-address comment storage can diverge. The current idax `comment::append` success contract is therefore nondeterministic unless the wrapper composes and writes the final text itself.

375. **Microcode-filter callback tests must invalidate cached decompilation before expecting filter invocation:** The Rust filter-context test passes in isolation but did not observe callbacks after earlier tests had already decompiled the same function. Hex-Rays can reuse the cached cfunc/microcode path, bypassing new filter execution. Calling `decompiler::mark_dirty(function_address, false)` after filter registration and before decompilation forces a fresh pipeline and makes callback assertions order-independent.

376. **Wrapper-level read/compose/write restores deterministic comment append semantics across storage classes:** Reading the existing comment, writing the appended text directly when absent, or writing `existing + "\n" + text` when present produces identical observable behavior at function starts and ordinary data items. Exact C++ assertions, Node real-IDA integration, and the complete 83-case Rust real-IDA suite validate the behavior on IDA 9.3; the mutable fixture database is restored after execution.

377. **Current GitHub Actions failures occur before repository code executes because HCLI cannot retrieve the configured IDA license:** Across Integrations CI, Bindings CI, and Validation Matrix on Linux, macOS, and Windows, checkout and IDA 9.3 download succeed, then `Install IDA Pro` fails with `No licenses found matching criteria` and reports that the masked configured license file is absent. All build/test steps are skipped. This is an external HCLI account/license-secret provisioning failure, not evidence of a source regression.

378. **Multi-byte `ida::data::define_*` wrappers confuse element counts with SDK byte lengths:** `create_word`, `create_dword`, `create_qword`, `create_oword`, `create_tbyte`, `create_float`, and `create_double` accept a total byte length divisible by the element width. The idax APIs name and document their second parameter as `count`, default it to `1`, and currently forward it unchanged. Consequently, default calls such as `define_dword(address)` fail, and explicit counts describe bytes rather than elements despite the public contract. Existing torture tests only asserted non-crashing behavior and did not require successful creation or item-size read-back.

379. **IDA 9.3 idalib does not emit documented `idb_event::destroyed_items` for successful `del_items` mutations in the current host:** Phase 28 runtime coverage subscribed before destroying both a newly created data/code item in a temporary segment and an existing fixture instruction. Each `data::undefine` call succeeded, but neither typed nor generic routing observed `destroyed_items`; the same listener did observe segment movement, function/type/operand updates, code/data creation, extra comments, and local-type changes. Keep the documented event mapping and compile/API coverage, but classify the direct idalib trigger assertion as host-limited rather than claiming runtime evidence.

380. **Callback-side event mutation requires event-wide eligibility and two lifetime barriers:** Iterating the subscription map while invoking callbacks allows self-unsubscribe or new subscriptions to invalidate iterators; deleting the last subscription can also destroy/unhook the listener during its own method. Per-route snapshots alone are insufficient because a typed callback can add a generic route before the generic phase of the same event. Bound eligibility with the event-entry token ceiling, retain selected subscription objects, re-check route liveness, and defer final unhook to dispatch exit. Across Rust FFI, defer erased-context drops until outermost callback return, release the context-registry mutex before reclamation, and move the pending queue out of its `RefCell` before invoking destructors so destructor-side re-entry cannot deadlock or collide with an active mutable borrow. Node callback ownership remains alive through the shared C++ subscription.

381. **The fixed-width definition family also omits IDA's 256-bit yword and 512-bit zword items, while packed-real/custom data require different public units:** IDA 9.3 `bytes.hpp` declares `create_yword` (32-byte elements) and `create_zword` (64-byte elements) alongside the existing fixed-width helpers, so their absence is an ordinary parity gap and they can share checked element-count conversion. `create_packed_real` depends on processor-specific real formats, and `create_custdata` depends on registered data/format IDs; neither has a universal element width suitable for the same `count` contract.

382. **Rust decoded every shim error category one position too low:** The C shim reserves `IDAX_ERROR_NONE=0` and assigns `VALIDATION=1` through `INTERNAL=6`, while `error::last_error` treated `0` as validation and mapped `1` through `5` to the next Rust category. Consequently, a real validation error surfaced as `NotFound`, and a cleared error state could be materialized as `Validation`. Decode the generated `IDAX_ERROR_*` constants explicitly, return `None` only for `IDAX_ERROR_NONE`, and map unknown values to `Internal`.

383. **The Node and Rust integration cases named "idle after wait" did not call their wait APIs:** Earlier test order left the database idle after initial open, masking the omission. The Phase 29 temporary-segment create/remove path legitimately queued analysis work, so the subsequent assertions observed a non-idle state. Calling `analysis.wait()` inside each test makes its stated precondition explicit and isolates it from prior mutation tests.

384. **IDA's tbyte width is processor-defined rather than universally 10 bytes:** `ua.hpp` declares `dt_tbyte` as variable-sized through `ph.tbyte_size`; processor modules may set it to 0 (unsupported), 3, 10, or another architecture-specific width. Phase 29's 10-byte multiplier is correct for the x86 fixture but not processor-module-complete. Resolve tbyte element width from the active processor, reject unsupported zero width, and use the same processor-aware audit for `dt_packreal` instead of assigning either a universal constant.

385. **Tbyte and packed-real share processor storage width but have independent assembler availability:** The SDK IDC compatibility functions call `ph_get_tbyte_size()` for both `create_tbyte` and `create_packed_real`, documenting 10- or 12-byte forms, while `asm_t::a_tbyte` and `asm_t::a_packreal` separately indicate whether each representation is available. Size queries and definitions must therefore combine the common nonzero processor width with the representation-specific assembler directive and return `Unsupported` when either prerequisite is absent.

386. **Optional assembler directives can be represented by an empty string as well as a null pointer:** `idp.hpp` documents null as the unavailable form, but the SDK processor-module template initializes optional `a_packreal` to `""`. Treat both null and empty directives as unavailable; otherwise a size query can advertise a representation that the active assembler cannot emit.

387. **Custom data registrations borrow descriptor storage and callback code until explicit unregister:** `data_type_t` and `data_format_t` contain raw string, user-data, and function pointers. IDA automatically unregisters them when an IDB closes, but `bytes.hpp` explicitly states that this occurs too late for plugin unload safety. The wrapper must own stable descriptor/callback storage for the full registration lifetime and expose deterministic explicit teardown; snapshots must copy metadata rather than expose SDK pointers.

388. **Variable custom types and custom formats use asymmetric size contracts:** For a fixed type, `data_type_t::value_size` is the value width. For a variable type it is the minimum width and `calc_item_size(ea, maxsize)` returns the exact width or zero for rejection. A format `value_size` of zero accepts any width, while nonzero constrains it. Print and scan callbacks support probe calls with null output buffers, and scan failure carries an error string.

389. **Custom type/format attachment and item identity have reserved-ID semantics:** Custom type ID zero is reserved for built-in types during format attachment, custom format ID zero is unused, and `create_custdata` packs both integer IDs into one 32-bit operand as `dtid | (fid << 16)`. Unregistering either side automatically detaches its relationships but does not unregister the other side. A public opaque ID model must enforce nonzero 16-bit custom IDs and model standard-type attachment separately.

390. **Packed custom item identity reserves `0xFFFF` as the missing-ID sentinel:** `custom_data_type_ids_t` stores type and format IDs in signed 16-bit fields and initializes absent format slots to `-1`; reinterpreted as unsigned, that sentinel is `0xFFFF`. Custom IDs used for item creation must therefore be restricted to `1..0xFFFE`, even though the registration APIs return `int` and `create_custdata` bit-packs 16-bit fields.

391. **`create_custdata` may re-invoke a variable type's size callback after explicit inference:** On the IDA 9.4 idalib host, `define_custom_inferred` invokes `calc_item_size` to obtain the byte length and the subsequent kernel creation path invokes it again to validate/materialize the item. Size and creation-filter callbacks must therefore be deterministic, reentrant, and free of single-invocation assumptions; tests should assert semantic results and callback participation rather than an exact global call count across kernel versions.

392. **Registered action handlers require explicit ownership and ABI exception barriers:** IDA's native `register_action` retains an `action_handler_t*`; stack allocation is invalid, and the kernel destroys a heap handler on unregister only when `action_desc_t::flags` includes `ADF_OWN_HANDLER`. `ACTION_DESC_LITERAL_PLUGMOD` sets only the owner-type bits, so idax's previous heap `ActionAdapter` had no deterministic successful-registration destruction path. Its `activate`/`update` virtual methods are host ABI callbacks and must catch all C++ exceptions. IDAPython's `add_hotkey`/`del_hotkey` are conveniences over this action mechanism, not native C++ SDK calls.

393. **Headless action registration does not establish UI activation or immediate owned-handler reclamation:** On the local IDA 9.3 idalib host, `register_action` and `unregister_action` return success, but `process_ui_action` returns false without entering the callback, and a callback capture retained by an `ADF_OWN_HANDLER` adapter remains live immediately after successful unregister. Deterministic wrapper reclamation must therefore keep adapters in wrapper-owned storage and erase them after successful SDK unregister rather than rely on host-timed destruction. Activation/exception runtime evidence requires an interactive IDA UI host; headless tests can prove registration, move/release state, and callback-state reclamation.

394. **The current public SDK processor-ID set ends at `PLFM_NDS32 = 76`; `PLFM_MCORE = 77` is not present:** An exhaustive search across the installed SDK's `v9.2`, `v9.3`, 9.4 placeholder, local branches, and remote-tracking refs found no `PLFM_MCORE`. The authoritative `idp.hpp` range is `0..76` and states that values above `0x8000` are reserved for third-party modules. The existing idax `Mcore = 77` entry and “full current SDK” claim are therefore unsupported provenance from an earlier user-supplied set. Processor normalization must preserve the raw signed ID, return a typed ID only for verified public constants, and remain usable for future/third-party modules instead of casting arbitrary integers into an enum.

395. **The Node and Rust decoded-operand transfers drop processor-reported read/write access:** Authoritative `idp.hpp` defines `CF_USE1..8` as operand-value use and `CF_CHG1..8` as operand modification. C++ `ida::instruction::Operand` preserves those canonical-feature results in `is_read()`/`is_written()`, but `IdaxOperand`, safe Rust, and both Node instruction-object conversion paths transferred neither field. Consumers therefore could not distinguish a memory destination from a memory source. Data-reference direction is not an equivalent substitute. Preserve both booleans through every binding snapshot so feature extractors can identify written memory operands without raw SDK access.

396. **idax can read a known string but cannot enumerate IDA's string list, and it omits source-file address ranges:** The audited IDAMagicStrings source (`IDAMagicStrings.py` SHA-256 `3d5c496899d58c99dca62debdb03383d0df84fdebba08759a63c9a486ef90f99`) depends on `idautils.Strings` plus `get_sourcefile`. Authoritative `strlist.hpp` exposes cached `string_info_t` entries with address, octet length, and string type, while `lines.hpp` maps a source filename to a half-open address range. Existing `ida::data::read_string` requires a known address and existing `ida::lines` exposes neither mapping family. Add owned snapshots and range values; do not expose `string_info_t`, `strwinsetup_t`, `range_t`, or borrowed filename pointers.

397. **IDA's string list is an explicitly rebuilt process-global cache with mutable shared options:** `strlist.hpp` states that the kernel does not keep the list current and consumers must call `build_strlist`; `get_strlist_options` returns the shared configuration used by the Strings window. IDAPython's primary `Strings.setup` adapter mutates that shared object and then rebuilds. Wrapper configuration must validate byte-sized string-type codes and signed minimum length, make the global side effect explicit, and return copied options/items so no SDK storage escapes. Tests and standalone analyses that temporarily change options must snapshot and restore them.

398. **The IDA 9.3 string-list rebuild prepends an internal zero entry to `strwinsetup_t::strtypes`:** Exact runtime configuration with requested types `{0,1}` reads back as raw bytes `{0,0,1}`, while `{1}` reads back as `{0,1}` and discovers no one-byte strings. The initial one-byte-only host state is `{0,0}`. The leading zero is therefore host bookkeeping, not a duplicate requested C-string type. Strip exactly one leading zero when copying public options, but pass caller type codes unchanged into the shared configuration before rebuild. This keeps public configure/query round trips stable without changing which strings IDA discovers.

399. **The safe Rust name binding omitted the filtered full-name inventory required by class-hierarchy analysis:** C++ and Node already expose `name::all(ListOptions)`, including user-defined and auto-generated names, but safe Rust exposed only `all_user_defined`. IDAMagicStrings derives scoped class evidence from the complete name inventory and demangled fallbacks, so the narrower Rust function is not semantically equivalent. Transfer `ListOptions` through the C ABI and preserve each entry's origin booleans; do not approximate the missing auto-generated names from functions or string references.

400. **The native integration harness passed the repository fixture directly to every idalib target:** A complete passing CTest run changed `tests/fixtures/simple_appcall_linux64.i64`; isolation identified `smoke` as the first mutator. Semantic cleanup inside a test does not guarantee byte-identical IDB serialization because analysis queues, cache state, and metadata writes can persist. Execute every integration target against a uniquely named temporary copy of both the raw input and adjacent `.i64`, then remove that copy regardless of test success.

401. **IDAMagicStrings' non-NLTK semantics include name-list candidate evidence and observation-level language accounting:** `find_function_names` scans the concatenation of named-function/demangled scoped strings and string-list entries, so name inventory affects both class objects and candidate rarity. Source language counts increment once per referenced source-string observation and once per debug-metadata address, not once per xref row; insertion order also classifies `.c`, `.cc`, `.cxx`, `.cpp`, `.h`, and `.hpp` as `C/C++` before narrower categories are considered. Ports must preserve these details to make percentages and candidates equivalent.

402. **Auto Enum requires two mutation primitives absent from the wrapper despite existing read coverage:** The audited plugin reads imported function prototypes, replaces selected integral arguments with generated enum types, and applies enum rendering to a call-site operand. idax can inspect function details and control numeric/offset/structure operand formats, but it cannot produce a revised prototype while retaining existing argument metadata or set/read a named enum operand representation. The existing ctree visitor already supplies call arguments, numeric constants, object/local-variable identity, and expression addresses, so no raw ctree escape hatch is required.

403. **Function-argument edits must preserve the complete SDK prototype record, not reconstruct from the public summary:** `func_type_data_t` contains argument names, comments, locations, flags, return location, spoiled registers, calling convention, and function flags beyond the current public `FunctionDetails` snapshot. Rebuilding from only return/argument types and a normalized calling convention silently discards ABI metadata. The safe wrapper operation must copy the native function record internally, replace only the indexed argument type, rebuild the function/function-pointer value, and return an opaque copy. SDK `op_enum` accepts either one operand or `OPND_ALL` and requires a local-type TID; public name-based resolution plus copied name/serial readback keeps both values opaque.

404. **The native and IDAPython all-operands sentinels are numerically different:** IDA 9.3 `bytes.hpp` defines `OPND_ALL` as `OPND_MASK` (`0x0F`), outside the ordinary `0..UA_MAXOP-1` range, while Auto Enum passes `-1` through IDAPython's `op_enum` convenience. Validating a public `-1` directly against `OPND_ALL` rejected both `-1` and normal operand index `1`. Preserve the binding-level `-1` convention and translate it to `OPND_ALL` only at native dispatch/readback.

405. **Auto Enum's global and local workflows have different host requirements:** Imported-prototype enrichment is deterministic under idalib and maps directly to a headless report/apply tool. Selector-dependent annotation starts from the currently selected decompiler call and requires callback-scoped `ExpressionView` child navigation. Preserve the latter in the interactive C++ plugin; the Rust headless adaptation covers the global workflow rather than approximating cursor state from flat expression snapshots. Isolated IDA 9.4 evidence changed 8 argument types across 6 imports and observed all 8 after reopen.

406. **idax exposes formatted final microcode and callback-scoped instruction snapshots, but no owned function-level microcode graph:** Symless generates `MMAT_PREOPTIMIZED` microcode, calls `build_graph()`, traverses block predecessor/successor relations, injects values at function argument locations, and propagates register/stack state across addressed instructions. `DecompiledFunction::microcode()` only prints the final `cfunc_t::mba`; existing `MicrocodeInstruction` snapshots are reachable only while a lifting filter callback owns the native context and omit instruction address, graph identity, function arguments, address-of operands, and call-argument lists. A traversal consumer therefore cannot reconstruct the Symless data flow through the public API.

407. **Owned microcode snapshots must be copied before destroying one `mba_t`, and pre-local-optimization graphs require one explicit build:** IDA 9.3 `hexrays.hpp` states that `mop_t`, `minsn_t`, and `mblock_t` are specific to their parent `mba_t`; operand-owned pointers are freed with the operand, and `mba_t` is noncopyable. `gen_microcode()` yields a separately owned `mba_t` at the requested `mba_maturity_t`; before `MMAT_LOCOPT`, adjacency is unavailable until the one-per-MBA `build_graph()` call. A wrapper must validate maturity, generate/build/copy all blocks, operands, call arguments, text, addresses, and function argument locations into SDK-independent values, then destroy the native graph. Unsupported-but-valid opcodes/operand kinds must remain traversable as copied `Other` values rather than aborting the entire snapshot.

408. **Full Symless parity spans several independent surfaces beyond microcode graph traversal:** The audited MIT-licensed upstream additionally depends on interprocedural call/return propagation, allocator-wrapper recognition, constructor/vtable discovery, shifted-pointer metadata (`TAPTR_SHIFTED` plus parent/delta), metadata-preserving return/argument-location mutation, local-type forward-reference replacement and C++/vftable flags, multi-element struct-offset paths, member-TID informational xrefs, and a microcode-widget operand picker. An intraprocedural function-argument port can rigorously cover register/stack state propagation, pointer shifts, load/store field recovery, minimum-width conflict resolution, UDT creation, and explicit prototype application, but it cannot be labeled complete Symless parity.

409. **Recursive owned microcode transfer requires one allocation/free invariant at every pointer/count boundary:** The function graph contains recursively nested instructions, address-reference operands, call-argument arrays, scattered ABI-location parts, block edges, and instruction arrays. The Rust C shim must allocate each owned node/array, publish a nonzero count only with storage, recursively unwind partial construction on failure, and free all copied strings and descendants exactly once. Safe Rust must reject null/nonzero and slice-size-overflow pairs before constructing a slice, copy the complete graph, and free the C root even if conversion fails. IDA 9.4 runtime evidence across two consecutive generations retains the first graph after both native MBAs are gone and validates all copied edge targets.

410. **A faithful bounded Symless transfer must preserve address-state stores and stable predecessor ties:** In Symless, `stx` records a write through its destination pointer but does not assign or invalidate that pointer variable; generic destination assignment after a store incorrectly loses subsequent accesses. Its predecessor selection sorts by tracked structure/call values with stable first-candidate ties, so an implementation using a last-maximum iterator changes branch preference. The adapted engine special-cases store state preservation and selects the first strictly higher-information predecessor. IDA 9.4 evidence on a host-native fixture recovers four exact offset/width/access-direction fields; report mode leaves the named UDT absent, first apply creates four members and changes one eligible argument, and fresh-process reopen recognizes the argument as already typed.

411. **Interprocedural Symless apply requires immutable return-type replacement, not reconstruction from public summaries:** Upstream propagates tracked call arguments into resolved callees, reads each callee's ABI return location at terminal flow, and later types both propagated arguments and structure-pointer returns. The owned idax graph already copies direct call targets, call arguments, function argument locations, return location, and CFG edges, so depth-bounded direct-call traversal requires no raw MBA exposure. However, idax can only replace an argument type. IDA 9.3 `func_type_data_t` retains return location, flags, spoiled registers, calling convention, argument metadata, and other ABI state; `tinfo_t::create_func` consumes that complete record. Return replacement must therefore copy the native record, change only `rettype`, rebuild either the direct function or function-pointer value, and preserve the input. Shifted propagated sites cannot be safely typed until opaque shifted-pointer metadata exists.

412. **Preoptimized microcode does not resolve unknown direct-call arguments until explicit call analysis:** On the IDA Professional 9.4 arm64 host, a graph generated at `MMAT_PREOPTIMIZED` retained an unknown call with an exact address operand but no `mop_f` call-argument payload, so the first interprocedural run followed zero calls and missed the callee field. The audited Symless path first builds the graph, pre-decompiles direct callees to improve argument counts, and invokes `mba_t::analyze_calls(ACFL_GUESS)`. An opaque graph-generation option must reproduce that sequence before copying the MBA. Keep it disabled by default because it advances call-information state beyond a raw requested-maturity snapshot; enable it explicitly for interprocedural consumers.

413. **Terminal return consensus must distinguish ordinary scalar returns from contradictory structure returns:** A callee that returns only scalar/unknown values has no structure value to propagate and is not a conflict. A callee that yields the same structure pointer at every terminal is usable. Differing structure shifts, or a mixture of structure and non-structure terminal values, are ambiguous and must be rejected as one conflict. Counting every non-structure return as a conflict incorrectly marks ordinary field-reading helpers; accepting a structure value from only some terminals over-propagates through path-dependent returns.

414. **Allocator discovery is graph-expressible, but source-equivalent allocator typing requires metadata-preserving argument renaming:** Symless resolves declarative module/import-prefix or function seeds, walks code/data references, classifies constant malloc/calloc sizes, recognizes forwarded caller arguments, confirms a wrapper only when its terminal return carries the originating call token, and recursively visits wrapper heirs. Copied import modules, xrefs, function containment, analyzed owned microcode arguments, ABI locations, and return consensus cover that analysis without new SDK exposure. Its apply path also names size parameters. idax exposes copied function argument names but can only replace their types; rebuilding from `FunctionDetails` would discard argument locations, comments, flags, return location, spoiled registers, and function flags. Add immutable indexed argument-name replacement by copying the complete `func_type_data_t` and changing only `funcarg_t::name`. Do not synthesize missing arguments without concrete ABI evidence.

415. **Allocator wrappers and allocation objects require different abstract roots and mutation targets:** A forwarding wrapper is valid only when every terminal ABI return location contains the token produced by the exact configured allocator call; merely calling an allocator with a caller argument is insufficient. A static allocation object instead begins at that call result, so structure transfer must inject offset zero at the exact call address and then reuse the existing interprocedural engine. The constant allocation extent is an upper bound on evidence-backed fields: discard any recovered field whose `offset + width` exceeds it. Apply a distinct UDT per static call root, but never assign that UDT to the reusable allocator/wrapper return; enrich only the generic `void*`/`size_t` prototype.

416. **Constructor/vtable discovery is expressible through owned idax values, but source-equivalent materialization needs UDT semantic mutation:** Symless accepts a pointer-width table while members are exact function entries or mapped external imports, stops at a referenced non-first slot, excludes all-import tables, and accepts a class root only after microcode proves a pointer-width store of that exact table into argument zero at offset zero. idax already exposes loaded item traversal, pointer reads, exact function containment, external segment identity, references, owned argument locations/microcode constants, named UDTs, function pointers, and prototype editing. It can therefore reproduce the evidence graph without SDK escape hatches. However, upstream rebuilds UDT details with `TAUDT_CPPOBJ` for classes and `TAUDT_VFTABLE` for tables; idax only reads these flags. Add a metadata-preserving semantic setter that changes exactly those two bits in a copied `udt_type_data_t`, retaining layout, members, packing, alignment, and unrelated flags. Symless's `get_most_derived` ranking by member count, method-xref count, and load count is explicitly described upstream as inaccurate; multiple offset-zero candidates must remain ambiguous rather than drive mutation.

417. **Constructor graph evidence and prototype mutation have different arity requirements:** An owned preoptimized graph can infer argument zero and prove an exact table store even when the database has no applied function prototype. That is sufficient for non-mutating constructor classification, but it is not sufficient to synthesize a public ABI declaration: `TypeInfo::with_function_argument_type` intentionally edits an existing `func_type_data_t` argument and preserves its complete metadata. Therefore semantic apply must classify functions with absent argument records as ineligible, while generic pointer or pointer-width scalar first arguments may be replaced. IDA 9.4 fixture evidence with explicit generic `void*` DWARF prototypes changes the constructor and all three methods on first apply and recognizes all four on reopen; the stripped-prototype falsification leaves the three method functions ineligible without weakening discovery.

418. **Bindgen emits two valid recursive-instruction shapes depending on libclang parsing:** The same C shim can yield an opaque one-byte `IdaxMicrocodeInstruction` placeholder or the complete recursive record with generated layout assertions and a zeroing `Default` implementation. The existing postprocessor normalized only the opaque shape, so a newer local libclang produced a generated file different from the checked binding despite an unchanged C ABI. Locate the complete `repr(C)` declaration by name and replace the entire declaration-to-first-FFI span for both shapes. This converges generation byte-for-byte while retaining the manually required recursive public fields.

419. **Same-offset UDT occupancy is not evidence of reusable semantic layout:** A preexisting generated-name class or vftable can have a member at the expected byte offset with an incompatible type. Counting that slot as reused would apply a misleading table type and could type constructor/method prototypes against a conflicting class layout. Before any class/prototype mutation, require every existing vftable member to be pointer-width aligned, within the discovered method count, and exactly type-compatible with the corresponding method pointer. For recovered class fields, count exact-offset members as reused only when their types match; otherwise preserve them and report a skip. Existing offset-zero class members must exactly match the named vftable pointer or reject that class root.

420. **IDA pointer equality does not compare shifted-parent metadata:** Local IDA 9.3 `ptr_type_data_t::operator==` compares only `obj_type`, `closure`, and `based_ptr_size`; it omits `parent`, `delta`, and `taptr_bits`. Therefore `tinfo_t` equality or rendered pointee equality cannot establish that an existing `__shifted` argument has the required parent and displacement. Upstream explicitly copies pointer details, sets `TAPTR_SHIFTED`, signed 32-bit `delta`, and `parent`, applies shifted types only to arguments, and rejects shifted returns. An opaque wrapper must copy parent/delta out explicitly and rebuild from the complete existing pointer record, changing only the shifted bit/parent/delta. Zero delta is an unshifted pointer and non-struct parents are invalid for this bounded API.

421. **Exact shifted-parent metadata survives function-prototype application and database reopen:** On IDA Professional 9.4, applying a function argument whose pointer record retains the named structure pointee while setting `TAPTR_SHIFTED`, parent to that same named structure, and delta `+8 B` persists all three properties. A fresh process classifies the root argument as ordinary `shift = 0 B` and the callee argument as exact `shift = +8 B`; explicit `pointer_details` comparison reports both already typed. The fixture recovers `+4/4 B`, `+8/8 B`, and `+24/1 B` fields and changes two arguments on first apply, then changes zero on reopen. This validates metadata inspection rather than native pointer equality as the idempotency condition.

422. **Forward local types require ordinal-preserving definition copy, not ordinary name reuse:** Symless detects an exact local structure forward, captures its ordinal, creates an empty complete UDT, and saves it back with `set_numbered_type(..., NTF_REPLACE)` before adding members. Local IDA 9.3 defines forwards as explicit ordinal-backed placeholders (`is_forward_decl`, `is_forward_struct`, `is_forward_union`) and documents `NTF_COPY` as saving a new definition rather than a typeref. Therefore a general opaque replacement must require an exact local forward, a complete same-kind struct/union candidate, and save a copied candidate into the existing ordinal with `NTF_REPLACE | NTF_COPY`; ordinary `save_as(name)` is too broad because it also overwrites complete definitions and a named candidate may otherwise remain a reference to another type. Validate name, locality, forward state, ordinal, candidate completeness, and kind before mutation.

423. **`get_pointed_object()` can discard a valid forward pointee while pointer details retain it:** IDA Professional 9.4 imported the DWARF argument `struct symless_forward_target *` as a valid pointer whose `ptr_type_data_t::obj_type` is the named structure forward. The original `TypeInfo::pointee_type()` called `tinfo_t::get_pointed_object()`, received an absent value, and aborted Symless apply with `Failed to get pointer target type`. `tinfo_t::get_ptr_details()` succeeds on the same value and preserves the forward placeholder. Pointer pointee extraction must therefore use the copied complete pointer record, matching `pointer_details`, so forward classification is observable before replacement and the same ordinal reference resolves after replacement.

424. **Ordinal-preserving forward replacement resolves existing prototype links without retyping them:** IDA Professional 9.4 imported the host fixture's DWARF `struct symless_forward_target *` argument plus a same-name local structure forward. Report mode recovered exact `+4/4 B`, `+8/8 B`, and `+24/1 B` reads. First apply copied the complete three-member UDT into the forward ordinal (`structure_created=false`, `structure_forward_replaced=true`) and immediately classified the original function argument as already typed, proving its existing ordinal link resolved to the new definition. Fresh-process reopen performed zero replacement/mutation, reused all three members, and again recognized the argument. Complete same-name targets and kind mismatches remain rejected before mutation.

425. **Persistent member cross-references require internal UDM-TID resolution, not a generic address escape:** Symless obtains the exact UDT member TID for each recovered offset and adds `dr_I | XREF_USER` from every access instruction, including additional same-instruction fields and a fallback when operand stroff application omits the member reference. idax already supports informational data xrefs, but its opaque `Member` snapshot intentionally omits `tid_t`; callers therefore cannot name the destination without treating an SDK identity as a generic address. Local IDA 9.3 documents `get_udm_tid(index)` specifically for member xrefs, requires xref sources to be item heads, and states that only `XREF_USER` survives reanalysis. Resolve a unique exact byte-offset member only inside `TypeInfo`, require a complete saved local UDT with stable member identity, ensure a user informational reference idempotently, and enumerate only source addresses. Reject unsaved/sub-TIL/forward types, missing or ambiguous offsets, non-head sources, and incompatible existing from/to references without exposing the member TID.

426. **User informational references to exact UDT member identities persist and enumerate after fresh-process reopen:** On IDA Professional 9.4, the Symless DWARF fixture recovered item-head sites `0x100000424`, `0x100000430`, and `0x10000043c` for exact members at `+4 B`, `+8 B`, and `+24 B`. Report mode counted three candidates and created no IDB. First apply replaced the existing structure forward, added three members, and verified three new `dr_I | XREF_USER` references. A separate process opening the saved `.i64` enumerated those same three references: zero additions, three reuses, zero skips. This falsifies neither TID stability nor xref persistence and establishes source-address readback as a sufficient opaque idempotence contract.

427. **The repository Node test harness cannot deep-compare arrays containing `BigInt`:** Its `toEqual` implementation delegates directly to `JSON.stringify`, which throws on JavaScript `BigInt` before equality can be evaluated. `Address[]` integration assertions must compare array length and individual strict-equality elements, or the harness must gain an explicit BigInt serializer. The initial Phase 43 Node live run reached successful member-reference creation/readback but failed only in this serializer; the element-wise assertion then passed 81/81.

428. **Exact Symless operand typing requires register evidence plus a two-component opaque stroff path, while the existing raw-ID surface violates the locked opacity contract:** Upstream converts each register-backed access location with `ida_hexrays.mreg2reg(mreg, width)`, groups offsets by `(instruction address, processor register)`, selects the first matching phrase/displacement operand or register-preceded immediate, computes the signed-width difference `field_offset - encoded_displacement`, and calls `op_stroff` with `[structure TID, exact member TID]`; only the first observed offset receives a stroff path and later offsets receive member xrefs. Local IDA 9.3 defines `strpath_t::ids[0]` as the root structure and `ids[1..]` as selected member identities, supplies `get_udm_by_tid` for opaque member-name resolution, and supplies `mreg2reg` for the exact register-domain conversion. IDAX currently applies only a one-element root path while `StructOffsetPath::structure_ids`, the numeric setter overload, Node numeric inputs, Rust `*_by_id`, and the C array expose native database identities. Replace these surfaces with copied root/member names, resolve an exact unique saved-local member internally by byte offset, retain the processor register as owned microcode evidence, and reject incompatible existing paths before mutation.

429. **Symless register evidence originates from size-zero pointer-arithmetic observations, not necessarily the final memory-access operand:** A real IDA Professional 9.4 run on the forward-structure fixture recovered three fields and three member-reference sites but produced zero operand-stroff candidates when evidence was captured only from `ldx`/`stx` pointer operands. Those pointer operands are nested microcode expressions rather than registers. Audited upstream `cpustate.py` records an additional size-zero access for pointer `add`/`sub` using the source register location and shifted target offset; `model.handle_access` excludes that observation from field creation while retaining it for machine-operand selection. The port must mirror this split: record register-backed add/sub observations, attach them by exact resolved field offset after width conflict resolution, and never create a zero-width UDT member. This is falsified if the same real fixture still yields zero candidates, creates an extra field, or selects an operand without matching processor-register evidence.

430. **Opaque exact operand paths persist and remain idempotent after database reopen:** On IDA Professional 9.4 arm64, the corrected pointer-arithmetic evidence path recovered exactly three nonzero fields and three operand candidates at `0x100000410`, `0x100000414`, and `0x100000418`. First apply created the named UDT, its three members, three persistent member references, and three two-component operand struct-offset paths. A distinct process reopening the saved `.i64` added zero paths and classified all three as exact reuses; it likewise reused all three members and references. The unchanged fixture executable SHA-256 is `d1f921b0db5d392a0a7be117aee088187802f0b117a42e08e1a08af73ebc1c33`; the reopened IDB SHA-256 is `5edc3e4964b92a23984db58c9c6c132edbee684a5c1769d7a309c2f429f8dc4c`. Exact readback therefore resolves stable root/member names without publishing native identities.

431. **Absence of a struct-offset path does not imply absence of an operand representation:** The first exact-member ensure preflight called `get_stroff_path()` and treated its empty result as permission to apply. Local IDA 9.3 `bytes.hpp` independently records arbitrary-operand representation state through `is_defarg(flags, n)` and struct-offset state through `is_stroff(flags, n)`. An enum, ordinary offset, character/number format, stack variable, forced operand, floating value, or custom format can therefore have no stroff path yet still be incompatible state that `op_stroff` would replace. Exact ensure must return `Conflict` for any defined non-stroff representation, and must report an SDK inconsistency when the stroff flag exists but path readback fails. This is falsified by preserving a pre-existing enum/offset representation unchanged and rejecting it before mutation.

432. **Opaque path readback requires nonempty names, and exact apply can be made failure-atomic because preflight requires a pristine operand:** `get_udm_by_tid()` can resolve a member record whose public name is empty; returning that empty string would satisfy native identity lookup but not the wrapper's copied-name contract. Treat empty root or member names as unavailable. Separately, exact ensure has already rejected every existing representation before `op_stroff`; therefore clearing the operand type after either SDK apply failure or verification mismatch cannot erase user state and guarantees rollback of partial native mutation. Address/index validation must occur before all path/flag queries.

433. **Phase 44 complete validation confirms cross-language opacity, preservation, and live persistence:** The C++ build and all 26 CTest targets pass, including preservation of a defined non-stroff format, exact two-component negative-delta readback, incompatible-path rejection, and repeated ensure. Node native build, authoritative strict example declarations, 238 structural checks, and 82 ABI-matched live checks pass. Rust formatting/all-target checks, 139 library tests, 14 Symless tests, and 99 IDA Professional 9.4 live checks pass; generated bindings are byte-identical at SHA-256 `3a143a13309725ed66c5ebce1dd5199fafcc30ea8a0d92b33404c9fef66d7a13`. A final fresh arm64 fixture (SHA-256 `1c1f7d72a5d13a6f74101e09c0f46048d1321190c3272a728dbc0bce94bb0843`) reports three candidates, first apply adds three exact paths, and fresh-process apply reuses all three with zero additions; the IDB SHA-256 is `5eacc6b9554734a9d58fec4475697c09207eeb42330edda16f13047ffa2e4f94`.

434. **Upstream Symless resolves a bounded database-backed subset of indirect calls and distinguishes target provenance from integer equality:** For `m_icall {l=selector, r=offset} d=args`, upstream ignores the selector, copies arguments from `mop_f`, and resolves only the right operand when its abstract value is `mem_t`; it then requires `get_func(value)` to start exactly at that value. `mem_t` is an `int_t` subtype created by reading a global `mop_v`, taking the address of global `mop_a`, or loading bytes through an already database-derived address; it remains distinct from a plain immediate integer through move, extension, and pointer arithmetic. Current IDAX owned graphs copy indirect left/right/destination operands and call arguments, while public address/data/function APIs can read loaded bytes and validate exact entries. The adaptations nevertheless reject every `IndirectCall` immediately and their `Integer` value loses provenance. Add an internal memory-derived value kind and mirror the upstream data-flow boundary; no new public wrapper API is required. Object-dependent virtual dispatch whose target is a structure dereference remains unresolved rather than guessed.

435. **Fixed indirect allocator calls require exact data-reference reachability in addition to call-xref classification:** An encoded global function-pointer slot produces a data reference from the slot to `_malloc` and a code reference from the wrapper to the slot; it does not produce the direct code-call reference assumed by the Phase 39 seed walker. Resolve only this one exact evidence hop: inspect code users of a target-referencing data slot, generate each containing owned graph once, enumerate its copied indirect-call addresses, and retain a site only when database-derived evaluation equals the configured allocator exactly. The IDA Professional 9.4 arm64 fixture then reports one indirect allocator wrapper, one 32 B root, and zero unclassified calls. Scanning unrelated functions or accepting a call-info target without provenance would broaden the model beyond the evidence.

436. **Database-resolved indirect ordinary and allocator paths persist through apply/reopen:** The Phase 45 arm64 fixture loads relocation-derived target-minus-`0x135` values from two volatile slots and restores each target with an integer add. Ordinary analysis follows exactly one database-resolved indirect call with zero unresolved calls and recovers `+4/4 B`, `+8/8 B`, and `+24/1 B`; first apply adds three members/references/operand paths and types both arguments, while fresh-process reopen reuses all three metadata sets and both arguments with zero additions. Allocator analysis classifies one database-resolved `_malloc` wrapper and one 32 B root; first apply adds three members, six references, and six operand paths, and reopen reuses all of them. Source and executable SHA-256 values are `ce0d76fde61fbdcd07c889fc5f3f730f5b4e915bef334c60cf0e9271a7f12be1` and `a29cda2ef8470f1f254e1bf7b943b908f644632e5b852c1176859ca93359368d`; the final reopened IDB SHA-256 is `0ef1c5bc5853daaef8f919205c80001263fbb8484819109db67bbffbb14caddc`.

437. **Phase 45 complete validation preserves every unchanged public boundary:** The change adds no wrapper, Node, Rust-library, or generated-C-ABI surface. Full C++ build/CTest passes 26/26 in 24.54 s; Node native build, authoritative strict example declarations, 238 structural checks, and 82 live checks pass; Rust formatting/all-target checks, 139 library tests, 0 sys tests, 15 Symless tests, and 99 live IDA 9.4 checks pass. Generated bindings remain byte-identical at SHA-256 `3a143a13309725ed66c5ebce1dd5199fafcc30ea8a0d92b33404c9fef66d7a13`; the tracked fixture executable/IDB hashes and Git blob remain unchanged.

438. **Symless vtable propagation is static root seeding, not runtime virtual-call resolution:** Upstream `utils/vtables.py` scans function-pointer arrays, requires at least one non-import member and a confirmed code load, first searches references to the function-array address, and only if none are confirmed searches the Itanium-style RTTI label at `table - 2 * pointer_width`; exact pointer-valued data references are traversed recursively. `model/model.py` then adds every accepted non-import table member as an argument-zero propagation root. The current adaptations scan equivalent tables and validate exact argument-zero constructor stores but inspect only direct table references and reconstruct fields only from constructors. Existing owned xref, pointer-read, function-containment, and depth-bounded microcode surfaces are sufficient; do not describe this as resolving a runtime object-dependent `m_icall`.

439. **RTTI alias reachability and virtual-method field roots survive materialization and fresh reopen:** The Phase 46 arm64 fixture places three exact method pointers at `0x100004010` after a two-pointer prefix at `0x100004000`; a pointer-valued data alias at `0x100008000` is the constructor's only static route to the prefix. Report finds one candidate/class, zero direct-load tables, one RTTI fallback/load, one followed alias, three analyzed methods, and exact fields `+8/4 B`, `+16/8 B`, `+24/1 B`, and `+32/8 B`; the last two are not written by the constructor. First apply creates one class and vftable, adds five class members including `__vftable`, three method members, eleven references, eleven operand paths, and changes four prototypes. A distinct process reopening the saved `.i64` adds or changes zero and reuses all counts. Source/binary SHA-256 values are `ab2594cd975742e0d521dfec0d494952fba7408453b6e57ea78ffab9c0984e56` and `3408da9c64e5bfe34b18bd980c46bacd680b7f70747a4425eea244ebb88a7b75`; the post-reopen IDB SHA-256 is `41e72624faf91985ebc93fd583b422bd92b7e9983edc20ad9a705180a087e116`.

440. **Phase 46 preserves unchanged wrapper/binding surfaces and direct-table behavior:** Full C++ build/CTest passes 26/26 in 22.42 s; Node native build, strict example declarations, 238 structural checks, and 82 live checks pass; Rust formatting/all-target checks, 139 library tests, 0 sys tests, 17 Symless tests, and 99 live IDA 9.4 checks pass. Fresh bindgen output is byte-identical to the checked file at SHA-256 `3a143a13309725ed66c5ebce1dd5199fafcc30ea8a0d92b33404c9fef66d7a13`. The original direct-table fixture remains one accepted class with `direct=1`, `RTTI fallback/load=0/0`, three methods, and the same three fields. Tracked simple fixture executable/IDB SHA-256 values and IDB blob remain `af23d4fde7d2b5ebe20385f5aa8c23221988fd1bdbab777c18daf8c9d9543f80`, `ce6d678f484d681a5bc147dab49c272e3a7f9883b3c15c41974ec52cb95a431b`, and `84ff142e9cd6c39dbd22d94c7d164b2db48c64dd`.

441. **Exact Symless operand-root selection requires destination-modification metadata absent from the owned graph:** Upstream `builder.py` recursively visits each instruction's left, right, then destination operands, records only `mop_r` registers and resolvable `mop_S` stack variables, and marks the destination as writable only through `minsn_t::modifies_d()`. Nested instructions execute depth-first before their parent; `cpustate.flatten_minsn()` assigns their injection indices in that order and injects source roots before processing or destination roots after processing the exact `(EA, sub-index)`. IDAX already owns recursive operands, register/stack identities, graph addresses, modal choosers, and interprocedural state, but `MicrocodeInstruction` copies only opcode/operands/floating-point/address/text. Treating every destination as modified would misclassify the address operand of stores. Copy `modifies_d()` as an opaque boolean across C++, Node, and Rust, enumerate roots from the owned graph in the same execution order, and scope injection to the selected root function. The upstream assembly operand hint is optional selection convenience, not an analysis prerequisite.

442. **The upstream picker can assign a parent operand the wrong sub-instruction index when a later operand contains nested microcode:** `builder.parse_minsn()` collects left, right, and destination candidates during syntactic parsing and increments its counter only after returning from each encountered `mop_d`. By contrast, `cpustate.flatten_minsn()` emits every nested left/right/destination instruction before the patched parent. A parent-left register collected at index zero before a nested-right instruction is therefore displayed as `(EA, 0)`, although injection index zero belongs to that nested instruction and the parent executes later. The upstream code also resets its display counter for each top-level instruction while the state engine accumulates indices across consecutive instructions sharing one EA. A bounded port must derive candidates and injection identities from one shared depth-first execution traversal and retain a private block/instruction/nested path to disambiguate duplicate `(EA, sub-index)` values; reproducing the renderer bug would violate exact root selection.

443. **Owned microcode-root selection is readable, exact, and persistent on a real IDA database:** Owned operand/instruction `text` retains IDA color tags, so the first headless candidate list contained control bytes even though identities were correct; applying `lines::tag_remove` only to copied picker display strings produced 18 readable candidates without altering the graph. On IDA Professional 9.4, candidate zero for `inspect_fields` is register `x0.8{2}` at `0x100000460.0`, classified source/before. Exact private-path injection occurs once and recovers `+4/4 B` write plus `+8/8 B`, `+18/2 B`, and `+24/1 B` reads. Report mode is non-mutating. First apply creates one four-member structure and adds four persistent member references plus four exact operand paths without typing an unrelated function argument; a distinct reopen process creates/adds zero and reuses all four members, references, and paths. Fixture source, arm64 executable, and reopened IDB SHA-256 values are `e9eac89ea492d197048d0f7e34c357824626fc5433adb11b3d44297831ea73c3`, `465efd22b8d7ab90130428b2b70ea794c694bb0d3b0a23f131939cb1673785c1`, and `724fdf034ae9a5aa93f56280894d1e08e97bcf34bd88970cf41a53ae434fa61c`.

444. **Runtime object-dependent indirect dispatch is not an unported Symless behavior:** The only upstream `m_icall` handler ignores the selector register, reads the offset register, and accepts it only when its abstract value is `mem_t` and resolves to an exact function entry. Phase 45 implements that complete bounded rule. Reads through structure pointers instead produce structure/read entry values, not `mem_t`, and the vtable path never feeds a runtime indirect-call target; it statically seeds each recognized non-import table member at argument zero, which Phase 46 implements. A repository-wide Python source search finds no second indirect-target resolver. Therefore object-dependent dispatch is unknown/dynamic behavior outside the upstream implementation, not a remaining parity gap. Implementing it would require a new analysis specification for object/vtable state and branch-set semantics rather than source-port closure.

445. **Phase 47 complete validation confirms exact destination semantics, cross-binding ABI synchronization, and selected-root persistence:** The full C++ build and all 26 CTest targets pass in 22.58 s, including live store-source and move-destination metadata checks; the Symless plugin links. Node native build, strict declarations, 238 structural checks, and 82 IDA Professional 9.4 checks pass. Rust formatting/all-target checks, 139 library tests, 0 sys tests, 20 Symless tests, and 99 process-main-thread IDA 9.4 checks pass. Two independent clean-build bindgen outputs are byte-identical to the checked file at SHA-256 `865f53507d8dd44ef7b2033eccb901f3bf26bf21e0653c8528c493e3692c7b3f`. The live root-selection fixture lists 18 readable candidates, injects candidate zero exactly once, adds four members/references/paths on first apply, and reuses all four with zero additions after a distinct-process reopen. The tracked executable/IDB hashes and IDB Git blob remain unchanged.

446. **Diaphora 3.4.0's independently testable foundation is a per-function feature record, while its complete workflow spans several separable domains:** Fixed release commit `84aa7dd83fd45d13ae4e5cbe10b12effb97b9b99` exports one SQLite row plus instruction/basic-block/callgraph relations per function. High-confidence matching consumes exact address/RVA, normalized/full byte hashes, instruction/mnemonic counts, CFG metrics, names, assembly, pseudocode, microcode, constants, types, comments, and flags; later phases import function and instruction metadata, definitions, and UI-selected matches. The first bounded adaptation can preserve exact full and relocation-light byte fingerprints, canonical CFG/mnemonic metadata, deterministic unique matching, and conservative function name/prototype/repeatable-comment import without claiming SQLite, heuristic, pseudocode, microcode, type-library, or chooser parity. Audited SHA-256 values for `diaphora.py`, `diaphora_ida.py`, `diaphora_plugin.py`, and `db_support/schema.py` are `a4a5603a537de4816b01b4662f4ae947bc309438000da248290a626b028f134c`, `47dab5eaa4729721024202a36e76f289d24c3801242136f5053d469db0a99e39`, `022efadeab7020015c811e5014521a3b64f7bd265af556e80e8d3812891bc56e`, and `abcf8af39284b063bd858b69b7f6ff9614e7fa1505e7c90d4c35a84701e359c`.

447. **Diaphora's relocation-light instruction fingerprint requires copied encoded-value byte positions absent from IDAX operands:** `CIDABinDiff.get_decoded_instruction()` begins with the decoded instruction size and subtracts each address/immediate/displacement operand's `op_t::offb` before hashing the resulting byte prefix. IDAX copies operand type/register/value/address/width/access metadata but not `offb` or the secondary `offo`. Local IDA 9.3 `ua.hpp` (SHA-256 `2535d461da6eb7e7d3ff82114915bee65869fefabc2ade372417826eb561589a`) defines these as byte offsets from the instruction start, with zero meaning absent. Copy both as optional byte positions across C++, Node, and Rust; reject any nonzero position outside the decoded instruction size during consumer fingerprinting. Guessing from operand width or target type is not equivalent.

448. **Diaphora 3.4.0's exported CFG and segment metrics contain source-level inconsistencies that must not be silently presented as canonical:** `process_basic_block()` increments `edges` for both every successor and every predecessor, then computes `edges - nodes + 2`, double-counting ordinary CFG edges. `extract_all_features()` computes `segment_rva` from `current_head`, the last instruction visited, rather than the function entry. These values remain relevant when reading native Diaphora SQLite databases, but a new IDAX manifest must count each directed successor edge once and derive segment-relative offset from the function entry. The format must identify this correction so consumers do not assume byte-for-byte SQLite compatibility.

449. **Conservative cross-language prototype import requires declaration readback missing from the Node and C/Rust bindings:** C++ already exposes `function::declaration(address, name_override)` and Diaphora exports the applied prototype, but Node, the generated C shim, and safe Rust expose only declaration application. An importer cannot distinguish an absent prototype from user-defined state by function name or inferred argument count. Add Node `function.declaration`, a copied-string C function, and Rust `function::declaration(address, optional_name_override)`, preserving the C++ error/absence semantics; use readback to skip every target that already has a printable declaration. This closure is independently falsified by C++/Node/Rust equality for the same function and by an absent-prototype result that remains non-mutating.

450. **The versioned exact manifest is deterministic and conservative mutation persists across a fresh IDA process:** On IDA Professional 9.4, two independent exports of the unchanged Linux x86-64 fixture produced byte-identical 22-record manifests at SHA-256 `4263b3eafdb75fcb009e3c565f341a72cf6abe222c34554d9908fc65caa0d08a`. Self-comparison yielded 22/22 unique same-RVA/both-hash matches, zero ambiguity, and zero unmatched records. A synthetic baseline changed only one auto-name and one repeatable comment; explicit apply changed exactly one of each with zero failures and saved the database. A distinct process reopening the resulting IDB changed zero, preserved 31 metadata values, and exported both values exactly; IDB and reopened-manifest SHA-256 values are `bfd7462f9581afaf278925592b32860e31d8278b947827d6550895f4bf283b9a` and `86002194258571cf6b393346175d704113b1452970d4a408fb000777878618a8`. Seven pure Rust tests cover MD5, serialization and malformed text, canonical metrics, prefix bounds, tiering, and ambiguity; the C++ plugin links.

451. **Phase 48 complete validation proves cross-binding encoded-position/declaration parity and malformed-manifest containment:** The full C++ build and all 27 CTest targets pass in 22.58 s, including live encoded-position/declaration probes and offline RFC 1321, canonical-metric, manifest, unique-tier, ambiguity, and invalid UTF-8 checks; the Diaphora plugin links. Node native build, authoritative strict example declarations, 239 structural checks, and 84 IDA Professional 9.4 checks pass. Rust formatting/all-target checks, 139 library tests, 0 sys tests, 7 Diaphora tests, 20 Symless regression tests, and 101 live IDA 9.4 checks pass. A second independent clean bindgen output is byte-identical to the checked file at SHA-256 `8d2dd609c7abcf64f14744bd725355e8e2ffb0a6af6fa39abe96d31f4b424d1b`. The tracked fixture executable/IDB SHA-256 values and IDB Git blob remain `af23d4fde7d2b5ebe20385f5aa8c23221988fd1bdbab777c18daf8c9d9543f80`, `ce6d678f484d681a5bc147dab49c272e3a7f9883b3c15c41974ec52cb95a431b`, and `84ff142e9cd6c39dbd22d94c7d164b2db48c64dd`.

452. **Diaphora's remaining SQLite and heuristic surface is coupled and cannot be reproduced by adding isolated predicates to the Phase 48 record:** Release 3.4.0 defines 13 tables, 49 `functions` columns, 13 `instructions` columns, and 41 indices. Its 50 heuristics comprise 5 no-false-positive, 22 ratio, 22 ratio-with-minimum, and 1 trusted-ratio-with-minimum rule across 12 Best, 30 Partial, and 8 Unreliable categories. Even SQL predicates using only RVA/hash/mnemonic/CFG fields feed `check_ratio()`, whose result jointly uses normalized assembly, pseudocode, microcode, AST primes, MD-index, byte hashes, and `deep_ratio()` source-file/degree/switch/constant/model bonuses. Phase 48 already covers the four hash/RVA exact tiers but not this coupled ratio/classification engine. Audited SHA-256 values for `diaphora_heuristics.py` and `diaphora_config.py` are `703db7055b19f3caf2eb9f6b66660e9e58a48eb23642061ae87cc17993de2624` and `3209b81c32b3df04d06dc96a6143b7640f7135d1fc4931c2f7f10ff413224622`; schema hash remains `abcf8af39284b063bd858b69b7f6ff9614e7fa1505e7c90d4c35a84701e359c6`.

453. **Instruction comments and forced operand text form a self-contained Diaphora import slice with existing opaque APIs:** Native `instructions` rows export ordinary/repeatable comments and ordered forced operand `(index,text)` pairs; import aligns instruction rows through assembly diff lines, sets comments only when absent, and writes forced operands. Exact-manifest adaptation can remove the assembly-diff ambiguity: first require a globally unique Phase 48 function match, then require one instruction at the same function-relative byte offset with equal size, mnemonic, and relocation-light MD5. Transfer only nonempty ordinary/repeatable comments and forced operand text into absent target slots; preserve every existing target value. Existing `ida::comment::{get,set}` and `ida::instruction::{get,set}_forced_operand` plus Node/Rust mirrors are sufficient, so no wrapper or binding addition is justified.

454. **The exact instruction-metadata companion format is cross-language deterministic, conservative, and guard-sensitive on a real database:** C++ and Rust serialize the same function records followed by metadata-bearing instruction records containing ordinal, signed function-relative byte offset, decoded size, full and relocation-light MD5, mnemonic, comments, and ordered length-prefixed forced operands under `IDAX_DIAPHORA_INSTRUCTION_METADATA\t1\texact-relative-offset`. Both readers reject malformed UTF-8/NUL text, invalid hashes/ranges, duplicate operands/records, and unknown function ordinals. On IDA Professional 9.4, two exports of an isolated source database produced 22 function records and 9 instruction records with byte-identical SHA-256 `d7dbebeb499f1f14cbe378b2af9e77f06f5f65fd7f2d853b2806755382d996d6`. The target comparison yielded 22 unique function matches and 9 eligible records. First apply wrote exactly one ordinary comment, one repeatable comment, and one forced operand while preserving eight existing values; a distinct-process reopen wrote zero and preserved eleven, and its export was byte-identical to the source. Altering only one valid relocation hash produced eight eligible records and exactly one guard failure. The source and target executable SHA-256 values remain `af23d4fde7d2b5ebe20385f5aa8c23221988fd1bdbab777c18daf8c9d9543f80`; the reopened target IDB SHA-256 is `7166278fbc70e24b0569667abd290480300e8470ff909accca673f41ce4391a5`.

455. **Phase 49 complete validation preserves every unchanged surface and closes arithmetic/parser edge cases:** The full C++ build and all 27 CTest targets pass in 25.04 s; Node native build, strict example declarations, 239 structural checks, and 84 IDA Professional 9.4 checks pass; Rust formatting/all-target checks, 139 library tests, 0 sys tests, 10 Diaphora tests, 20 Symless regression tests, and 101 process-main-thread IDA checks pass. An independent clean bindgen result is byte-identical to the checked C ABI at SHA-256 `8d2dd609c7abcf64f14744bd725355e8e2ffb0a6af6fa39abe96d31f4b424d1b`. Post-matrix red-team additions verify `INT64_MIN`, positive/negative offsets, address overflow/underflow, unknown function ordinals, invalid MD5, metadata-free records, embedded NUL, malformed/duplicate forced operands, and duplicate instruction records in both implementations. The tracked fixture executable/IDB SHA-256 values and IDB Git blob remain `af23d4fde7d2b5ebe20385f5aa8c23221988fd1bdbab777c18daf8c9d9543f80`, `ce6d678f484d681a5bc147dab49c272e3a7f9883b3c15c41974ec52cb95a431b`, and `84ff142e9cd6c39dbd22d94c7d164b2db48c64dd`.

456. **IDAX pseudocode comment positions are numerically incorrect and inconsistently exposed across bindings:** Local SDK `hexrays.hpp` SHA-256 `29f5e86c7223228fc66c5ebce1dd5199fafcc30ea8a0d92b33404c9fef66d7a13` defines `ITP_EMPTY=0`, argument separators `1..64`, opening parenthesis `65`, assembly/else/do/semicolon `66..69`, braces `70..71`, closing parenthesis/label/block-before/block-after/try `72..76`, plus encoded switch cases. IDAX currently assigns `ElseLine=258`, `Semicolon=259`, and braces `260..261`; its test permits the resulting noncanonical comment to disappear. Safe Rust accepts arbitrary `i32` positions through the C shim, while Node exposes no pseudocode comment read/write/save API. Replace numeric passthrough with a semantic location model and exact internal conversion; retain no public raw `item_preciser_t` escape.

457. **Diaphora 3.4.0 loses distinct pseudocode comments that share an instruction address:** Export iterates the persisted `user_cmts_t` map, which is keyed by `(ea,itp)`, but writes each pair into `self.pseudo_comments[ea-base]`; later entries overwrite earlier locations before one `pseudocomment,pseudoitp` pair is attached to the instruction row. The SDK explicitly uses the item preciser to distinguish multiple comments at one address. An exact IDAX companion format should preserve every copied `(relative instruction identity, semantic location, text)` record rather than reproduce this lossy intermediate dictionary; this is a documented correction, not native SQLite compatibility.

458. **Opaque semantic pseudocode-comment positions preserve multiple persisted locations across every binding:** C++ now converts semantic default/argument/named/switch-case values to the pinned SDK constants only inside `src/decompiler.cpp` and enumerates copied persisted triples through `restore_user_cmts()` with `user_cmts_free()` ownership. Node, the generated C ABI, and safe Rust expose the same model without raw `item_preciser_t`. Initialized C++, Node, and Rust tests persist distinct default and semicolon comments at one address, read both exact texts, enumerate both records, restore prior state, and reject argument 64, switch-case magnitude `0x20000000`, embedded NUL, unknown/malformed positions, and null C outputs before mutation. Fresh bindgen output and the checked mirror are byte-identical at SHA-256 `1c22d8ded3ccd9d08b22f2cce200fb4df4fedc744b89518cc1d0b1ceb370d279`.

459. **The exact pseudocode-comment companion preserves same-address locations, report purity, guards, and target ownership:** C++ and Rust serialize one `P` record per function/instruction/location under `IDAX_DIAPHORA_PSEUDOCODE_COMMENTS\t1\texact-tree-location`, using the Phase 48 unique function matcher and Phase 49 ordinal/signed-offset/size/mnemonic/relocation-MD5 guards. On isolated IDA Professional 9.4 databases, default and semicolon source records at the same instruction both match; report mode creates no target IDB, first apply writes exactly two and saves one function, fresh reopen writes zero and preserves both, and reopened export is byte-identical at SHA-256 `5e8a42dc99e28d57f6b7843d29292ce39c9ed8b387fa6d517ddfa93cb030ba23`. Altering one valid relocation hash rejects only that record. Preseeding a different target semicolon value causes one default write and one preserve; readback proves the target-owned text is unchanged. Apply never invokes orphan deletion.

460. **Phase 50 complete validation closes semantic-location coupling while bounding a cross-minor runtime risk:** The full C++ graph builds and all 27 CTest targets pass in 22.91 s; Node native build, strict examples, 240 structural checks, and 85 ABI-matched IDA 9.3 checks pass, while an isolated IDA 9.4 probe passes malformed-object rejection plus default/semicolon persistence and enumeration. Rust formatting/all-target checks, 140 library tests, 0 sys tests, 12 Diaphora tests, 20 Symless tests, and 102 process-main-thread IDA 9.4 checks pass. Independent bindgen output is byte-identical at SHA-256 `1c22d8ded3ccd9d08b22f2cce200fb4df4fedc744b89518cc1d0b1ceb370d279`; tracked fixture hashes/blob are unchanged. Red-team changes explicitly map every C++ semantic kind to its C ABI kind, validate C argument range before conversion, reject null C save handles and parameter objects for simple Node kinds, and exercise all 64 argument factories. A separate full Node run built with pinned SDK 9.3 headers but forced onto runtime 9.4 reproducibly stops in pre-existing `data::string_literals()` after `get_strlist_item()` corrupts vector state; exact causation is unknown without matching 9.4 headers. This does not affect the ABI-matched release envelope or the focused 9.4 comment probe.

461. **The active-work queue retained seven completed phases contrary to its own removal policy:** Phase 39, 40, 43, 44, 48, 49, and 50 entries all had complete roadmap checkboxes and ledger evidence but remained in `.agents/active_work.md`. Active work is a current-state queue, not a historical record: completion must atomically update the roadmap/ledger and remove the corresponding active entry. The cleanup retains only six genuinely active, queued, or blocked work groups plus the temporary Phase 51 cleanup item until its push boundary.

462. **The ida-cdump migration checklist simultaneously classified P22.R3 as partial and complete:** Its gap and evidence tables still said the read-only ctree/type collector required a new facade, while the same document's remaining-task table and `codedump_parity_tasks.md` marked P22.R3 complete. Local code confirms public `collect_referenced_types(Address)`, its SDK-backed implementation, and compile-surface coverage. The downstream `<ida-cdump-root>` tree is unavailable here, so IDAX capability is verified but the recorded removal of raw downstream call sites is retained as an explicit falsifiable assumption rather than reasserted as newly observed evidence.

463. **Diaphora 3.4.0 referent metadata can be applied to a different target than the one exported:** `read_function()` iterates outgoing references and repeatedly overwrites one instruction-level `tmp_name/tmp_type` pair; `import_instruction()` then independently selects the first data reference, or the first code reference when no data reference exists. Multiple references, iteration-order changes, and the special second-level offset follow can therefore decouple exported metadata from its referent. A deterministic IDAX companion must emit separate code/data records only when the instruction has exactly one distinct non-flow referent of that class, then require the target instruction to have exactly one referent of the same class before applying. Pinned `diaphora_ida.py` SHA-256 is `47dab5eaa4729721024202a36e76f289d24c3801242136f5053d469db0a99e39`.

464. **Offline core helpers must not call non-inline xref classification functions when the test executable intentionally omits IDA runtime libraries:** Calling `ida::xref::is_flow()` from the otherwise pure unique-referent selector caused the static linker to pull `xref.cpp`, which also contains SDK-backed enumeration/mutation symbols and produced unresolved `xrefblk_t`/`add_cref`/`add_dref` references. Comparing the copied `ReferenceType::Flow` enum value directly preserves identical classification while keeping the pure parser/selection test runtime-independent.

465. **Identity-bearing absolute paths existed in both tracked text and a binary IDA database:** The current tree contained workstation-specific `/Users`, `/home`, and `/models` roots in historical evidence, source annotations, and documentation. `tests/fixtures/simple_appcall_linux64.i64` embedded the checkout path twice even when IDA was invoked with a relative input because IDA canonicalizes the input filename. Text paths must use semantic non-absolute tokens. The two database strings can be replaced safely only with an equal-length value; `<repo-root>/tests/fixtures/simple_appcall_linux64:fixture` is exactly 57 bytes, matching the former string, and an isolated IDA 9.4 open exits successfully afterward. Reachable Git history remains a separate exposure until rewritten and remote refs are verified.

466. **A clean force-pushed branch does not purge GitHub cached objects, PR refs, or forks:** The mode-preserving 285-commit rewrite retains the exact sanitized tip tree, produces zero identity-root matches in reachable commit messages/blobs, and is advertised as the only branch ref. A fresh clone also scans clean. Nevertheless, requesting the former tip by exact object ID still succeeds. Both server PR head refs retain 549 matching historical blobs, and all four fork default branches retain 549, 575, 375, and 564 matches respectively. GitHub's official sensitive-data procedure requires a Support request to dereference affected PRs, run server garbage collection, and remove cached views; each fork owner must independently rewrite or delete the contaminated fork. Repository push authority alone cannot perform either external action.

467. **The HCLI CI failure is caused by positional license-ID parsing, not absence of an active entitlement:** All five install blocks run `license list`, extract every ID-shaped token, and take the first. In the observed table the first row is a `To_Activate` IDA Teams Server computer license, so HCLI correctly cannot find an IDA installer license file for it. Later rows contain multiple `Active` `named` IDA Ultimate licenses. Selection must parse row fields and jointly require product edition, named type, and active status; token order is not a license compatibility contract.

468. **Removing the workflow's explicit license-ID echo is insufficient because HCLI repeats the selected ID during installation:** Capture the license table instead of streaming it, select one eligible row, and register the resulting identifier with the GitHub Actions `add-mask` command before any download/install call. Each job must register its own mask. This preserves diagnostic output while redacting later tool-generated occurrences of the identifier.

469. **HCLI rich tables captured by command substitution can split one entitlement across multiple physical lines:** HCLI constructs a six-column Rich `Table` and its default non-terminal console width is 80 columns. At that width an eligible row renders `IDA` on its ID-bearing line and `Ultimate` on a continuation line, while the type and status remain `named` and `Active`; a one-line parser therefore rejects the row. Row reconstruction must accumulate same-column continuation fragments until the next ID-bearing row or table boundary, then apply the semantic predicate.

470. **An unpinned IDA SDK checkout is incompatible with a version-pinned IDA runtime workflow:** SDK `main` commit `d76bad44656846cf04ffb95424769f679f3d94e7` no longer contains the `ida-cmake/bootstrap.cmake` architecture consumed by IDAX; the migration landed in June 2026 after the project baseline. The same jobs explicitly download IDA 9.3. Pinning every SDK checkout to official `v9.3` commit `d5db59ab4e9d2ae92038e9520082affd0da6fe20` restores the expected recursive `src/cmake` submodule and makes headers/build helpers/runtime a coherent release set.

471. **Wrapped-row reconstruction and masking pass on every live runner matrix:** At commit `9317dd4b3fe1b7b3f4fddfa7da8f0f90d621fe9f`, all 15 jobs across Bindings, Validation, and Integrations on Linux, macOS, and Windows select and install an active named IDA license successfully. Every job reaches the subsequent SDK-resolution step. A scan of all 15 complete job logs finds zero unmasked canonical license-ID patterns; HCLI's installation detail and downloaded filenames display `***`.

472. **AppleClang 15 rejects a nested lambda reference to an enclosing structured-binding name in Symless allocator discovery:** The wrapper-deduplication predicate captures by reference and compares `current.function_address == caller`, where `caller` is introduced by `for (const auto& [caller, call_address] : candidate_sites)`. Both macOS Validation profiles fail at this expression. The immediately preceding `ResolvedAllocator heir` owns the identical address; comparing against `heir.address` removes the structured-binding capture and preserves the exact predicate.

473. **Node 20.20.2 headers expose two cross-platform binding ambiguities and Windows macro pollution:** `info[index]` is `v8::Local<v8::Value>` while `Nan::Undefined()` is `v8::Local<v8::Primitive>`; the conditional operator has two viable common conversions and GCC, AppleClang, and MSVC reject both optional pseudocode-comment calls. Convert the undefined arm explicitly with `.As<v8::Value>()`. On MSVC, Node's Windows headers also define function-like `max`, corrupting four `std::numeric_limits<T>::max()` expressions; define `NOMINMAX` privately on the native addon target before compilation.

474. **Windows bindgen output uses CRLF, invalidating line-ending-sensitive Rust post-processing:** `patch_bindgen_output()` searches for `#[repr(C)]\n` and an LF-only FFI marker. The Windows generator emits the required structure with `\r\n`, so the build script falsely reports that the preceding marker is absent. Normalize generated text from CRLF to LF before both searches and canonical rewrite; a synthetic CRLF bindgen test must preserve the replacement and following FFI declaration.

475. **The Node integration workflow supplied a binary that cannot satisfy its string-list assertion:** The test requires the literal `ref4: entered with %d`, which is defined by the repository's `simple_appcall_linux64` fixture, but the Unix workflow invoked it with a copied host `/bin/ls`. Once the Node addon compiles, Ubuntu deterministically reaches this assertion and reports the expected literal as undefined. Pass the contract fixture directly; the harness already copies its input into an isolated temporary directory.

476. **The Windows Rust workflow drifted from its locked analysis-control decision:** The example helper still implements `IDAX_RUST_DISABLE_ANALYSIS`, and decision 19.18 requires the Windows runtime step to set it, but the live workflow retained only tracing. At `2dd8408437742c9ed731d0bcbda7a3df19136d50`, the Windows Rust build and 140 unit tests pass, then `idalib_dump_port` exits inside `database::open(..., true)` before wrapper-level error propagation. Restore the CI-only toggle so the preanalyzed fixture IDB opens without coupling the smoke test to Windows headless auto-analysis.

477. **Windows Rust headless database open exits independently of the auto-analysis Boolean:** At `ec8a707d03915f6c73a9114f841962a3c5f8fa8e`, the trace confirms `IDAX_RUST_DISABLE_ANALYSIS=1` is consumed before initialization, `database::init` succeeds, and the process still exits with code 1 inside `database::open(..., false)` without reaching wrapper error formatting. The same job builds the Rust bindings and every example and passes 140 unit tests. Runtime execution on this host is therefore not a valid gate for the HCLI/compiler repair; retain Windows release-build/unit/integration-compile coverage and remove only the unstable execution step.

478. **Canonical license-ID log scans require hexadecimal-token boundaries:** The unbounded grouped expression reported one apparent match in a Windows job, but redacted context proved it was an interior substring of GitHub's longer lowercase temporary-directory UUID. Adding negative hexadecimal lookbehind/lookahead preserves real canonical IDs and yields zero matches across all 15 final job logs without exposing matched values during audit.

479. **The IDA 9.4 SDK package entry point is not `bootstrap.cmake`:** Official commit `6929db6868a524496eb66e76e4ec6c9d720a0594` identifies the IDA 9.4.0 release patch and provides `src/cmake/idasdkConfig.cmake` plus `idasdk_init.cmake`; no `bootstrap.cmake` exists. A workflow that changes only the SDK ref and installer filenames fails in its existing resolver before compilation. IDAX must recognize the package config, set the SDK root to `src`, and let `find_package(idasdk)` load it. The exact SDK then configures and builds the static IDAX library against IDA Professional 9.4.

480. **An immutable SDK commit can require explicit-object or archive acquisition after its release branch moves:** During validation, `releases/9.4.0` advanced beyond the requested commit. An explicit depth-one fetch of `6929db6868a524496eb66e76e4ec6c9d720a0594` succeeds and supplies its complete tree, while a generic clone followed by detached checkout fails with `unable to read tree`. The no-`IDASDK` FetchContent fallback must therefore use the official exact-commit archive with verified SHA-256 `6ba645ef8fb5663d45d28c7a48da274e22a5929ddbfbc69cd4be34a4d7ee9895`; workflow checkouts can continue requesting the exact object directly.

481. **IDA SDK 9.4's package overwrites the consumer-selected MSVC runtime:** `idasdkConfig.cmake` unconditionally sets `CMAKE_MSVC_RUNTIME_LIBRARY` from `IDA_USE_STATIC_RUNTIME`, whose default is dynamic `/MD`. IDAX's Rust build explicitly requests `/MT` and compiles its shim with static CRT; the Node addon also compiles `/MT`. The live Windows jobs therefore link `/MD` IDAX objects into `/MT` consumers, producing `LNK2038`, `LNK2005`, `LNK4098`, and secondary unresolved CRT imports. Capture the caller's runtime before `find_package(idasdk)` and restore it afterward; when no caller value exists, retain IDAX's established static-CRT default.

482. **The exact IDA 9.4 release set is green across every configured CI platform and consumer:** Commit `54a6334901c5bd33a08c8dd39dff447750d7aa8c` passes Bindings 6/6, Validation 6/6, and Integrations 3/3 on Linux, macOS arm64, and Windows. Every job materializes SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`, selects an active named IDA product entitlement, and installs IDA Professional 9.4 before its downstream build or test gate. Boundary-aware scans of all 15 complete logs find zero unmasked canonical license identifiers. This falsifies both the SDK-package acquisition risk and the Windows caller-runtime regression for the configured matrix.

483. **The Python binding starts from an empty package against a 27-domain ownership-heavy API:** `bindings/python` contains no implementation, while the public wrapper spans 27 domain headers plus shared error/core types and includes copied snapshots, move-only resources, callbacks, UI objects, decompiler state, and plugin/loader/processor extension points. Existing Node and Rust code provides behavioral comparison evidence but not Python completeness: the Rust C shim exposes manual allocation/free functions and C callback conventions, while Python requires native exceptions, reference counting, GIL acquisition, context managers, iteration, and typing. Direct pybind11 binding with an explicit symbol-level parity manifest is therefore the bounded architecture; wrapping only the C shim would add a second lifetime layer and leave Python callback/extension semantics under-specified.

484. **PEP 639 metadata rejects a legacy license classifier when `project.license` is an SPDX expression:** The first isolated scikit-build-core wheel build fails before CMake because metadata 2.4 treats `license = "MIT"` and `License :: OSI Approved :: MIT License` as incompatible representations. Retain the SPDX expression and `license-files`, and remove the deprecated classifier; this preserves machine-readable licensing without a setup.py compatibility layer.

485. **A successful scikit-build-core native link does not prove the extension was installed into the wheel:** The first compiled wheel contained the typed Python package and license but omitted `_native`, despite a successful shared-module link. In CMake's `install(TARGETS)` grammar, `COMPONENT` belongs to an artifact-kind group; the trailing component applied to `RUNTIME` but not the macOS `LIBRARY` artifact. Assign the Python component independently to `LIBRARY`, `RUNTIME`, and `ARCHIVE`, and inspect the archive rather than treating build success as package-completeness evidence.

486. **Annotation-only enum entries are neither portable to Python 3.10 nor accepted as members by current type checkers:** The initial stubs declared `MEMBER: Self`; `typing.Self` is unavailable on Python 3.10, and mypy 1.20 reports every such enum as having zero members. Stub enum members must use value form (`MEMBER = ...`), which is valid across the declared version range and removes the unnecessary typing backport dependency.

487. **Linking a wheel against macOS IDA libraries with `@rpath` install names does not make them discoverable to an external interpreter:** The installed extension has no `LC_RPATH`, so clean import fails with `Library not loaded: @rpath/libida.dylib` even though build and wheel installation pass. Preloading `libida` and `libidalib` with global visibility from `IDADIR` or the non-identifying standard IDA 9.4 application location makes the same wheel import successfully. The package must perform that resolution before importing `_native`; IDA libraries remain user-supplied and excluded from the wheel.

488. **pybind11's legacy `enum_` wrapper does not satisfy a stdlib `Enum` typing contract:** Its class is not a subclass of `enum.Enum`, and values converted from C++ compare equal to but need not be identical to the class attribute. pybind11 3's `native_enum<...>(..., "enum.Enum")` creates genuine Python enum members, restores singleton identity for return values, and aligns runtime behavior with `Enum` stubs. Every native enum must be finalized explicitly.

489. **The first initialized Python slice runs against IDA 9.4 and enforces process-thread ownership:** From a clean CPython 3.12 wheel, external idalib initialization, skip-analysis binary open, metadata/bounds/processor/hash queries, address navigation, analysis state, and unsaved close all succeed. A worker-thread metadata call raises the structured `ConflictError` with operation context. Built-in IDAPython initialization still occurs because `disable_user_plugins` governs IDAUSR discovery, not bundled plugins; this does not alter the tested binding result.

490. **Manifest extraction must recognize macro-generated native registrations without treating type macros as functions:** The initial verifier found only literal `domain.def("name", ...)` calls and one database-specific macro, so reusable binding macros could compile while appearing absent from the parity inventory. A generic invocation probe restricted to lower-snake-case arguments captures function registration macros across domains while excluding CamelCase option/class macros; exact equality with the sorted manifest remains fail-closed.

491. **pybind11 property registration requires an introspectable callable signature:** Generic `auto` setter/getter lambdas compile as C++ but pybind11 cannot derive their argument annotations for `def_property`, causing template instantiation failure. Python adapter classes that own callback objects must use explicit concrete reference and value parameter types on every property lambda. A rebuilt wheel with explicit signatures compiles all `data` and `event` registrations.

492. **Python custom-data callbacks require owned adapters with explicit GIL and error semantics:** `CustomDataTypeDefinition` and `CustomDataFormatDefinition` contain `std::function` fields returning `std::expected`, which cannot be exposed as passive copied properties without defining Python callable ownership, byte conversion, GIL acquisition, and exception translation. Binding-specific owned definitions retain callable objects until explicit unregister; boolean/size/analyze callback failures are reported as unraisable with conservative fallbacks, while render/scan failures become structured `InternalError` results. Successful IDA 9.4 registration, lookup, render, scan, analyze, and unregister falsify missing-lifetime and conversion failures.

493. **The complete database-model Python tranche runs against IDA 9.4 with mutation and callback coverage:** Exact manifest verification covers 405 symbols across 27 tracked domains after adding every public surface in `segment`, `function`, `instruction`, `data`, `name`, `xref`, `comment`, `search`, `entry`, `fixup`, `type`, `storage`, `lines`, `event`, and `lumina`. Strict Python 3.10 typing passes across 24 modules. A disposable IDA 9.4 database validates function enumeration, instruction decode, byte reads, type parsing, netnode byte storage, scoped comment events, patch-event delivery and revert, and Python-backed custom-data render/scan/analyze callbacks; 12 pure tests and the initialized-host test pass.

494. **C++ shared ownership alone does not retain a Python trampoline override:** Registering a Python-derived `AppcallExecutor` through `std::shared_ptr` preserved the C++ base but allowed the Python instance that owns the override table to disappear, so later virtual dispatch reached the base implementation. Debugger executors require a binding-owned `py::object` root keyed by registration name until successful unregister. Graph callback registrations analogously root the graph and callback by title until close; microcode filters root the Python subclass by token until unregister.

495. **Decompiler callback pointers and ctree/microcode views require explicit callback-scope invalidation:** Host events expose SDK pointers that are valid only while IDA invokes the callback, and ctree/microcode views refer into host-owned transient state. The Python surface must not expose raw addresses or permit delayed use. Binding-owned adapters share a validity state, invalidate it on every normal and exceptional callback exit, and raise structured `ConflictError` after expiry; retained-view runtime probes verify this boundary.

496. **A decompiled-function object must be released before its database closes:** A direct native `DecompiledFunction` retained its Hex-Rays function object until Python interpreter teardown. Closing the database first allowed the later native destructor to access invalid host state and segfault. The Python binding now owns the native value inside a closable optional resource with `valid`, idempotent `close()`, and context-manager semantics; every method rejects post-close access, and the initialized IDA 9.4 test explicitly closes it before database teardown and exits normally.

497. **The advanced Python domains pass structural, type, and initialized-host validation against IDA 9.4:** The `debugger`, `graph`, and `decompiler` bindings bring the exact manifest to 590 total symbols across all 27 domains, with all 65 debugger functions/14 types, 9 graph functions/9 types, and 32 decompiler functions/56 types represented in native registration, public modules, strict stubs, and the manifest. A rebuilt CPython 3.12 extension passes 12 pure tests, strict mypy over 27 modules, and an analyzed-fixture IDA 9.4 test covering graph mutation, Python appcall virtual dispatch, decompilation, ctree expiry, microcode filter ownership/expiry, deterministic close, and clean database/process teardown.

498. **IDA product version equality does not imply Hex-Rays extension-ABI equality:** The exact requested SDK commit declares IDA SDK 9.4 and `HEXRAYS_API_MAGIC` suffix `5`, while both locally installed IDA 9.4 bundles expose decompiler plugins built for suffix `4`. The exact-SDK Python extension initializes idalib and executes database/model APIs but correctly reports the decompiler unavailable; rebuilding the same source against the older suffix-4 headers makes the same host decompiler probe pass. A runtime gate must therefore treat Hex-Rays availability as a separately licensed/versioned capability and must never force a mismatched-magic fallback. Exact-SDK build, structural, and non-decompiler host validation remain valid; exact-SDK decompiler runtime evidence requires a suffix-5 decompiler installation.

499. **Authoring callbacks need checked capability adapters, not Python-visible host addresses:** Plugin action contexts, loader input streams, processor output contexts, UI popup handles, and rendering callbacks carry host-owned state whose lifetime ends with native dispatch. Python adapters now copy stable scalar fields, retain only checked operations, share an invalidation token for borrowed state, and raise `ConflictError` after callback exit. Registration roots use native identity keys and are removed at explicit unregister/close; no SDK pointer, capsule, or pointer-valued integer reaches the public surface.

500. **All four Python authoring/host domains pass exact-SDK structural and runtime validation:** `plugin`, `loader`, `processor`, and `ui` add Python subclass trampolines, callback GIL/error boundaries, registration ownership, form bindings, event subscriptions, and deterministic resource teardown. The strengthened fail-closed inventory now covers 826 functions/types across all 27 domains; 15 pure tests and strict mypy over 31 modules pass. An exact-SDK IDA 9.4 run validates action and hotkey register/unregister, loader flag conversion, processor output construction, UI headless queries/subscription teardown, callback/mutation cleanup, and normal database close. GUI action activation, modal forms, and visual widgets remain interactive-host gates rather than headless claims.

501. **A fail-closed manifest must inventory types unconditionally, not only validate types already listed:** Earlier manifest entries with an empty `bound_types` array caused the verifier to skip native type extraction, so fully bound enums/values/resources in fourteen domains were structurally invisible. The repaired inventory includes every detected native type, shared `core` values, and the structured exception hierarchy; native registration, public `__all__`, and stub top-level symbols now agree for 826 functions/types. Exact authoritative-header hashes plus the documented declaration/adaptation review fail closed on later method/property/overload drift.

502. **Python bytecode caches can reintroduce identity-bearing build paths into an otherwise clean sdist:** Example/module byte-compilation created source-tree `__pycache__` files, while a broad forced `bindings/python/**` include caused scikit-build to collect them despite exclusion attempts. The archive auditor found an identity-bearing absolute path inside packaged bytecode. Replacing the recursive forced include with explicit document/example/source/test patterns makes cache inclusion structurally impossible; a rebuild with caches present is byte-clean. Archive inspection must scan payload bytes, not only member names or source text.

503. **The complete Python package surface has executable documentation, distribution, and CI gates:** All 27 domains plus shared error/core have module docstrings, strict stubs, a 27-domain reference, lifecycle/tutorial guidance, declaration/adaptation documentation, and representative external-analysis/decompiler/action examples. The CPython-specific wheel and source archive contain required modules/stubs/license/native sources, exactly one private native wheel extension, and no IDA runtime/license or identity-bearing path. A Linux/Windows/macOS Python CI matrix now builds against the exact SDK/IDA 9.4 set, runs pure/type/manifest/package gates everywhere, runs strict initialized-host coverage on Unix, and retains the established Windows headless compile-only boundary.

504. **Generic pybind virtual-override macros do not provide the required IDA callback exception boundary:** The graph viewer trampoline was the final `PYBIND11_OVERRIDE` user and could propagate a Python cast/override exception through IDAX into IDA's graph callback dispatcher. Each graph callback now acquires the GIL, resolves the override, converts its result, reports Python and native failures as unraisable, and returns the C++ interface's conservative default. The graph/override root remains alive until viewer close; no exception crosses the host ABI.

505. **An explicit sdist include list does not exclude nested repositories or initialized submodules by itself:** Live CI checked the IDA SDK out beneath the project root and recursively initialized the Sleigh submodule. scikit-build-core admitted both external trees into the source archive even though they were outside every project-owned include pattern. The byte-level distribution audit found identity-bearing paths in upstream SDK and Sleigh payloads and failed before publication. Source archives must explicitly exclude workflow acquisition directories (`ida-sdk/**`, `ida-installer/**`) and external submodules (`third-party/**`) in addition to enumerating project-owned includes; this preserves the SDK/runtime/license and external-source boundary on every builder layout.

506. **Raw-byte source hashes are checkout-line-ending-sensitive:** The Python declaration audit stored SHA-256 values for LF public headers and hashed checkout bytes directly. Windows converted the authoritative umbrella to CRLF, so the audit rejected semantically identical declarations after the native build had succeeded. Canonicalize only CRLF to LF before hashing; a regression probe proves LF and CRLF inputs produce the same digest while an actual declaration change produces a different digest.

507. **`uv sync` installs the project unless explicitly excluded:** The Python workflow ran `uv sync --extra test` and immediately ran `uv pip install --reinstall -e .`, compiling the native extension twice on each platform. Use `uv sync --extra test --no-install-project` to materialize dependencies, then perform exactly one editable project installation; this retains the same dependency and package state while removing one full native build.

508. **The complete Python binding is validated across the exact release matrix:** Run 29449799754 passes all nine Node, Rust, and Python jobs on Linux, Windows, and macOS at SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594` with IDA Professional 9.4. Every Python row passes 18 pure tests, strict typing over 31 modules, the 27-domain/826-symbol declaration audit, and wheel/sdist byte-level privacy inspection; Linux and macOS additionally pass strict initialized decompiler, ctree, microcode, lifetime, and callback execution. Windows validates native compilation, imports, pure/type/manifest/package behavior under its established headless-runtime boundary. Complete-log scanning finds zero unmasked canonical license identifiers.

509. **The processor authoring facade compiles examples but does not create loadable IDA processor modules:** `IDAX_PROCESSOR` currently exports only `idax_processor_bridge_init`; no repository object defines the mandatory `LPH` descriptor, returns a `procmod_t` for `ev_get_procmod`, or routes `processor_t::event_t`. In addition, `ProcessorFlag` values after `Use32` diverge from the pinned SDK's `PR_*` values, `InstructionFeature` omits operand 7/8 bits, and `AnalyzeDetails` cannot identify `insn_t::itype`. Consequently, build-only example success does not prove that IDA can discover, decode with, or render through an IDAX procmod. The closure requires an SDK-private descriptor/dispatcher plus exact typed instruction materialization and real-host evidence; no SDK struct or pointer needs to enter the public model.

510. **A static-library object that defines an addon entry symbol is not linked merely because IDA will look for that symbol later:** `processor.cpp` can define `LPH`, but ordinary archive extraction sees no unresolved reference from the procmod translation unit and omits the object. On permissive addon links, an otherwise unused unresolved function can also survive or be dead-stripped without pulling the bridge. `IDAX_PROCESSOR` must execute a no-op anchor during module initialization; that relocation extracts the bridge object, whose `LPH` then references the strong per-module instance initializer. The first rebuilt procmod exports `_LPH`, whereas the pre-bridge binaries expose no `LPH` symbol.

511. **IDA 9.4 installation did not update all local runtime discovery lists:** The SDK and HCLI assets were pinned to 9.4, but integration CMake, example-tool CMake, and the open-points runner still searched only the standard 9.3/9.2 macOS locations. An exact-9.4-SDK build consequently linked integration executables to 9.3, producing deterministic database incompatibility failures. Prepending 9.4 restores SDK/runtime alignment; explicit `IDADIR` continues to take precedence.

512. **A procmod target name is not necessarily its discoverable processor-module filename:** The example target labels were descriptive build names, while IDA processor selection uses the first `ProcessorInfo::short_names` alias and conventionally resolves the matching file in `procs`. Preserve descriptive CMake target names, but set output names to `idaxmini`, `xrisc32`, and `jbc`. Export inspection must validate the final artifacts, not target labels.

513. **Real processor-module validation requires both ABI inspection and authenticated host execution:** `LPH` inspection proves archive extraction/export but cannot prove `ev_get_procmod`, typed analysis, or output routing. A deterministic runtime gate must install the module under a disposable `IDAUSR`, force its processor and raw loader, batch-render a fixture, and require the expected mnemonic. The local installed 9.4 runtime reaches license validation but the workstation HCLI session is expired; authenticated CI is the current executable host probe, while offline script tests validate command construction, redaction, and failure behavior.

514. **ABI materialization rejection tests must execute the production predicates without pulling SDK structures into tests:** Duplicating malformed-payload checks in a test would not prove the bridge uses them, while directly exposing `insn_t`/`switch_info_t` would violate opacity. A private SDK-free validation header can consume the existing public copied models; both the SDK materializer and an offline unit target call the same operand, instruction, and switch predicates. This covers positive size/icode bounds, eight unique operands, payload/data-type/register completeness, switch addresses/counts/sizes/shift/expression bounds, and unknown enum values.

515. **IDA 9.4 current function events and mnemonic-only output require explicit compatibility dispatch:** The first bridge routed `ev_func_bounds`, `ev_create_func_frame`, and `ev_get_frame_retsize`, but the pinned SDK deprecates those pointer-based events in favor of `ev_function_bounds`, `ev_create_function_frame`, and `ev_get_function_retsize`. A processor module must accept both shapes to preserve declared callbacks across current and compatibility hosts. Separately, `ev_out_insn` has a void/full-render contract: treating a successful `output_mnemonic_with_context` result as a complete instruction suppresses operands. The full formatter default must remain unhandled and the bridge must perform canonical mnemonic/operand fallback rendering.

516. **Normalized processor bitness must replace contradictory raw `PR_*` bits rather than OR with them:** The bridge initially copied `ProcessorInfo::flags` and then added the bits implied by `default_bitness`. A descriptor declaring 32-bit default behavior while carrying `Use64`/`DefaultSeg64` could therefore expose both 32-bit and 64-bit modes simultaneously. Clear all use/default-segment bitness bits first, then apply exactly one normalized 16/32/64 state; keep the remaining caller flags unchanged.

517. **An exported procmod descriptor can be inspected without initializing or licensing an IDA database:** After globally preloading the installed `libida`, the platform loader can load an example procmod and `ctypes` can resolve its exported `LPH` data symbol. The stable leading `processor_t` layout then exposes version, ID, flags, bit widths, names, assembler pointer, callback pointer, registers, segment indices, instruction range/table, and return code. This is a stronger gate than `nm`/`dumpbin` alone and remains independent of the authenticated batch-dispatch smoke.

518. **An exact release workflow pin does not prevent local consumers from silently selecting an older ambient SDK:** The Rust build helper discovered a neighboring 9.3 SDK when one verification command omitted `IDASDK`; compilation then failed only after reaching the new 9.4 event names. Because the public build now intentionally targets 9.4, top-level configuration must read `IDA_SDK_VERSION` from the resolved SDK and fail immediately unless it is exactly 940. The archive fallback and workflows continue to pin the requested immutable commit; the version gate prevents misleading mixed evidence outside those paths.

519. **A pre-analysed IDB can embed the creating host's license certificate and identity even when source text is clean:** Raw-byte/`strings` inspection of the tracked `simple_appcall_linux64.i64` found plaintext named-license metadata, employee email, product/add-on entitlement IDs, and owner references. Text-only path scans and package audits cannot detect this. Same-length replacement removed 48 repeated entitlement references and four emails without changing file length; historical text records were redacted. The sanitized fixture passes all 102 initialized-host Rust integration tests, and a new raw-byte gate reports zero prohibited material across 475 project-owned tracked/candidate files.

520. **A descriptor probe that stops at the instruction-table pointer cannot validate the declared return instruction:** IDA 9.4 stores `processor_t::icode_return` after `tbyte_size` and `real_width`. The first `ctypes` probe carried an expected return code but only checked that expectation against the instruction range, so it would accept an incorrect exported value. Model the complete 144-byte pinned 64-bit descriptor, assert the ABI size, read `icode_return`, and compare the actual field before accepting the descriptor.

521. **Present-tree privacy success does not remove sensitive metadata from reachable Git blobs:** A streaming scan of 3,213 unique blobs reachable from `origin/master` found 249 historical blobs across six historical paths with non-synthetic entitlement identifiers; 97 binary database blobs also retain an embedded email. No reachable identity-bearing home path remains after Phase 54. The repository tip must be committed, rewritten with fixed-width replacements in an isolated mirror, and re-scanned before any rewritten ref is pushed; cached objects, pull-request refs, and forks remain the separate F466 external retention boundary.

522. **Validating a switch descriptor does not validate callback-produced custom case vectors:** The bridge initially copied every `SwitchCase` returned with a positive callback status, including empty output, missing values, duplicate values, incomplete case totals, or `BadAddress` targets. Add one shared private predicate requiring a nonempty grouping, unique values, valid targets, and an exact total equal to `case_count`; reject zero-case/zero-entry switch descriptors and make the XRISC example fail rather than silently omit an unreadable table entry.

523. **Prepending IDA 9.4 does not make automatic runtime discovery release-exact:** After F511, integration/example discovery preferred 9.4 but still fell through to 9.3/9.2, Python fell through to 9.0, and Node did not search 9.4 at all before an unversioned development bundle. This contradicts the exact-940 SDK configure gate and can silently link an older runtime. All implicit discovery is now restricted to the standard 9.4 installation; an explicit `IDADIR` remains the caller-owned override.

524. **A tracked/candidate privacy gate must exclude workflow-owned acquisition roots:** Validation run 29470851825 successfully installed IDA 9.4 and passed the Linux procmod analyze/output smoke, then the repository scanner inspected the untracked proprietary installer downloaded under `ida-installer/` and rejected metadata inside that external artifact. `ida-installer/` and the nested `ida-sdk/` checkout are workflow inputs rather than repository candidates; root ignore rules exclude exactly those acquisition roots while the scanner continues byte-scanning every tracked and non-ignored untracked project file.

525. **A Unicode HCLI table regression must not inherit the Windows CP1252 pipe codec:** Replacement validation run 29471023737 installed IDA 9.4 successfully, then the selector's negative subprocess test failed before child launch because `text=True` encoded Rich's box-drawing delimiter through CP1252. The production selector now reads stdin bytes and decodes UTF-8 with replacement, while CLI tests set UTF-8 explicitly and cover both fail-closed and positive rich-table selection. License matching remains ASCII and never emits rejected input.

526. **A disposable `IDAUSR` must inherit the already accepted installation state:** On macOS run 29471023737, HCLI installed IDA 9.4 with `--accept-eula`, all three procmod exports/descriptors passed, and batch launch then reported that the license was not accepted because the smoke replaced the normal user root with an empty directory. The accepted state is stored in `ida.reg`, and installed named-license files may share that root. Seed only `ida.reg` and `*.hexlic` from the platform's normal IDA user directory into the temporary root before launch; the directory remains disposable and no source state is modified.

527. **IDA batch failures may exist only in the requested log file:** A local seeded-state reopening probe exited nonzero with empty stdout/stderr, and the smoke discarded the `-L` log on that path. Failure diagnostics must combine console streams with the bounded log tail and pass the result through path/license redaction before reporting. This separates host/license failures from procmod bridge failures without exposing identifiers.

528. **A non-null assembler pointer is not a valid `asm_t`:** The first local licensed procmod launch loaded `idaxmini` and selected its processor, then IDA rejected the materialized generic assembler because `asm_t::esccodes` was null. IDA 9.4 requires a nonempty set of characters that cannot appear unescaped in string/character literals. Materialize the quote delimiters as escape codes, provide the byte directive for string output, and make the loaded descriptor probe dereference the assembler prefix and reject a missing name or escape-code set.

529. **Canonical processor output cannot depend on a newly added nullable `outctx_t::out_mnemonic()` slot:** After assembler acceptance, a fresh exact-SDK build loaded `idaxmini`, completed analysis, and faulted at the indirect call inside `outctx_t::out_mnemonic()` while generating assembly; the host crash record showed that virtual slot was null. The bridge already owns typed mnemonic and operand output, so canonical fallback must render the processor's `OutputContext` tokens directly, use the descriptor mnemonic only when the optional mnemonic hook is unimplemented, and reserve the SDK operand helper for an unimplemented typed operand. The rebuilt bridge then completed real IDA 9.4 load, analysis, and assembly output with the expected mnemonic.

530. **A reachable-history privacy command is ineffective on a default depth-one CI checkout:** Scanning `--history-ref=HEAD` in a shallow checkout sees only the candidate commit and cannot attest the rewritten ancestry. The validation workflow must fetch full branch history before the common matrix runner executes both candidate-tree and reachable-history scans. This makes every configured validation host fail closed on reintroduced identity paths, canonical entitlement identifiers, or binary email metadata anywhere reachable from the tested branch.

531. **Windows may expose a symlink target in extended-length form even when it was created from a display-form path:** Validation run 29471746601 failed both Windows rows before configuration because the privacy reader regression compared `os.readlink()` bytes with `str(target)`. On the runner, the former correctly included the Windows extended-length prefix. The scanner must retain the raw link text and never follow the target; the portable assertion therefore compares against the platform `os.readlink()` result and independently proves the target contents were not returned.

532. **HCLI `Active` status alone does not prove the listed certificate expiration is installable:** Validation run 29472084052 passes Windows privacy, compilation, all three exports, and all three descriptors, then IDA rejects the installed first-priority named Ultimate certificate as expired. The supplied HCLI table's first such row has the anomalous expiration year 3025, followed by a plausible 2036 Ultimate row. Parse ISO expiration, reject malformed or post-2100 values, and prefer the longest plausible expiration within edition priority; this remains fail-closed and avoids the known anomalous certificate.

533. **A copied pre-analysed database can terminate the first exact-9.4 integration open after an IDB-format conversion pass:** The common CTest runner copied the tracked older `.i64` beside its isolated binary. The exact IDA 9.4 process exited during the first open, while a raw-only isolated copy passed; the same behavior affected an otherwise passing existing smoke target. The generic runner must not copy a release-specific sidecar implicitly. An explicit existing-database test may still select the sanitized `.i64`; ordinary runtime tests analyze the isolated raw fixture with the current release.

534. **The release-specific sidecar affected the complete native integration matrix, not only the new referent test:** A fresh exact-SDK/IDA 9.4 matrix passed every offline target but failed 19 of 20 database-backed C++ targets before their assertions when the common runner copied the older `.i64`. The existing smoke executable passes 267/267 checks when the same runner supplies only an isolated raw fixture. The common CTest boundary must therefore copy only the executable and let every runtime analyze its own disposable database; the sanitized pre-analysed IDB remains available only to consumers that explicitly require one.

535. **`Result<bool>` capability checks must distinguish transport success from the contained capability value:** Two native decompiler tests treated a successful `available()` result containing `false` as either mandatory success or truthy capability. On a valid IDA 9.4 host without a compatible loaded decompiler, this caused one false assertion and ten guaranteed decompile failures. Require a successful result first, then branch on its contained boolean; unavailable optional capability is explicit skipped coverage under decision 19.62, not a test failure or permission to call it.

536. **IDA 9.4 string-list enumeration requires extended output storage even through the legacy exported function on the tested arm64 runtime:** The exact SDK declares a 16-byte `string_info_t` and a separate `get_strlist_item_ex`, but the installed arm64 IDA 9.4 library does not export the latter. Passing only base-sized storage to `get_strlist_item` overwrote the immediately adjacent vector state and faulted at address `0x10`; passing the ABI-prefix-compatible `string_info_ex_t` storage eliminates the fault and passes the complete string/source contract. The wrapper retains the exported legacy call but supplies extended zero-initialized storage and copies a decompiler string when present.

537. **The complete IDA 9.4 undo subsystem is absent from IDAX despite being a small stable cross-domain mutation primitive:** The pinned `undo.hpp` exposes exactly five runtime-exported operations: one opaque byte-record checkpoint, copied undo/redo action labels, and undo/redo execution. The official SDK and IDAPython adapter define the checkpoint record as two packed strings (`action_name`, then display `label`); IDAX currently exposes none of these operations in C++, Node, Rust, or Python. Preserve the record encoding privately, reject embedded NUL before C-string conversion, copy labels into owned UTF-8 strings, represent absent labels and unavailable execution explicitly, and expose no SDK record bytes or `qstring`.

538. **An embedded-NUL C++ test must construct `std::string_view` with an explicit byte count:** Passing a string literal such as `"bad\0action"` directly to a `std::string_view` parameter selects the null-terminated constructor and presents only the prefix, so a correct embedded-NUL validator cannot observe the suffix. The first undo runtime probe therefore accepted both truncated values while every checkpoint/mutation/label/undo/redo state assertion passed. Constructing `std::string_view(pointer, length)` makes the negative probe cover the intended bytes; Rust `CString` and Python/Node owned-string conversions already retain their language-level lengths before the C++ boundary.

539. **A release pin can remain contradicted by user-facing runtime examples after build discovery is corrected:** The Rust binding README still named the standard IDA Professional 9.3 application in direct-execution linker examples even though configuration, workflows, package resolution, and current Python documentation require SDK/runtime 9.4. Update current setup examples to 9.4 and retain 9.3 only in explicitly historical validation evidence.

540. **The shared Node `ToString` helper truncates JavaScript strings at embedded NUL:** `Nan::Utf8String` retains an explicit byte length, but constructing `std::string(*text)` discards everything from the first NUL. The undo binding must construct its owned input with `text.length()` so the C++ validation boundary receives and rejects the actual JavaScript value; pure Node regressions cover both action and label inputs.

541. **The tag-only Node release matrix still selected a retired Intel macOS runner:** GitHub's current hosted-runner contract lists `macos-15-intel` and `macos-26-intel`; `macos-13` is no longer a recognized standard label, so a version tag would fail before checkout. Replace the row with `macos-15-intel`. Quoting the derived Node version in both addon workflows and writing the Windows LLVM path with `printf` also removes every current `actionlint`/ShellCheck finding.

542. **The opaque undo boundary is portable across every configured release consumer:** Implementation commit `1d947c4aad7aaf18841d8406771a70db2b81e993` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 on Linux, Windows, and macOS. The matrix covers native checkpoint/mutation/label/undo/redo/restoration plus compile and initialized-host parity for Node, Rust, and Python. Complete-log scanning finds zero unmasked canonical license identifiers.

543. **The complete IDA 9.4 analysis-problem subsystem is absent from IDAX and is distinct from generic error search:** The pinned `problems.hpp` exposes six runtime-exported operations over 16 semantic problem kinds: copied description lookup, remember with an optional message, ordered next-address lookup, removal, copied short/long kind names, and presence. IDAX exposes none of these operations in C++, Node, Rust, or Python; `search::next_error` calls the separate generic `find_error` facility. Validate kinds and addresses before dispatch, reject embedded NUL messages, preserve absent versus explicitly empty messages, return ordinary absence as optional values, and expose no `problist_id_t`, `qstring`, pointers, or storage records.

544. **The Python initialized-host test still selected the tracked pre-analysed sidecar despite the Phase 53 release-neutral fixture decision:** With the repository fixture path, IDA exits during database open before assertions; supplying the same raw binary from a directory without its companion passes the complete test, including the new problem round trip. The generic Python runtime test must copy only the raw fixture, matching the common native runner and Rust/Node isolation boundaries; an existing-database test must opt into a sidecar explicitly.

545. **The opaque analysis-problem boundary passes every configured release consumer:** Implementation commit `c3412cc74a7c2f76d74ed3a9c18cc0bebad6e39c` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 on Linux, Windows, and macOS. Every job installs IDA Professional 9.4 after selecting an eligible active named Ultimate license and checks out exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`; complete-log scanning finds zero unmasked canonical license identifiers.

546. **The complete architecture-independent exception-region subsystem is absent from IDAX:** The pinned `tryblks.hpp` exposes five runtime-exported operations for copied retrieval, ranged deletion, C++/SEH addition, system-EH region lookup, and semantic address membership. Its records represent fragmented protected and handler regions, calculated nesting, catch selectors, SEH filters/dispositions, and optional frame-relative metadata; no equivalent exists in any current IDAX language surface. Preserve those semantics in owned discriminated values and expose no native records, SDK vectors, raw flag integers, or pointers.

547. **A stored SEH try block is not necessarily a system-EH region:** An isolated `TB_SEH` add/list/membership round trip succeeds, including filter and handler classification, while `find_syseh()` returns `BADADDR` for the same protected address. The system-region query is an independent optional host classification; IDAX must not infer a positive result from the presence of ordinary SEH metadata.

548. **The opaque exception-region boundary passes every configured release consumer:** Implementation commit `35d3776ffeb3e6fc83de426f05b2d64f2e90cdfc` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 on Linux, Windows, and macOS. The matrix compiles and executes the five-operation C++/SEH surface plus Node, Rust, and Python parity against exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594` and IDA Professional 9.4. Complete-log scanning finds zero unmasked canonical license identifiers.

549. **The complete IDA 9.4 source-parser subsystem is absent from IDAX despite being runtime-exported and semantically distinct from generic declaration parsing:** The pinned `srclang.hpp` exposes nine operations for selection by parser name or source-language mask, copied selected-parser identity, parser command-line arguments, language-based and name-based source/file parsing, extended semantic parse flags, and parser option get/set. The tested IDA 9.4 runtime exports all nine, while existing `type::parse_declarations` covers only the built-in generic declaration parser. Preserve parser-not-found and unsupported-argument states explicitly, copy parser names/options, validate language masks/strings/packing, store parsed types in the current local type library, and expose no `srclang_t`, `til_t`, `qstring`, parser pointers, or raw `HTI_*` integers.

550. **Default-parser selection and parser configuration variables are related but non-identical runtime state:** On the tested IDA 9.4 arm64 host, selecting the parser supporting C and C++ yields copied name `clang`; selecting the default parser afterward can still report that explicit name even though the pinned header permits an empty name for default state. The registered `CLANG_ARGV` and `CLANG_APPLY_TINFO` options are readable, while configuration-only `CLANG_PARSE_STATIC_DECLS` is not exposed through `get_parser_option`. Preserve the host-returned optional string exactly, and treat parser-defined option discovery/rejection as runtime state rather than deriving keys from the configuration file.

551. **A C transport output pointer must be rejected before any stateful parser dispatch:** The initial Rust shim passed a parser call as an argument to a report-conversion helper that validated the output pointer. C++ evaluates the parser call before entering that helper, so a null report pointer still allowed source parsing and local-type mutation before returning validation failure. Each parse entry point now rejects null output before parser dispatch; the helper retains defense in depth. Extended parser modes that request both high-level and lowered prototypes are also rejected as contradictory, and Node rejects unrepresentable packing values before conversion to `size_t`.

552. **NAN optional arguments require an explicit common V8 handle type for GCC portability:** Apple Clang accepted a conditional expression whose present branch was `v8::Local<v8::Value>` and absent branch was `v8::Local<v8::Primitive>`, but GCC correctly reported ambiguous bidirectional conversions in all three parser calls using optional third arguments. Normalize omitted arguments through a helper with explicit `v8::Local<v8::Value>` return type before conversion; do not rely on conditional-expression type selection across V8 handle specializations.

553. **The opaque source-parser boundary passes every configured release consumer after cross-compiler correction:** Release commit `3d6b8b1b6b39e15b0ff32c1f0044070474990c5f` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 on Linux, Windows, and macOS. The matrix compiles and executes the nine-operation C++/Node/Rust/Python surface against exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594` and IDA Professional 9.4; complete-log scanning finds zero unmasked canonical license identifiers.

554. **The complete built-in database directory-tree subsystem is absent from IDAX despite full runtime availability:** The pinned `dirtree.hpp` supplies eight standard trees and exported operations for path/cursor resolution, copied names/attributes, enumeration and wildcard search, directory/item linking, rename/folding, natural/custom ordering, bulk move/remove, and traversal. IDAX currently exposes none of it, so plugins must use native `dirtree_t`, `dirspec_t`, inodes, cursors, entries, indexes, visitors, vectors, and raw `dterr_t` codes to organize Functions, Names, Imports, Local Types, breakpoints, bookmarks, or snippets. A standard-tree wrapper can preserve every user-facing operation through tree kind plus owned paths/snapshots and keep the native identity model entirely private.

555. **All eight standard trees materialize in a headless initialized IDA 9.4 session, and path-only mutation preserves native partial-result semantics:** The first exact-SDK idalib probe opens and resolves the root of Local Types, Functions, Names, Imports, IDA-place bookmarks, breakpoints, Local Types bookmarks, and snippets. On the Functions tree, copied cwd/absolute-path/entry/children/snapshot/search state, duplicate error mapping, directory rename, ordering/rank, two-source bulk move, recursive bulk remove, and missing-source indices all round-trip in a disposable database. A missing source is reported at its original input index without preventing valid siblings from moving or being removed; no inode, cursor, directory index, native vector, or pointer crosses the public boundary.

556. **Returning an SDK in-class static integral member through `std::expected` can create an unintended link dependency:** `direntry_t::ROOTIDX` is declared as `static const` in the pinned header without an exported out-of-class definition. Returning it directly as `Result<diridx_t>` caused the Rust-linked static archive to emit an undefined `direntry_t::ROOTIDX` reference even though the ordinary native target linked. Returning the semantic root value as an explicit `diridx_t(0)` remains pinned-header-equivalent and removes the ODR use. The corrected generated C/safe Rust boundary then passes all ownership conversions and the isolated directory round trip.

557. **Standard-tree prefix folding and item membership have path-visible but pointer-free round trips:** On the tested IDA 9.4 Functions tree, folding a single-child directory chain replaces the copied child name separator with the pinned ASCII group-separator byte (`0x1D`), while the result remains addressable as one directory entry. Unlinking an existing root item removes its path without deleting the underlying database object, and linking the same full item name into the current directory restores the identical path. The C++, Node, Rust, and Python initialized-host probes now cover both behaviors plus empty-directory removal, ordering, rank changes, and interleaved missing-source indices; no inode or cursor is required publicly.

558. **A partially allocated C aggregate must not advertise an element count before its backing pointer exists:** The first directory bulk-report converter assigned `affected_paths_count` and `failures_count` before `calloc`. On allocation failure, its aggregate destructor could iterate a nonzero count through a null pointer, replacing the intended internal error with undefined behavior. Counts are now published only after their zero-initialized arrays exist; every later string-allocation failure can safely invoke the aggregate destructor.

559. **An own-child source rejection proves compact native bulk indices can be remapped alongside pre-resolution failure and success:** Moving `[missing, parent, independent]` into `parent/child` yields a wrapper `NotFound` at original index 0, native `OwnChild` at original index 1, and a successful moved path for original index 2. The native error index refers to the compact two-cursor vector, so mapping through retained original indices is required. The same mixed report passes through C++, Node, safe Rust, and Python without exposing the rejected native cursor.

560. **The opaque standard directory-tree boundary passes every configured release consumer:** Implementation commit `dc9d3ac61bad6c82cfd8bd81bcdb6a3fb5a2ab21` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 on Linux, Windows, and macOS. Every job selects an eligible active named IDA product license, installs IDA Professional 9.4, and checks out exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`; complete-log scanning finds zero install failures and zero unmasked canonical license identifiers.

561. **The complete persistent plugin-configuration registry is missing from IDAX despite portable runtime exports:** Pinned `registry.hpp` exposes typed string, binary, 32-bit integer/boolean, existence/type, child/value enumeration, key/value/tree deletion, and string-list operations; all 14 underlying operations are exported by the tested IDA 9.4 runtime and generated IDAPython wraps the same family. IDAX exposes none in C++, Node, Rust, or Python. A scoped store can own a nonempty subkey and return copied values without exposing SDK strings/vectors, raw buffers, native value types, or platform-specific Windows-registry versus Unix-file storage. `set_registry_name` is a separate process-global root mutation with no getter/restoration contract and cannot be represented as scoped store state.

562. **`reg_update_strlist()` is globally suppressed by `IDA_NO_HISTORY`, even for plugin-owned keys:** In an initialized IDA 9.4 idalib session, `reg_write_strlist()` and `reg_read_strlist()` round-trip a disposable three-value plugin key, but `reg_update_strlist(key, "delta", 3, "beta", false)` returns void and leaves that list unchanged. Runtime disassembly confirms an early `qgetenv("IDA_NO_HISTORY")` return before any key dispatch. IDAX therefore implements checked add/remove/deduplicate/front-insert/trim semantics through copied read-modify-verified-write state, validates the native 1..1000 record range, and rejects contradictory add/remove requests instead of reporting a silent update as success.

563. **Pybind object subclasses should not be selected through a conditional expression when both convert to the other:** `py::bytes` and `py::none` both convert to `py::object`, but a direct `optional ? bytes : none` expression is ambiguous because each concrete wrapper also admits conversion toward the other branch. Return through explicit branches from a lambda declared as `py::object`; this preserves binary optional absence and compiles without relying on wrapper-conversion ranking.

564. **Native Node registration does not guarantee package-level destructured export parity:** The registry namespace was attached to the native addon and therefore passed direct native structural/runtime probes, while `lib/index.js` omitted the explicit `module.exports.registry` assignment and the namespace inventory omitted `registry`. The wrapper object was currently still reachable because `module.exports` initially aliases the complete native object, but that was an undocumented aliasing dependency and made the enumerated package contract incomplete. Explicit re-export plus namespace-inventory coverage closes the release-path gap.

565. **Optional Node return values require explicit V8 handle branches across release compilers:** The local compiler accepted five conditional expressions combining present `v8::Local<String|Value|Int32|Boolean>` handles with `Nan::Null()`'s `v8::Local<Primitive>`, but the implementation push failed both Linux/GCC and macOS/Apple-Clang Node builds because each specialized handle admits conversion toward the other branch. Explicit present/absent statements avoid conditional common-type inference while preserving string, binary, integer, boolean, and kind absence as JavaScript `null`.

566. **The opaque scoped persistent-registry boundary passes every configured release consumer after cross-compiler correction:** Corrective release commit `1489e775a1bb0f6d128e927cf4ca6147ccc69b85` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 on Linux, Windows, and macOS. The matrix compiles and executes C++/Node/generated-C/safe-Rust/Python registry parity against exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594` and IDA Professional 9.4. Boundary-aware complete-log scanning finds zero standalone canonical license identifiers, zero license-install failures, and zero reported identity-path occurrences.

567. **IDA 9.4's complete register-value tracker is absent from IDAX despite direct binary-analysis value:** Pinned `regfinder.hpp` exposes seven runtime operations for constant lookup, stack-relative lookup, rich register value state by number/name, nearest-of-two selection, and control-flow/data-reference cache invalidation. The tested IDA 9.4 runtime exports all seven and generated IDAPython mirrors them, while IDAX currently offers only decoded register names, debugger register metadata, and function register-variable annotations. A name-based owned result can preserve dead-end/aborted/unknown/constant/stack-delta state, multiple candidates, defining addresses/instruction codes, and PC/GOT/short-instruction attributes without exposing processor register numbers, `reg_finder_t`, `reg_value_info_t`, native vectors, or pointers.

568. **The exact 9.4 SDK's inline register-finder compatibility redirects do not match the shipped IDA Professional 9.4 library ABI:** At pinned SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`, `find_reg_value_info()` and `find_nearest_rvi()` inline-call `reg_finder94_*` symbols. The installed 9.4 `libida` exports the documented legacy `find_reg_value_info` and `find_nearest_rvi` names, but neither version-suffixed symbol; the initial nearest target consequently compiled and failed only at link. Define the header's own `IDA_REGFINDER_LEGACY_COMPAT` guard for this translation unit, redeclare the two adjacent official signatures under their shipped ABI names, and retain exact function-pointer assertions. This avoids runtime symbol probing and preserves the public operation on every release library using the documented legacy ABI.

569. **Register-tracker support is processor-module-specific, so the generic x86-64 integration fixture cannot prove positive state:** IDA Professional 9.4's x86-64 processor returns the documented unsupported result for constant, stack, rich, and nearest register-finder queries, while a minimal AArch64 ELF returns deterministic `x29 = 0`, `x0 = 42`, `SP − 32 B`, function-input/undefined, and nearest-of-two state. The integration harness now permits an explicit per-test fixture and selects a source-reproducible, debug-free AArch64 sample only for register tracking. Unsupported remains a structured public error and is not converted into empty optional state.

570. **Semantic subsumption is insufficient evidence that every audited runtime export is callable:** The first bridge implemented unique constants entirely from `find_regname_value_info()` and used that same named helper as the nearest capability probe, leaving `find_reg_value()` and `find_reg_value_info()` signature-checked but undispatched. Default-depth `constant_at()` now calls the dedicated constant export and truncates the full base-register result to the parsed alias width; explicit depths retain rich named tracking. `nearest_at()` now probes support through numbered rich tracking before calling nearest. All seven audited exports therefore participate in executable paths without exposing register numbers.

571. **AArch64 low-width alias truncation agrees between the dedicated and rich register-finder paths:** A fixture value with nonzero bits above bit 31 yields the complete value for default-depth `x0`, its low 32 bits for default-depth `w0`, and the same low 32 bits for explicit-depth rich `w0`. The result passes C++, Node, safe Rust, and Python initialized-host probes, directly validating the private 1/2/4/8-byte mask strategy for this processor family.

572. **A debug-free ELF can still carry an email-shaped toolchain provenance string:** LLD emitted a non-allocated `.comment` section containing its public source-control identifier, whose transport syntax matches the repository's binary-email privacy rule. Remove that section reproducibly with `llvm-objcopy` after linking; the loadable segments, symbols, and tracker instructions remain unchanged, and the privacy gate then remains fail-closed without special-casing public toolchain metadata.

573. **The AArch64 tracker retains compatible branch constants as multiple owned candidates:** At a two-path merge assigning `x2 = 0x11` or `x2 = 0x22`, exact IDA 9.4 returns semantic `Constant` with two distinct defining origins and values. The unique-constant convenience operation correctly returns absence at the same address. This directly validates the public candidate vector rather than inferring it from the native type.

574. **Closed native states must fail closed at both enum and tracker-state boundaries:** A cast C++ enum value outside `Added`/`Removed` previously fell through as removal, and a future unknown native tracker state accepted by the SDK's broad `is_unknown()` predicate could have collapsed to public `Undefined`. Exhaustive mutation switches now reject unrecognized values before cache dispatch, and only an actually empty native value maps to `Undefined`; every nonempty unclassified state returns `Unsupported`.

575. **Unpinned `setup-uv` latest-version resolution can reach an unauthenticated GitHub API bucket even though v5 declares a token default:** Phase 65 implementation runs 29541397249 and 29541397296 failed only in macOS jobs 87764215050 and 87764215005, whose public annotations report GitHub API rate-limit exhaustion during `Setup uv`. Exact v5 source inspection shows that `github-token` already defaults to the workflow token, while an omitted version still invokes latest-release resolution and can fall back to anonymous requests. Pinning explicit uv `0.11.28` eliminates that API lookup; immutable setup-uv `v8.3.2` supplies a Node 24 runtime, includes the selected release checksum, defaults to the Astral download mirror, and retains the explicit token for GitHub fallback acquisition.

576. **Manual complete-log privacy validation is unnecessarily coupled to a developer credential:** GitHub exposes public workflow/job/step conclusions but requires authenticated `actions:read` for complete run-log ZIPs, so a stale local CLI credential and unavailable browser backend can block the privacy gate after all repository tests pass. A separate default-branch `workflow_run` consumer receives a scoped token after each release workflow completes, can download the triggering run's immutable logs, and can fail closed on canonical license identifiers or non-runner home paths without exposing matched values.

577. **Fail-closed CI privacy diagnostics must be public without revealing the match:** Plain scanner stderr is retained inside an authenticated job log, while the unauthenticated job page exposes only a generic exit code. Emitting the already-sanitized entry ordinal/category as a GitHub workflow-command error makes classification public without disclosing matched bytes. A numeric tag-triggered replay path can then rescan an immutable run with a scoped workflow token even when the workstation API token is unavailable.

578. **GitHub's hosted Ubuntu image has a service-owned Linuxbrew home that is not developer identity:** The official `actions/runner-images` Ubuntu 24.04 inventory documents the preinstalled Linuxbrew location under its dedicated service account. Bindings log replay 29544138787 reports exactly two non-runner POSIX-home categories and no canonical license category, while Integrations and Validation audits pass. The privacy scanner must allow that exact CI toolchain identity without weakening rejection of arbitrary POSIX home users.

579. **A public-key diagnostic can classify a private log finding without publishing it:** Adding only the documented Linuxbrew service identity did not clear Bindings replay 29544303227, so the remaining prefix cannot be inferred safely. RSA-OAEP encryption of each deduplicated matched prefix to an ephemeral committed public key permits ciphertext-only public annotations and local decryption with an uncommitted private key. The diagnostic key/code must be removed immediately after classification.

580. **Hosted macOS runner homes can appear with normalized account-name case in complete logs:** Encrypted replay 29544535103 reduced entries 73 and 84 to one unique POSIX prefix; local OAEP decryption identified the uppercase `RUNNER` account component and no other value. Exact byte comparison therefore rejected a case-variant of the already allowed hosted macOS identity. Normalize the full candidate and the two exact hosted-runner keys only for membership; do not allow Linuxbrew or any other inferred service identity.

581. **Case-normalized exact runner membership clears the byte-identical Bindings archive without an extra allowlist identity:** Replay 29544791067 passes against unchanged source run 29542885658 after lowercasing only the full candidate and two hosted-runner keys. Removing Linuxbrew and all encrypted diagnostic artifacts leaves arbitrary POSIX/Windows homes and canonical license identifiers fail-closed.

582. **A subprocess-format test must control `GITHUB_ACTIONS` rather than inherit it:** Validation run 29544788074 failed all six rows before CMake because `ci_log_privacy_test.py` copied the CI parent environment into every scanner subprocess. Rejection cases intended to assert the local `error:` format therefore received the scanner's correct Actions `::error` annotation format. Removing `GITHUB_ACTIONS` from the child environment by default and setting it only for the explicit annotation case makes the two diagnostic modes deterministic without changing scanner policy.

583. **A delayed `workflow_run` audit can execute the scanner from an older source revision after a correction reaches the default branch:** Bindings run 29544300132 started at 00:16:02 UTC and ran 20 min 56 s, exactly mapping it to audit 29545265951 at 00:37:00 UTC. That audit checked out source revision `87eb596`, whose scanner still used byte-exact hosted-runner membership, and reported the known case-variant entries 89/100. Exact current-scanner replay 29545854051 passes the same archive, emits no encrypted finding, and proves there is no new allowlist identity.

584. **The opaque register-value tracking boundary and its privacy-acquisition corrections pass every configured release consumer:** Corrective release commit `c035111e9cf8ed8db5fcd3cdf03a7ab33712a540` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 across Linux, Windows, and macOS. Automatic complete-log audits 29545467762, 29545625755, and 29546203247 pass with only the two exact case-normalized hosted-runner home keys and no license exception. All replay tags and every ephemeral diagnostic artifact are removed before closure.

585. **Green GitHub-hosted jobs can conceal imminent JavaScript-action runtime failure and unrelated image state:** The Phase 65 release matrix passes only because GitHub forces four Node 20 actions onto Node 24, producing 19 warning annotations across the three workflows; `ilammy/msvc-dev-cmd` remains Node 20 at current v1.13.0. The Rust macOS job also invokes Homebrew only to obtain `libclang`, causing an untrusted hosted-image tap warning. Current checkout v7, setup-node v7, upload-artifact v7, and download-artifact v8 manifests declare Node 24; Xcode already supplies a compatible `libclang`; a local composite can export only the environment delta from `vcvarsall.bat` without a JavaScript action.

586. **An immutable-action scanner must accept both expanded and shorthand YAML step syntax:** The first negative regression used valid `- uses:` syntax while the initial inventory regex recognized only a line beginning directly with `uses:`. Repository workflows happened to use expanded named steps, so their inventory passed while shorthand could evade review. Match one optional list marker before `uses:` and retain exact action/count validation for both forms.

587. **`$LASTEXITCODE` is not guaranteed to exist before its first successful native-command status assignment under PowerShell strict mode:** All five completed Windows jobs from source commit `a6d8c8510b4af434be699a1d061c3dd6de77d01a` failed in the local MSVC composite with `The variable '$LASTEXITCODE' cannot be retrieved because it has not been set.` on the Windows 2025/Visual Studio 2026 hosted image. Capture `$?` immediately after `vswhere.exe` and `cmd.exe`, before any intervening pipeline, and use those initialized Boolean results plus output validation.

588. **The immutable Node 24 workflow boundary passes every configured release consumer without runtime or hosted-image warnings:** Corrective commit `245dd2bf4a425c9b9bbfaa9d6842540db49a37be` passes Integrations 3/3, Validation 6/6, and Bindings 9/9. All six Windows rows pass the local MSVC composite and downstream compiler work; Rust macOS passes with Xcode `libclang`. A complete-log sweep across all 18 jobs finds zero GitHub warning commands, Node 20 action-runtime warnings, Homebrew LLVM invocations, or untrusted-tap warnings, and all three automatic privacy audits pass.

589. **The complete address-bookmark behavior family is absent despite pinned runtime and tree support:** IDA 9.4 `moves.hpp` exports bookmark mark/get/get-description/set-description/find/size/erase and dirtree-identity operations over cloned `place_t` locations. The standard IDA-place bookmark tree is already wrapped only as organization metadata; IDAX cannot create, locate, describe, enumerate, update, or remove the underlying address bookmarks. An address-specific wrapper can construct `idaplace_t` privately, use null widget userdata for the shared EA-capable store, and expose only owned address/slot/description values.

590. **Bookmark size is an exclusive sparse-slot bound and get is slot indexed:** An isolated IDA 9.4 probe created the first bookmark at explicit slot 17. Native `bookmarks_t::size()` returned 18, `bookmarks_t::get(..., index=0, ...)` returned false, and `get(..., index=17, ...)` plus address lookup found the bookmark. Therefore size is one past the highest occupied slot, not cardinality, and false `get` results inside `[0, size)` are ordinary holes. Scan that bounded interval and retain successful reads.

591. **Native bookmark erase compacts the high-water sequence and does not preserve sparse marker identities:** With live bookmarks in slots 0, 17, and 19, erasing tail slot 19 removes it immediately, but one erase at occupied slot 17 only decrements the high-water bound across the empty slot above it and leaves slot 17 present; a second erase removes it. Likewise, one erase at slot 0 can leave that bookmark while compacting higher storage. The native implementation shifts higher supplemental-array indices and deletes the former tail, so direct deletion is not an identity-preserving operation when holes exist. Exact wrapper removal must snapshot all owned bookmarks, erase the native store strictly from its current tail to zero, rebuild every survivor at its original slot, verify the complete copied state, and attempt full-snapshot rollback on failure.

592. **The API-surface target is an object-only compile gate; its apparent `main` aggregator is never linked or executed:** Every defined check body, including registry and register tracking, was compiled and therefore retained declaration coverage even though dead `main` omitted those calls. The dead aggregator also expected 33 groups while listing 34. Phase 67 adds the bookmark probe, reconciles the dead list/count to 37 groups for 35 domains plus shared core/error, and adds a configure-time umbrella-to-probe inventory. CTest evidence remains compilation plus an echo command, not runtime execution.

593. **The Python manifest's native-function inventory must be invariant under standard whitespace formatting:** The first bookmark binding placed five `bookmark.def(` calls and their string names on separate lines. The extension compiled and runtime behavior passed, but the structural manifest reported those functions as absent because its regex required the public name on the same line. Restricting the grammar to the same domain receiver and literal identifier while accepting whitespace after `def(` restores exact 35-domain/898-symbol parity and remains fail-closed for unsupported registration forms.

594. **Binding runtime probes must select an unbookmarked address rather than deleting unknown baseline state:** The initial Node, Rust, and Python bookmark probes chose a fixed code address and called remove before snapshotting. Although workflow fixtures are disposable copies, that can erase pre-existing bookmark state used by another probe in the same initialized session. Selecting the first copied code address whose lookup is absent preserves baseline state; choosing slot 0 or 1 opposite the created slot also guarantees that the explicit same-address conflict probe remains valid at the capacity boundary.

595. **The opaque address-bookmark boundary passes every configured release consumer:** Implementation commit `151a4622294a71836c9063a6ac9fa569053d6d07` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 across Linux, Windows, and macOS. All 18 jobs install IDA Professional 9.4 with an eligible active named product license and use exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`. Complete-log audits 29551426405, 29551508095, and 29551931196 pass without a canonical license identifier or non-infrastructure identity-path finding.

596. **IDA 9.4's current navigation-history stack is entirely absent from IDAX:** Pinned `moves.hpp` defines 15 current `navstack_t` operations for lifecycle, persistent-stream initialization/transfer, current entries, stack push/navigation/seek/index/size/clear, and indexed reads/writes, plus two entry serialization helpers. The tested runtime exports all 17 normal symbols and generated IDAPython mirrors the stack class, while IDAX exposes only one-shot address/UI jumps and custom-viewer position helpers. An address-specific wrapper can own copied addresses and textual entry metadata while keeping place clones, renderer state, widget handles, netnode identity, native vectors, and serialized records private.

597. **`navstack_t::init` reports creation rather than success and treats the default entry as in/out state:** In an exact IDA 9.4 disposable database, first initialization of a unique stream returns true and creates a one-entry stack. Reinitializing the persisted stream after save/reopen returns false while loading all state successfully, and overwrites the supplied `navstack_entry_t` with stored state. Therefore false cannot map to an SDK error, and the wrapper must pass a private temporary so opening history never mutates the caller's owned default entry. The pinned `moves.hpp` stack definition also requires `netnode.hpp` to precede it when included outside the existing SDK bridge.

598. **Headless address navigation-history mutation and cursor movement are persistent without an interactive widget:** `set_current(..., true)` replaces the indexed current entry without increasing size; `stack_jump(..., false, entry)` appends and advances; back, forward, and seek move only the cursor and return the copied destination; indexed replacement preserves size; clear installs exactly one new tip. A three-entry stack at cursor 0 retained exact addresses, widget-ID strings, userdata strings, size, and index after save/reopen in idalib.

599. **Native current-location state is per textual widget channel and independent from the history stack:** `set_current(entry, false)` adds or replaces the entry's widget-ID current state without changing stack size/index. `set_current(entry, true)` performs the same channel update and also replaces the indexed stack entry, while other channel-current values remain present. `get_all_current` copies each channel state. With `LHF_HISTORY_DISABLED`, current-channel state remains available but clear/jump leave stack size zero; the semantic address-history wrapper therefore keeps history enabled rather than exposing a misleading empty-stack mode.

600. **`navstack_t::perform_move` transfers channel ownership and optionally retains its history; it is not a generic copy:** Moving channel alpha between two live streams always removes alpha current state and all alpha stack entries from the source and installs alpha current state in the destination. With `move_stack=false`, the removed alpha entries are discarded and the destination stack is unchanged. With `true`, they append to the destination in original order and its cursor advances to the appended current alpha entry. Other-channel entries/current state survive. A missing named source still returns true as a no-op, so a wrapper must require two acquired histories, prove source-channel presence, reject destination conflicts, and verify postconditions instead of treating the Boolean as success.

601. **Retained channel transfer can leave the native source cursor outside its shortened stack:** In the exact IDA 9.4 initialized-host lifecycle, moving alpha with history from an interleaved `other, alpha, other, alpha` source at its final alpha entry produced the correct two retained `other` entries but left `stack_index()` at the removed pre-transfer index 3 while `stack_size()` was 2. Native entry/current state transfer therefore succeeded while an ordinary post-transfer snapshot was structurally invalid. IDAX computes the nearest retained predecessor index from the pre-transfer snapshot, applies the exported seek helper with `apply_cur=false`, preserves the destination's pre-transfer cursor, and only then accepts exact entries/current maps. This normalization prevents cursor repair from overwriting independent per-channel current state.

602. **Opening an existing navigation stream inserts the supplied default as missing channel-current state:** An exact IDA 9.4 duplicate-live/reopen probe started with history entries `other, alpha` and current channels `alpha, beta`, transferred alpha, and observed the correct live source `other` entry plus beta-only current map. Initializing a second stack for that source with an `other` default retained the transferred state but inserted `other` into current; reopen preserved that insertion. Reusing the caller's original alpha default therefore resurrected alpha after a successful transfer whenever alpha had been removed. IDAX now initializes every native session with a reserved bootstrap channel, filters that channel from copied current state, rejects the reserved namespace from public channels, and explicitly installs the caller's initial tip/current only when the stream is newly created.

603. **The opaque address-navigation boundary passes every configured release consumer:** Implementation commit `c3977730f20b65adb30f6443b0f366fe0651a4ee` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 across Linux, Windows, and macOS. All 18 jobs install IDA Professional 9.4 with an eligible active named product license and use exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`. Complete-log audits 29555414138, 29555572331, and 29556133959 pass without a canonical license identifier or non-infrastructure identity-path finding.

604. **The GitHub cached-object/PR/fork privacy boundary remains externally retained:** A 2026-07-17 read-only re-audit still resolves the former pre-rewrite commit through GitHub's Git object API, advertises both closed pull-request head refs, and reports all four independent forks. P54.3 therefore remains correctly unchecked and non-active: branch push authority cannot garbage-collect GitHub caches, delete server PR refs, or rewrite fork-owner branches.

605. **The complete current segment-register range family is absent from IDAX:** Exact pinned IDA 9.4 `segregs.hpp` exports 12 current operations for value/default lookup, provenance-tagged range lookup/enumeration/indexing, split/delete, next-code containment, default data-segment seeding, and range copying. The exact arm64 library exports all 12 symbols. IDAX exposes only two numeric-index default setters in `ida::segment`, both implemented through the deprecated pointer-based SDK entry point, and exposes no query, range, semantic register identity, or binding model. The processor descriptor already defines the segment-register interval, names, width, and virtual code/data roles needed for an opaque name-based boundary.

606. **Segment-register mutation requires both processor acceptance and exact readback:** A disposable cross-minor proxy probe compiled against exact SDK 9.4 and executed only stable range exports on IDA 9.3. It enumerated x86-64 segment-register names/roles and showed that a user-tagged split of `es` at an instruction creates an exact new half-open range, increments the range inventory, and is exactly reversed by deleting that range start. The same requested split on the virtual code register was rejected. Range names therefore establish identity but not processor acceptance; every Boolean mutation must propagate rejection and verify the requested postcondition. The proxy runtime does not export the 9.4-only `set_default_sreg_value_ea` or current segment-info API, so no default-state or exact-version conclusion is drawn from it.

607. **Skip-native Rust builds cannot serve as the source for regenerated FFI declarations:** The skip-native path copies the checked-in `bindings.rs` into its output directory. Copying that output back after adding segment-register declarations initially preserved the new segment family but silently removed the newer bookmark/navigation declarations that were absent from the stale checked-in input used by that build cache. The authoritative regenerated file must come from a native bindgen execution over the complete current shim header, then be compared for retention of established domain types/functions before replacement. The exact native output retains bookmark/navigation and adds 173 segment-register declaration lines.

608. **The safe Rust crates form a lockstep publish pair:** Verifying an unreleased `idax` package resolves the registry's existing `idax-sys` version, whose private shim can be older than current master headers, and fails at that mismatched C++ boundary. Archive creation and content inspection remain valid with `--no-verify`; release verification requires packaging/publishing the matching `idax-sys` first, then verifying/publishing `idax`. This is release sequencing, not evidence that a stale registry shim is compatible with current source.

609. **The raw segment-register C shim must validate every output pointer independently of safe Rust:** The safe `idax` layer always supplies valid storage, but `idax-sys` exposes the C ABI directly. Initial query adapters dereferenced descriptor/range/value/count/presence outputs before checking them, so a malformed raw caller could crash instead of receiving a validation error. All new query entry points now reject null outputs before SDK access and initialize pointer/count pairs to an empty state before fallible work.

610. **The opaque segment-register boundary passes every configured release consumer:** Implementation commit `de7d708ad34a8abaccf827721e635e1a4ced4c66` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 across Linux, Windows, and macOS. Every row uses exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`; licensed rows install IDA Professional 9.4 through an eligible active named product license. Complete-log audits 29559500512, 29559649571, and 29560318570 pass without a canonical license identifier or non-infrastructure identity-path finding.

611. **The complete current operand offset/reference-semantics family is absent from IDAX:** Exact pinned IDA 9.4 `offset.hpp` plus `nalt.hpp` expose standard/custom reference descriptors, default-type selection, rich apply/query/remove, tagged expression rendering, offset/base/target calculations, candidate helpers, and reference-aware data-xref creation. The installed runtime proxy exports every audited stable operation. IDAX only dispatches `op_plain_offset` through `instruction::set_operand_offset`, exposing no readback, reference kind/options, outer operand, target/base/delta, calculation, rendering, removal, or reference creation. A dedicated opaque domain can retain semantic reference values while privately resolving all native IDs, flags, sentinels, instruction records, and color tags.

612. **Offset mutation success is not a sufficient semantic postcondition, and metadata deletion is not representation removal:** A disposable pinned-header/local-runtime proxy round-tripped all ten standard reference encodings; full-width kinds accepted absent targets while low/high-part kinds required explicit targets, and each of the nine behavioral flags was observable. Self-relative input normalized the stored base to the operand address. On an x86 operand without an outer displacement, `op_offset_ex(...|OPND_OUTER)` returned true but `get_refinfo` found no outer metadata. `del_refinfo` removed metadata but left `is_off` true until `clr_op_type`; both stored and explicit renderers produced the same tagged complex expression; and `add_refinfo_dref` produced the calculated offset xref. Safe public mutation must preflight outer capability, verify exact normalized readback, and restore prior state on any partial failure.

613. **Absent reference metadata does not prove an operand is available for offset mutation:** The first safe-Rust initialized-host probe selected a numeric operand whose rich reference query returned empty, but `apply_reference` correctly rejected it because the operand retained a non-offset display representation. Runtime probes that need a disposable reference location must also clear or independently exclude competing operand representations; production mutation must retain the conflict instead of overwriting user/analysis state.

614. **`calc_basevalue(target, base)` is not a documented subtraction identity:** The pinned SDK documents it only as calculating the value of the reference base. A cross-minor initialized-host probe returned `0` for target `8`, base `0`, falsifying the candidate assertion that the result must equal `target - base`; the earlier C++ pass used a fixture value for which that equality was non-discriminating. IDAX therefore preserves the SDK-defined optional result without assigning stronger arithmetic semantics.

615. **A native false return is part of the offset apply rollback boundary:** Self-red-team found that the first implementation restored prior state after mismatched successful readback but returned immediately when `op_offset_ex` itself returned false. The SDK Boolean cannot prove that no representation or supplemental state changed before rejection. The rejection path now invokes the same prior-reference restoration/empty-state clearing and distinguishes rollback failure from ordinary SDK rejection.

616. **JavaScript numeric validation must precede conversion to an unsigned native operand index:** The first Node offset parser accepted any finite, integral, nonnegative `Number` and converted it to `std::size_t`, so an out-of-range value such as `1e100` reached a C++ floating-to-integer conversion before the core `[0, 8)` gate. The binding now rejects values above JavaScript's maximum safe integer and regression-tests the malformed boundary before conversion.

617. **An offset runtime probe must fail when its disposable operand cannot be selected:** The first Node and Rust probes returned successfully and the Python probe skipped its body when no unrepresented numeric operand was found. This could turn fixture drift into false-positive exact-runtime evidence. All three binding probes now assert candidate existence before mutation; the C++ lifecycle already failed closed.

618. **Raw offset FFI scalar outputs must be initialized before fallible dispatch:** The first C shim validated optional-address, removal, and created-xref output pointers but left caller-provided scalar storage unchanged when the core returned an error. Direct `idax-sys` callers could therefore mistake stale values for outputs if they ignored the status. The shim now zeros presence/value, removal, and xref target outputs before any fallible call.

619. **The minimal Node test harness does not implement Jest's negated matcher chain:** The initial fail-closed candidate assertion used `expect(candidate).not.toBeNull()`, but this repository's local harness exposes direct matchers only. The assertion now compares the null predicate to `false`, ensuring the lifecycle fails for missing candidates without depending on an unsupported test API.

620. **The opaque operand offset/reference boundary passes every configured release consumer:** Implementation commit `fafcbaffe0f859b9f3e1835c0f76126486532a18` passes Integrations 3/3, Validation 6/6, and Bindings 9/9 across Linux, Windows, and macOS. Every row uses exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594`; licensed rows install IDA Professional 9.4 through the eligible active named-product selector. Complete-log privacy audits 29564829739, 29565022137, and 29565920823 pass across 294 entries and 12,611,190 uncompressed bytes.

621. **IDA 9.4's IDC value and script-execution family is absent from IDAX:** Exact pinned `expr.hpp` defines owned IDC values with conversion, object attributes, slicing, rendering, global-variable/reference helpers, plus header-path resolution, system-script execution, numeric/general/IDC-only expression evaluation, file/text/snippet compilation, named calls, file execution, and IDC snippet evaluation. The exact 9.4 arm64 import library exports every audited non-inline operation, and generated IDAPython wraps the value/evaluate/compile subset while deliberately omitting callback-heavy registration. IDAX currently uses `idc_value_t` only inside the private debugger Appcall bridge and has no public value or script namespace in C++, Node, Rust, or Python. The execution subset can be closed with owned semantic values while retaining native objects/functions/references opaquely; external IDC functions and third-party language installation require separate host-retained callback lifecycle phases.

622. **IDC typed access and SDK coercion require separate public operations:** A pinned-9.4-header/stable-runtime proxy passes integer/string evaluation, default-object attributes, shallow aliasing, deep-copy isolation, half-open string slicing/replacement, text compilation, named invocation, snippet evaluation, syntax/missing-function/runtime failures, exception-object retention, global references, rendering, and missing-file resolution. A falsey integer result still reports native success. Conversely, `idcv_int64("not-a-number")` reports success and coerces to integer zero, falsifying any contract that treats coercion as strict parsing. The wrapper must provide exact kind-checked accessors separately from explicitly named SDK coercions, preserve the result object on execution failure, and never infer success from result truthiness.

623. **IDC value strings and IDC source/name inputs require different FFI transports:** An IDC `Value` string is length-bearing and legitimately preserves embedded NUL bytes, while expression text, source, paths, attribute/global/function names, and resolver identifiers reach NUL-terminated SDK entry points and must reject embedded NUL. The Rust C ABI therefore transports value strings as pointer-plus-length byte arrays in both directions, but retains validated C strings for interpreter inputs. Execution reports also transfer an owned value even when the native Boolean outcome is false so exception objects are not collapsed into transport failure.

624. **Node optional booleans must be validated before native dispatch:** The first script binding converted `useHandler` and `complainIfMissing` with JavaScript truthiness even though its declarations accept only `boolean`. This admitted numbers and strings into behavior-selecting SDK flags. The shared parser now accepts only omitted/`undefined` or actual Boolean values, and structural tests cover all three affected calls.

625. **Raw script C ABI outputs must fail closed independently of safe Rust:** The initial script shim rejected null outputs but several scalar, handle, string, array, and result outputs retained caller-provided storage when validation or core execution failed. Direct `idax-sys` consumers could consume stale state after a nonzero status. Every new output is now initialized to zero/null/empty before fallible work, and an initialized-host raw regression seeds a nonzero integer output, forces an exact-kind failure, and requires zero.

626. **A Python class method named `object` shadows the builtin type inside its stub scope:** The script `Value.object()` factory caused the unqualified `object` tokens in `__deepcopy__`'s memo annotation to resolve as that method, not `builtins.object`, under strict mypy. Qualifying the builtin preserves the factory name and exact memo type without weakening the annotation.

627. **Global assignment must copy before mutating interpreter state:** The first `set_global` implementation created or selected the native global before copying into it, so a copy failure could leave a newly created empty global or partially replace an existing one. Copying into a private temporary first and then using non-failing native swap makes existing-global assignment atomic and delays creation until the value is ready. Empty `std::string_view` construction also supplies a non-null zero-length buffer to the native value API.

628. **Default ownership transfer left an opaque IDC value observably invalid:** The first `Value` move operations default-moved its private `std::unique_ptr`, leaving the source with null storage even though the public class documented no restricted moved-from state. Copying or querying that source could dereference null. A null private pointer now denotes a read-only logical integer-zero state, while mutable access materializes ordinary private storage only when required. Move construction/assignment remain constant-time and `noexcept`; the initialized-host lifecycle exercises query and copy of both moved-from states.

629. **Node required an explicit shallow-copy operation to reach value-model parity:** C++ copy construction, Rust `Clone`, and Python `copy.copy` preserve native shallow IDC object semantics, but JavaScript has no language-level copy constructor and the first Node surface exposed only `deepCopy()`. `Value.copy()` now returns an independent wrapper containing the ordinary native value copy; an initialized-host regression proves that copied objects share attribute state while `deepCopy()` isolates it.

630. **V8 optional-value ternaries are cross-compiler ambiguous:** The first Node script binding selected an argument or `Nan::Undefined()` with `?:`, mixing `v8::Local<v8::Value>` and `v8::Local<v8::Primitive>`; it also selected a string or null return the same way. The local newer Apple Clang accepted these conversions, while hosted Apple Clang, GCC, and MSVC rejected the sites because each local-handle type converts to the other. The binding now routes absent inputs through an explicitly typed `OptionalArgument` helper and branches explicitly for string-versus-null output, matching the established parser-binding repair.

631. **The opaque caller-owned IDC value and synchronous script family passes every configured release consumer:** Implementation commit `bbcb34511947f53b443fd004cf0ae93ea16fb388` plus cross-compiler correction `a9c8bd0cd2264603a8644b99cedfb3a596c8090a` pass Integrations 3/3, Validation 6/6, and Bindings 9/9 across Linux, Windows, and macOS. All 18 jobs use exact SDK commit `6929db6868a524496eb66e76e4ec6c9d720a0594` and IDA Professional 9.4. Complete-log audits 29573520110, 29573734060, and 29574575456 pass across 294 entries and 12,790,604 uncompressed bytes.
