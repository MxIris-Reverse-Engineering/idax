# SDK Domain Coverage Matrix

Date: 2026-02-13 (initial Phase 10 baseline)

This document tracks wrapper parity against the IDA SDK by two axes:

1. Public `idax` domain namespaces (`ida::segment`, `ida::function`, ...)
2. Major SDK capability families (segment management, decompiler workflows, debugger control, ...)

Status values:

- `covered`: high-confidence parity for the intended family scope, with supporting tests/docs.
- `partial`: meaningful support exists, but migration still requires additional APIs or behavioral hardening.
- `missing`: no intentional wrapper surface yet.

Tracking rules:

- Every row must include concrete symbol references.
- Every `partial`/`missing` row must define closure criteria and required evidence.
- Any row status changes must also update `.agents/roadmap.md`, `.agents/progress_ledger.md`, and findings/decision logs as applicable.

## A) Domain Coverage Matrix (one row per public domain)

| Domain | Status | Concrete idax symbol references | Primary SDK families | Closure criteria (`partial`/`missing`) | Required evidence |
|---|---|---|---|---|---|
| `ida::error` | `covered` | `ida::Error`, `ida::ErrorCategory`, `ida::Result<T>`, `ida::Status`, `ida::ok()` | Cross-cutting error transport | Maintain model invariants and category usage for all new APIs | `tests/unit/idax_unit_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::core` | `partial` | `ida::OperationOptions`, `ida::RangeOptions`, `ida::WaitOptions` | Cross-cutting option semantics | Intentional abstraction (P10.9.a): keep option surface compact and domain-agnostic instead of mirroring SDK flag breadth; expand only when concrete migration pressure appears | Unit tests + docs policy notes (`namespace_topology`, matrix intent notes) |
| `ida::diagnostics` | `partial` | `ida::diagnostics::log`, `set_log_level`, `assert_invariant`, `performance_counters` | Cross-cutting diagnostics | Intentional abstraction (P10.9.a): keep diagnostics lightweight (logging/assert/counters) rather than exposing SDK-internal telemetry channels | Unit coverage + concurrency-safe counter validation |
| `ida::address` | `covered` | `item_start`, `item_end`, `item_size`, `next_head`, `prev_head`, `next_defined`, `prev_defined`, `find_first`, `find_next`, `items`, `code_items`, `data_items`, `unknown_bytes` | Address flags/navigation (`ida.hpp`, `bytes.hpp`) | Closed in P10.2 (predicate ranges + discoverability aliases) | `smoke_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::data` | `covered` | `read_*`, `write_*`, `patch_*`, `revert_*`, `original_*`, fixed and processor-aware `define_*`, owned custom-data lifecycle, `StringListOptions`/`StringLiteral`, `string_list_options`, `configure_string_list`, `rebuild_string_list`, `clear_string_list`, `string_literals`, `define_string`, `define_struct`, `undefine`, `read_typed`/`write_typed`, `find_binary_pattern` | Byte IO, item definition, custom data registry, string-list cache (`bytes.hpp`, `strlist.hpp`, processor/assembler metadata) | P29-P31 close checked definitions and custom registries; P35 closes explicit global string-list configuration/cache enumeration with copied SDK-independent values | `data_mutation_safety_test`, `string_source_metadata_test`, Node/Rust real-IDA lifecycle and string-list cases, compile parity |
| `ida::database` | `covered` | `init` (+ `RuntimeOptions`/`PluginLoadPolicy`), `open` (bool + `OpenMode` + `LoadIntent`), `open_binary`, `open_non_binary`, `save`, `close`, `file_to_database`, `memory_to_database`, `address_bounds`, `address_span`, `input_file_path`, `idb_path`, `file_type_name`, `loader_format_name`, `compiler_info`, `import_modules`, `processor_id`, `processor_id_from_raw`, `processor`, `ProcessorProfile`/`processor_profile`, `processor_name`, `address_bitness`, `set_address_bitness`, `is_big_endian`, `abi_name`, `snapshots`, `set_snapshot_description` | Database lifecycle + loading (`idalib.hpp`, `loader.hpp`) | Closed in P10.2 with later headless policy/diagnostic metadata additions; P33 adds raw-ID-preserving processor normalization and correct verified-public typed boundaries for future/third-party modules | `smoke_test`, Node/Rust real-IDA processor-profile equivalence, `tests/unit/api_surface_parity_test.cpp` |
| `ida::segment` | `covered` | Segment CRUD/properties/traversal plus named segment-register discovery, effective/default optional values, copied provenance ranges, enumeration/indexing, split/delete, named/all/data defaults, next-code containment, and range copying; legacy numeric default overloads remain source-compatible | Segment lifecycle and complete current segment-register family (`segment.hpp`, `segregs.hpp`) | Processor register declaration, emulator event ordering, and selector creation policy remain processor-authoring concerns; no ordinal/sentinel/native-range escape hatch is exposed by the semantic state family | `segment_function_edge_cases_test`, `segment_register_roundtrip_test`, C++ API parity, Node/Rust/Python initialized-host and compile/manifest checks |
| `ida::function` | `covered` | `create`, `remove`, `chunks`, `tail_chunks`, `frame`, `sp_delta_at`, `define_stack_variable`, `set_prototype`, `apply_decl`, `declaration`, `update`, `reanalyze`, `item_addresses`, `code_addresses`, `frame_variable_by_name`, `frame_variable_by_offset`, `register_variables`, register-variable mutation APIs | Function/frame/chunks (`funcs.hpp`, `frame.hpp`, `typeinf.hpp`) | Closed in P10.3 (explicit update/reanalysis intent, address iteration helpers, frame/register ergonomics); P22 adds prototype application/export helpers | `segment_function_edge_cases_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::instruction` | `covered` | `decode`, `create`, `text`, operand metadata (`byte_width`, `register_name`, `register_category`, `is_read`, `is_written`), `set_operand_*`, `set_operand_format`, `set_operand_enum`, `operand_enum`, `set_operand_struct_offset`, `set_operand_based_struct_offset`, `ensure_operand_struct_member_offset`, `operand_struct_offset_path`, `operand_struct_offset_path_names`, `operand_text`, `get_forced_operand`, `code_refs_from`, `is_call`, `is_return`, `is_jump`, `is_conditional_jump` | Decode/output/operand representation (`ua.hpp`, `bytes.hpp`, `lines.hpp`) | Closed in P10.3 with post-closure struct-offset/access-mode additions; Phase 34 preserves read/write metadata; Phase 36 adds opaque named enum apply/readback; Phase 44 removes raw native path identities and adds copied exact root/member-name apply/readback | `instruction_decode_behavior_test`, `operand_and_text_test`, `operand_function_torture_test`, C++ compile parity, Node/Rust real-IDA evidence |
| `ida::name` | `covered` | `set`, `force_set`, `remove`, `get`, filtered `all`/`all_user_defined` inventories, address-based and arbitrary-symbol `demangled`, `resolve`, origin/property queries, `is_valid_identifier`, `sanitize_identifier` | Symbol naming/demangle (`name.hpp`, `demangle.hpp`) | Closed in P10.4; arbitrary in-memory demangling added in P24; P35 preserves the full filtered inventory through safe Rust | `name_comment_xref_search_test`, Node/Rust binding tests, `tests/unit/api_surface_parity_test.cpp` |
| `ida::xref` | `covered` | `add_code`, `add_data`, `remove_code`, `refs_from`/`refs_to` (+ `ReferenceType` filters), `ReferenceRange` + `*_range` APIs, typed predicates (`is_call`, `is_jump`, `is_data_*`) | Cross-reference mutation/enumeration (`xref.hpp`) | Closed in P10.4 (range-style traversal + richer typed filter utilities) | `name_comment_xref_search_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::offset` | `covered` | Live standard/custom descriptors, default type, copied rich query, verified apply/remove, stored/explicit expression rendering, OFF32 candidate and target/base/base-value calculations, probable/exact base lookup, and operand-aware data-xref creation | Operand offset/reference semantics (`offset.hpp`, `nalt.hpp`) | Phase 70 replaces native reference IDs/flags/records, operand encodings, sentinels, tagged strings, and instruction containers with owned semantic values; outer mutation requires processor evidence and all mutation verifies normalized copied post-state | `offset_reference_roundtrip_test`, C++ API parity, Node/Rust/Python initialized-host and manifest/declaration checks |
| `ida::script` | `covered` | `Value`, exact/coercing access, object attributes/slices, structured evaluation/compilation/calls, globals/references, include/file/script operations | IDC values and synchronous script execution (`expr.hpp`) | Phase 71 closes the caller-owned synchronous family after exact IDA 9.4 multi-host lifetime/error evidence without exposing native value records or callback pointers; external-function and external-language registries remain separate lifecycle families | `script_roundtrip_test`, C++ API parity, Node/Rust/Python initialized-host and manifest/declaration checks, exact release matrix |
| `ida::comment` | `covered` | `get`, `set`, deterministic newline `append`, `remove`, `set_anterior_lines`, `set_posterior_lines`, indexed helpers (`set_anterior`, `set_posterior`, `remove_anterior_line`, `remove_posterior_line`), `render` | Regular + extra-line comments (`lines.hpp`) | Closed in P10.4; append storage asymmetry hardened in P27 | `name_comment_xref_search_test`, `type_data_comment_torture_test`, Node/Rust integration |
| `ida::type` | `covered` | `TypeInfo::from_declaration`, `parse_declarations`, declaration renderers (`render_named_declarations`, `render_ordinal_declarations`, `declarations_for_ordinals`), `render_type_graph`, `function_type`, `enum_type`, `TypeInfo::pointer_details`, `TypeInfo::with_shifted_parent`, `TypeInfo::is_forward_declaration`, `TypeInfo::forward_declaration_kind`, `TypeInfo::replace_forward_declaration`, `TypeInfo::member_references`, `TypeInfo::ensure_member_reference`, `TypeInfo::with_function_argument_type`, `TypeInfo::with_function_argument_name`, `TypeInfo::with_function_return_type`, `TypeInfo::set_udt_semantics`, `TypeInfo::kind`, `name`, `declaration`, `function_return_type`, `function_argument_types`, `function_details`, `calling_convention`, `is_variadic_function`, `enum_members`, `enum_details`, `udt_details`, rich `Member` layout metadata, `create_struct`, `add_member`, `save_as`, `retrieve`, `apply_named_type`, `import_type`, `ensure_named_type` | Type system + local TIL and member-reference identity (`typeinf.hpp`, `xref.hpp`) | Closed in P10.4 for core type construction/introspection/application and named-type bootstrap; P22.10/P22.R2 add bulk import/rendering; P23 adds opaque rich metadata; Phases 36, 38, and 39 add immutable indexed prototype edits; Phase 40 adds metadata-preserving C++ object/vftable UDT semantics; Phase 41 adds copied exact shifted-parent/delta metadata; Phase 42 adds local forward state and ordinal-preserving copied-definition replacement; Phase 43 adds opaque persistent exact-member reference ensure/readback | `type_roundtrip_test`, `tests/unit/api_surface_parity_test.cpp`, Node/Rust real-IDA metadata evidence |
| `ida::entry` | `covered` | `count`, `by_index`, `by_ordinal`, `add`, `rename`, `forwarder`, `set_forwarder`, `clear_forwarder` | Entry/ordinal management (`entry.hpp`) | Closed in P10.4 (forwarder management parity helpers) | `smoke_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::fixup` | `covered` | `at`, `set`, `remove`, `first/next/prev`, `in_range`, descriptor (`flags`/`base`/`target`), signed types (`Off8Signed`/`Off16Signed`/`Off32Signed`), `register_custom`, `find_custom`, `unregister_custom` | Relocations/fixups (`fixup.hpp`) | Closed in P10.4 (descriptor fidelity + range traversal helpers) | `fixup_relocation_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::search` | `covered` | `text` (+ `TextOptions`), `immediate` (+ `ImmediateOptions`), `binary_pattern` (+ `BinaryPatternOptions`), `next_code`, `next_data`, `next_unknown`, `next_defined`, `next_error` | Text/immediate/binary find (`search.hpp`) | Closed in P10.5 (direction/options parity + `next_error`/`next_defined` helpers) | `name_comment_xref_search_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::analysis` | `covered` | `is_enabled`, `set_enabled`, `is_idle`, `wait`, `wait_range`, `schedule`, `schedule_range`, `schedule_code`, `schedule_function`, `schedule_reanalysis`, `schedule_reanalysis_range`, `cancel`, `revert_decisions` | Auto-analysis queue control (`auto.hpp`) | Closed in P10.5 (explicit schedule-intent + rollback/revert wrappers) | `name_comment_xref_search_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::bookmark` | `covered` | `Bookmark`, `MaxSlots`, `all`, `at`, `at_slot`, `set`, `remove`, `remove_slot` | Address bookmarks (`moves.hpp`) | Phase 67 copies address/slot/description state, chooses the lowest free slot deterministically, validates the pinned 1024-slot capacity, and preserves every surviving slot during removal without exposing places, location history, renderers, widgets, or directory-tree identities | `bookmark_roundtrip_test`, C++/Node/Rust/Python initialized-host tests, generated-ABI and compile-only parity |
| `ida::navigation` | `covered` | `Entry`, `History::open`, stream identity/creation state, entries/size/index/current snapshots, channel-current queries and updates, `push`, `seek`, `back`, `forward`, `replace`, `clear`, `transfer_channel_to` | Persistent address navigation stacks (`moves.hpp`, `navstack_t`) | Phase 68 replaces native places, renderer/widget identities, netnode stream keys, and mutable native containers with copied semantic entries and a persistent logical handle; a filtered private bootstrap channel prevents native stack reacquisition from resurrecting a transferred caller channel | `navigation_roundtrip_test`, C++/Node/Rust/Python initialized-host tests, generated-ABI and compile-only parity |
| `ida::problem` | `covered` | `Kind`, `description`, `remember`, `next`, `remove`, `name`, `contains` | Analysis-problem lists (`problems.hpp`) | Phase 60 closes all six pinned operations with validated semantic kinds and copied optional values | `problem_roundtrip_test`, C++/Node/Rust/Python signature and initialized-host tests, compile-only parity |
| `ida::exception` | `covered` | `HandlerMetadata`, `CatchSelector`, `CatchHandler`, `SehHandler`, `BlockDefinition`, `Block`, `Location`, `list`, `remove`, `add`, `system_region_start`, `contains` | Architecture-independent exception regions (`tryblks.hpp`) | Phase 61 keeps native records, selector sentinels, masks, and host containers private while preserving fragmented C++/SEH metadata | `exception_roundtrip_test`, C++/Node/Rust/Python initialized-host tests, compile-only parity |
| `ida::parser` | `covered` | `Language`, `InputKind`, `ParseOptions`, `ParseReport`, `select`, `select_for`, `selected_name`, `set_arguments`, `parse_for`, `parse_with`, `parse_with_options`, `option`, `set_option` | Third-party source parsers (`srclang.hpp`) | Phase 62 closes all nine pinned operations while keeping `srclang_t`, `til_t`, SDK strings, and raw `HTI_*` flags private | `parser_roundtrip_test`, C++/Node/Rust/Python initialized-host tests, compile-only parity |
| `ida::directory` | `covered` | `Kind`, `EntryKind`, `OperationError`, `Entry`, `BulkFailure`, `BulkReport`, `Tree::open`, path/enumeration/search, directory/item mutation, ordering/rank, `move`, `remove` | Standard database organization trees (`dirtree.hpp`) | Phase 63 covers all eight built-in trees while keeping native trees, directory specifications, cursors, entries, inodes, indexes, visitors, vectors, and raw errors private | `directory_roundtrip_test`, C++/Node/Rust/Python initialized-host tests, compile-only parity |
| `ida::registry` | `covered` | `ValueKind`, `StringListUpdate`, `Store::open`, `child`, existence/kind/inventory queries, typed string/binary/integer/boolean read/write, value/key/tree deletion, ordered string-list read/write/update | Persistent plugin configuration (`registry.hpp`) | Phase 64 retains one nonempty owned key, copies every returned value, verifies writes by readback, and excludes unrestorable process-global registry-root mutation | `registry_roundtrip_test`, C++/Node/Rust/Python initialized-host tests, compile-only parity |
| `ida::registers` | `covered` | `TrackingState`, `ValueOrigin`, `ValueCandidate`, `TrackedValue`, `NearestValue`, `track`, `constant_at`, `stack_delta_at`, `nearest_at`, cache clear/change notifications | Register-value finder (`regfinder.hpp`) | Phase 65 keeps processor register numbers, tracker records/candidates, native vectors, pointers, and raw reference-type sentinels private while preserving rich state | `register_tracking_roundtrip_test`, AArch64 C++/Node/Rust/Python initialized-host tests, compile-only parity |
| `ida::lumina` | `covered` | `has_connection`, `pull`, `push`, `OperationCode`, `BatchResult` | Lumina metadata sync (`lumina.hpp`) | Post-Phase-10 additive closure for real-port parity (`idalib_lumina`) | `smoke_test`, `tests/unit/api_surface_parity_test.cpp`, `examples/tools/idalib_lumina_port.cpp` |
| `ida::undo` | `covered` | `create_point`, `undo_action_label`, `redo_action_label`, `perform_undo`, `perform_redo` | Named restore points and undo/redo state (`undo.hpp`) | Phase 59 copies label state and keeps native record serialization private | `undo_roundtrip_test`, C++/Node/Rust/Python signature and initialized-host tests, compile-only parity |
| `ida::event` | `partial` | lifecycle/mutation subscriptions including `on_segment_moved`, `on_function_updated`, `on_item_type_changed`, `on_operand_type_changed`, `on_code_created`, `on_data_created`, `on_items_destroyed`, `on_extra_comment_changed`, `on_local_types_changed`, plus `on_event`, filtered routing, and RAII | IDB event routing (`HT_IDB`) | Phase 28 closes high-value post-change tracking with owned payloads and callback-mutation safety. Pre-change, dirtree, frame-member, and SDK-internal payload families remain intentionally unmirrored until concrete migration evidence exists | `event_stress_test` exact payload/self-mutation coverage; Node/Rust structural and real-IDA event tests |
| `ida::plugin` | `covered` | `Plugin`, wrapper-owned `register_action`/`unregister_action`, `activate_action`, move-only `ScopedHotkey`/`register_hotkey`, counted wrapper-managed `attach_to_menu`/`detach_from_menu`, counted `attach_to_toolbar`/`detach_from_toolbar`, `attach_to_popup`/`detach_from_popup`, `ActionContext`, `TypeRef`, `IDAX_PLUGIN` | Plugin lifecycle + actions (`loader/plugin` + `kernwin.hpp`) | Closed in P10.6; P22 adds Local Types snapshots; P25 hardens deterministic attachment state; P32 adds one-call shortcut ownership, deterministic adapter reclamation, and callback exception/panic barriers | `loader_processor_scenario_test`, Rust real-IDA lifecycle/unit tests, `tests/unit/api_surface_parity_test.cpp`, `docs/quickstart/plugin.md` |
| `ida::loader` | `covered` | `InputFile`, `Loader::accept/load/save/move_segment`, context-rich requests (`LoadFlags`, `LoadRequest`, `SaveRequest`, `MoveSegmentRequest`, `ArchiveMemberRequest`), `load_with_request`, `save_with_request`, `move_segment_with_request`, `process_archive`, `decode_load_flags`, `encode_load_flags`, `file_to_database`, `memory_to_database`, `IDAX_LOADER` | Loader module authoring (`loader.hpp`) | Closed in P10.6 (archive/member/reload/save request modeling + scenario tests) | `loader_processor_scenario_test`, `tests/unit/api_surface_parity_test.cpp`, `docs/quickstart/loader.md` |
| `ida::processor` | `covered` | `ProcessorInfo`, exact `ProcessorFlag`/`ProcessorFlag2`, extended `InstructionDescriptor`/`AssemblerInfo`, eight-operand `InstructionFeature`, `AnalyzeDetails::instruction_code` plus `AnalyzeOperand`, tokenized `OutputContext`, optional `Processor` hooks, `SwitchDescription`, loadable `IDAX_PROCESSOR` `LPH`/`procmod_t` bridge | Processor module authoring (`idp.hpp`, `ua.hpp`) | Phase 58 closes the prior declaration-only registration gap with an SDK-private descriptor/event bridge, checked instruction/switch materialization, ABI exception containment, discoverable module filenames, and exact 9.4 constants | exact-SDK build, cross-platform `LPH` export gate, licensed IDA runtime analyze/output smoke, `loader_processor_scenario_test`, Rust/Python parity, compile-only parity |
| `ida::debugger` | `covered` | process control APIs, request queue helpers (`request_*`, `run_requests`, `is_request_running`), thread introspection (`thread_count`, `thread_id_at`, `thread_name_at`, `threads`, `current_thread_id`), thread control (`select_thread`, `suspend_thread`, `resume_thread` + request variants), register introspection (`register_info`, `is_*_register`), `on_process_*`, `on_breakpoint_*`, `on_exception` | Debugger runtime/events (`dbg.hpp`, `idd.hpp`) | Closed in P10.7.a (async/request parity + thread/register introspection helpers) | `debugger_ui_graph_event_test`, `tests/unit/api_surface_parity_test.cpp` |
| `ida::ui` | `covered` | dialogs (`ask_text` with Node/Rust wrappers, markup-only and typed `ask_form`, `FormBuilder`; fixed-shape Node/Rust typed-form entrypoints for audited codedump packs), clipboard helpers (`copy_to_clipboard`, `read_clipboard`, `clipboard_backend`; Qt when enabled, otherwise host-command fallback), `WaitBox` with Node/Rust wrappers, `Progress`/`ProgressFn`, `jump_to`, stable-identity `Widget` lifecycle APIs, `current_widget`, `widget_host`/`with_widget_host`, custom viewer APIs (`create_custom_viewer`, `set_custom_viewer_lines`, `custom_viewer_*`), chooser, timer, expanded typed/generic UI/VIEW events (`on_database_inited`, `on_current_widget_changed`, `on_view_*`, `on_event`, `on_popup_ready`), popup action helpers (`attach_dynamic_action`, `attach_registered_action`) | UI + view notifications (`kernwin.hpp`, `HT_UI`, `HT_VIEW`, optional Qt Widgets) | Closed in P10.7.b with additive P22 UI parity and P24 current-widget polling | `debugger_ui_graph_event_test`, Node/Rust binding tests, `tests/unit/api_surface_parity_test.cpp`, `docs/namespace_topology.md` |
| `ida::graph` | `covered` | `Graph` node/edge/group/layout APIs (`set_layout`, `current_layout`, `redo_layout`), viewer lifecycle/query helpers (`show_graph`, `refresh_graph`, `has_graph_viewer`, `is_graph_viewer_visible`, `activate_graph_viewer`, `close_graph_viewer`), `flowchart`, `flowchart_for_ranges`, `switch_table` | Graph/viewer + CFG/switch metadata (`graph.hpp`, `gdl.hpp`, `nalt.hpp`) | Closed in P10.7.c (viewer lifecycle/query parity + layout behavior matrix validation) with P22 switch-table metadata for ida-cdump graph recovery | `debugger_ui_graph_event_test`, `tests/unit/api_surface_parity_test.cpp`, topology/matrix docs updates |
| `ida::decompiler` | `covered` | `available`, explicit ownership (`initialize`, `ScopedSession` with Node/Rust wrappers), `decompile`, `DecompiledFunction`, `collect_referenced_types`, event subscriptions (`on_maturity_changed`, `on_func_printed`, `on_switch_pseudocode`, `on_refresh_pseudocode`, `on_curpos_changed`, `on_create_hint`, `on_populating_popup`), ctree visitors (`visit`, `visit_expressions`, `for_each_*`) with read-only helper/type/parent accessors plus type sizing/member helpers (`type_byte_width`, `pointed_type_byte_width`, `member_name`, `third`, `is_assignment_lhs`), variable lookup/rename/retype/comment (`variable`, `rename_variable`, `retype_variable`, `set_variable_comment`), lvar settings snapshots (`LvarSnapshot`, capture/restore), serializable lvar settings (`LocalVariableUserSetting`, `saved_user_lvar_settings`, `apply_user_lvar_setting`), semantic persisted-comment workflows (`CommentPosition`, `PseudocodeComment`, `set/get/comments/save`, explicit orphan query/cleanup), mapping APIs (`line_to_address`, `address_map`), cache invalidation (`mark_dirty`, `mark_dirty_with_callers`), owned function-level `generate_microcode` snapshots with explicit maturity/call-analysis option/argument locations/CFG/addressed recursive operands and copied processor-register IDs, microcode-filter lifecycle (`register_microcode_filter`, `unregister_microcode_filter`), `MicrocodeContext` block/introspection APIs (`block_instruction_count`, `has_instruction_at_index`, `instruction`, `instruction_at_index`, `has_last_emitted_instruction`, `last_emitted_instruction`) | Hex-Rays facade (`hexrays.hpp`) | Closed in P10.7.d with additive lifter/codedump parity, P24 pseudocode-function-switch routing, Phase 37 owned graph traversal, Phase 38 explicit call-information analysis, Phase 44 processor-register evidence, and Phase 50 semantic persisted-comment parity | `decompiler_storage_hardening_test`, `decompiler_edge_cases_test`, `tests/unit/api_surface_parity_test.cpp`, Node unit/integration tests, Rust `decompiler_tests` and process-main-thread integration, topology/migration docs updates |
| `ida::lines` | `covered` | `SourceFile`, `add_source_file`, `source_file_at`, `remove_source_file`, `Color`, tagged-text and address-tag helpers | Source-file ranges and tagged text (`lines.hpp`) | Phase 35 adds copied half-open source mappings; tagged-text helpers remain the Phase 11 abyss closure | `string_source_metadata_test`, Node/Rust real-IDA cases, `examples/plugin/abyss_port_plugin.cpp` |
| `ida::path` | `covered` | `basename`, `dirname`, `is_directory` with Node/Rust wrappers | Portable path helpers (`std::filesystem`) | P22 additive closure for ida-cdump UI/path migration helpers without exposing SDK q* path utilities | `tests/unit/api_surface_parity_test.cpp`, Node unit tests, Rust `path_tests` |
| `ida::storage` | `covered` | `Node::open`, `Node::open_by_id`, `Node::id`, `Node::name`, `alt/sup/hash/blob` families | Netnode storage (`netnode.hpp`) | Closed in P10.7.e (node-id/open-by-id metadata helpers + safe blob index guidance) | `decompiler_storage_hardening_test`, `tests/unit/api_surface_parity_test.cpp`, storage docs updates |

## B) SDK Capability Family Matrix (one row per major capability family)

| SDK capability family | Primary SDK headers | Current idax domain(s) | Status | Closure criteria | Required evidence |
|---|---|---|---|---|---|
| Error transport + result model | N/A (cross-cutting) | `ida::error` | `covered` | Preserve category/code/message/context consistency | Unit + compile-only parity checks |
| Shared options + diagnostics | N/A (cross-cutting) | `ida::core`, `ida::diagnostics` | `partial` | Intentional abstraction (P10.9.a): compact cross-cutting option/diagnostics surface by design; broaden only for demonstrated migration need | Unit tests + docs conventions |
| Address flags/navigation | `ida.hpp`, `bytes.hpp` | `ida::address` | `covered` | Closed in P10.2 (predicate ranges + defined aliases) | `smoke_test`, compile-only parity checks |
| Byte read/write/patch | `bytes.hpp` | `ida::data` | `covered` | Closed in P10.2 (explicit revert APIs + mutation safety validation) | `data_mutation_safety_test` |
| Data definition/undefinition | `bytes.hpp`, `offset.hpp`, processor/assembler metadata | `ida::data` | `covered` | Fixed-width definitions accept checked positive element counts through 512-bit zword; tbyte/packed-real resolve processor width plus assembler availability; string/structure/undefine retain explicit byte units (P29-P30) | Exact C++/Node/Rust item-size, supported/unsupported, and overflow tests; compile-only parity checks |
| Custom data type/format lifecycle | `bytes.hpp` (`data_type_t`, `data_format_t`, custom registry/attachment/creation APIs) | `ida::data` | `covered` | P31 owns descriptor strings and callbacks, constrains packed IDs to `1..0xFFFE`, exposes copied snapshots and separate standard attachment, provides fixed/callback-derived item sizing plus explicit/inferred creation, and requires explicit unregister before module unload | 234-check C++ real-IDA lifecycle test; Node/Rust callback and creation integration; compile-only and pure signature parity checks |
| Database lifecycle/open/save/close | `idalib.hpp` | `ida::database` | `covered` | Closed in P10.2 (open mode + load intent wrappers) | `smoke_test`, compile-only parity checks |
| File/memory ingestion + snapshots | `loader.hpp`, `loader API` | `ida::database` | `covered` | Closed in P10.2 (metadata parity helpers + validated ingestion paths) | `smoke_test` |
| Segment management and register ranges | `segment.hpp`, `segregs.hpp` | `ida::segment` | `covered` | Complete current family is available through canonical names, owned values/ranges, closed provenance, optional unknown state, and verified mutation; processor declaration/event ordering/selector policy remain bounded processor-authoring scope | `segment_function_edge_cases_test`, `segment_register_roundtrip_test`, C++/Node/Rust/Python parity and release-runtime checks |
| Function/chunks/frame/register vars | `funcs.hpp`, `frame.hpp` | `ida::function` | `covered` | Closed in P10.3 (reanalysis/update intent APIs + migration ergonomics) | `segment_function_edge_cases_test`, compile-only parity checks |
| Instruction decode/text/operand formatting | `ua.hpp`, `bytes.hpp`, `lines.hpp` | `ida::instruction` | `covered` | Closed in P10.3 with post-closure struct-offset and access-mode parity; Phase 36 adds local named enum representation/readback while retaining native TID opacity | `instruction_decode_behavior_test`, `operand_and_text_test`, `operand_function_torture_test`, compile-only parity, Node/Rust real-IDA checks |
| Naming/demangling + identifier hygiene | `name.hpp` | `ida::name` | `covered` | Closed in P10.4, with full filtered name inventories retained across C++/Node/Rust in P35 | Name behavior, binding integration, and compile-only parity tests |
| Cross-reference mutation/enumeration | `xref.hpp` | `ida::xref` | `covered` | Closed in P10.4 (range-style iteration + richer typed filters) | Xref behavior + compile-only parity tests |
| Operand offset/reference semantics | `offset.hpp`, `nalt.hpp` | `ida::offset` | `covered` | Phase 70 exposes standard/custom semantic formats, nine named options, optional target/base state, rendering/calculation, verified layered apply/remove, and ergonomic reference-aware data-xref creation without native IDs, flags, records, sentinels, tags, or operand containers | Ten-kind/options/apply/query/calculate/render/xref/remove/save/reopen lifecycle in C++, plus Node/Rust/Python initialized-host parity and generated-ABI/manifest checks |
| IDC values and synchronous script execution | `expr.hpp` | `ida::script` | `covered` | Phase 71 exposes caller-owned values, attributes/slices, include resolution, expression evaluation, IDC compilation, named calls, snippets, scripts, and globals/references; exact IDA 9.4 release evidence passes and host-retained callback registries remain separately scoped | Exact IDA 9.4 scalar/object/lifetime/error/exception lifecycle plus C++/Node/Rust/Python parity |
| Comment pipelines (regular/anterior/posterior/render) | `lines.hpp` | `ida::comment` | `covered` | Closed in P10.4 (indexed line edits/removals + render parity checks) | Comment integration + compile-only parity tests |
| Type system + local TIL workflows | `typeinf.hpp`, `xref.hpp` | `ida::type` | `covered` | Closed in P10.4 for function-type/calling-convention/enum workflows and named-type bootstrap; P22/P23 add bulk declarations and rich metadata; Phase 36 adds metadata-preserving indexed argument replacement for direct and pointer function types; Phase 43 adds internal exact UDM identity resolution plus persistent user informational-reference ensure/readback | Type roundtrip + compile-only parity + Node/Rust initialized-host tests |
| Entry point + forwarder workflows | `entry.hpp` | `ida::entry` | `covered` | Closed in P10.4 (forwarder management helpers) | Smoke entry checks + compile-only parity tests |
| Fixup/relocation + custom handlers | `fixup.hpp` | `ida::fixup` | `covered` | Closed in P10.4 (descriptor fidelity + in-range traversal helper) | Fixup relocation + compile-only parity tests |
| Search (text/immediate/binary + cursor helpers) | `search.hpp` | `ida::search` | `covered` | Closed in P10.5 (typed option parity + `next_error`/`next_defined`) | `name_comment_xref_search_test`, compile-only parity checks |
| Auto-analysis scheduling/waiting/revert | `auto.hpp` | `ida::analysis` | `covered` | Closed in P10.5 (schedule-intent + cancel/revert wrappers) | `name_comment_xref_search_test`, compile-only parity checks |
| Address-bookmark lifecycle | `moves.hpp` | `ida::bookmark` | `covered` | Phase 67 replaces `place_t`, location history, renderer/widget context, native slot sentinels, and compaction-sensitive erase behavior with copied values, validated slots, deterministic allocation, and exact survivor reconstruction | Sparse low/middle/high slot create/update/enumerate/remove/save/reopen tests in C++, plus Node, Rust, and Python initialized-host round trips |
| Persistent address-navigation stacks | `moves.hpp` (`navstack_t`) | `ida::navigation` | `covered` | Phase 68 replaces `place_t`, renderer state, widget IDs, native stack keys, raw netnodes, and host containers with copied address/channel/metadata values; mutations verify exact post-state and roll back when representable, while reserved bootstrap state is excluded from all public snapshots | Create/reopen persistence, forward truncation, boundary movement, replacement/clear, discard/retain transfer, conflict/empty-source validation, bootstrap-channel resurrection regression, and C++/Node/Rust/Python initialized-host round trips |
| Typed analysis-problem lists | `problems.hpp` | `ida::problem` | `covered` | Phase 60 replaces raw list IDs, SDK strings, and sentinels with a closed kind enum, copied optionals, and booleans | Isolated Unicode remember/describe/traverse/remove tests in C++, Node, Rust, and Python |
| Architecture-independent exception regions | `tryblks.hpp` | `ida::exception` | `covered` | Phase 61 replaces `tryblk_t`/`catch_t`/`seh_t`, selector sentinels, and `TBEA_*` masks with owned discriminated values and semantic locations | Fragmented C++ plus filtered/disposition SEH add/list/classify/remove tests across C++, Node, Rust, and Python |
| Third-party source-parser registry and ingestion | `srclang.hpp` | `ida::parser` | `covered` | Phase 62 replaces raw language masks, `til_t*`, `qstring`, and `HTI_*` flags with semantic languages/options, copied values, and explicit reports/errors | Parser selection/configuration plus memory/file local-type ingestion in C++, Node, Rust, and Python |
| Standard database organization trees | `dirtree.hpp` | `ida::directory` | `covered` | Phase 63 replaces host pointers, inodes, cursors, indexes, visitors, SDK containers, and raw `dterr_t` with closed tree kinds, copied entries, owned paths, semantic errors, and deterministic partial reports | All eight kinds plus directory/item/fold/order/rank/interleaved-bulk round trips in C++, Node, Rust, and Python |
| Persistent plugin configuration registry | `registry.hpp` | `ida::registry` | `covered` | Phase 64 replaces `qstring`, SDK vectors, raw buffers/value kinds, and process-global root mutation with scoped owned stores, typed copied values, verified writes, and deterministic ordered-list updates | Disposable string/binary/signed-integer/boolean, inventory, child, list, delete, malformed-input, and recursive-cleanup round trips in C++, Node, Rust, and Python |
| Register-value tracking and cache coherence | `regfinder.hpp` | `ida::registers` | `covered` | Phase 65 replaces register ordinals, native rich-state records/vectors, and raw cref/dref cache sentinels with names, copied semantic state, and added/removed mutations | Deterministic AArch64 unique/alias/multi-candidate constants, negative stack delta, function-input, nearest, validation, and cache probes in C++, Node, Rust, and Python |
| Lumina metadata sync | `lumina.hpp` | `ida::lumina` | `covered` | Additive post-Phase-10 closure for real-port pull/push parity (`idalib_lumina`) | `smoke_test`, compile-only parity checks, example tool scaffold |
| Named restore points and undo/redo | `undo.hpp` | `ida::undo` | `covered` | Phase 59 replaces SDK-private record packing and `qstring` outputs with two text inputs, copied optional labels, and explicit boolean availability state | Isolated comment/checkpoint/label/undo/redo/final-restore tests in C++, Node, Rust, and Python |
| IDB event notifications | `event_listener_t` / `HT_IDB` | `ida::event` | `partial` | Curated high-value post-change events + generic route are covered; raw pre-change/internal payload mirroring remains evidence-driven | Exact mutation payload, self-unsubscribe, and binding runtime tests |
| Plugin authoring and action system | plugin API + `kernwin.hpp` | `ida::plugin` | `covered` | Closed in P10.6; P25 adds deterministic attachment state and P32 adds wrapper-owned adapters, action activation, scoped one-call hotkeys, and exception/panic barriers | 237-check `loader_processor_scenario_test` with explicit headless activation skips, Rust unit/real-IDA lifecycle tests, compile-only parity checks, quickstart docs |
| Loader authoring (accept/load/save/move) | `loader.hpp` | `ida::loader` | `covered` | Closed in P10.6 (archive/member/reload/save request surfaces + flag modeling) | `loader_processor_scenario_test`, compile-only parity checks |
| Processor authoring | `idp.hpp`, `ua.hpp` | `ida::processor` | `covered` | Phase 58 adds the missing loadable `LPH`/`procmod_t` bridge, exact `PR_*`/`PR2_*`/eight-operand `CF_*` values, typed `itype` materialization, discoverable output names, and exception-contained event routing | exact-SDK build; Linux/macOS/Windows export gate; licensed IDA analyze/output runtime smoke; C++/Python/Rust contract checks |
| Debugger control and notifications | `dbg.hpp`, `idd.hpp` | `ida::debugger` | `covered` | Closed in P10.7.a (request-queue parity + thread/register introspection helpers) | `debugger_ui_graph_event_test`, compile-only parity checks |
| UI/dialog/widget/view lifecycle | `kernwin.hpp` | `ida::ui` | `covered` | Closed in P10.7.b (custom-viewer parity + expanded UI/VIEW lifecycle routing) with P22 `WaitBox` plus Node/Rust wrappers, `ask_text` plus Node/Rust wrappers, typed `ask_form` plus fixed-shape Node/Rust entrypoints, and clipboard additions | `debugger_ui_graph_event_test`, compile-only parity checks, topology/matrix docs updates |
| Graph object/viewer/flowchart | `graph.hpp`, `gdl.hpp` | `ida::graph` | `covered` | Closed in P10.7.c (viewer lifecycle/query helpers + layout behavior matrix checks) | `debugger_ui_graph_event_test`, compile-only parity checks, topology/matrix docs updates |
| Decompiler pseudocode/ctree/comment/microcode workflows | `hexrays.hpp` | `ida::decompiler` | `covered` | Closed in P10.7.d for pseudocode/ctree/comment workflows; later phases add popup routing, serializable lvar metadata, scoped ownership, Phase 37 maturity-explicit owned function graphs, and Phase 50 semantic persisted-location enumeration across bindings | Decompiler hardening/edge-case tests + compile-only parity + Node/Rust same-address multi-location real-host evidence + docs updates |
| Tagged text, color tags, and source-file ranges | `lines.hpp` | `ida::lines` | `covered` | Phase 11 closes tagged-text needs; Phase 35 adds owned half-open source mappings | `string_source_metadata_test`, Node/Rust integration, abyss and IDAMagicStrings ports |
| Portable path helpers | N/A (`std::filesystem`) | `ida::path` | `covered` | P22 additive helper surface for plugin ports that need path splitting/directory checks, including Node/Rust wrapper parity | Compile-only parity checks, Node unit tests, Rust `path_tests` |
| Persistent storage (netnode) | `netnode.hpp` | `ida::storage` | `covered` | Closed in P10.7.e (node-id/open-by-id helpers + safe-index guidance) | `decompiler_storage_hardening_test`, compile-only parity checks, storage docs |

## C) Execution notes for Phase 10

1. This matrix is the canonical closure artifact for `P10.0`.
2. Status updates must happen in lockstep with:
   - `.agents/roadmap.md` TODO state,
   - `.agents/progress_ledger.md` progress entries,
   - `.agents/findings.md` and `.agents/decision_log.md` updates when applicable.
3. No row transitions to `covered` without both:
   - passing tests in changed profiles (`full`, `unit`, `compile-only`), and
   - docs evidence updates (`docs/migration/*`, `docs/namespace_topology.md`, API reference).

## D) Intentional abstraction notes (P10.9.a)

- `ida::core` / `ida::diagnostics`: intentionally compact cross-cutting helpers; not intended to mirror SDK-internal flag/counter breadth.
- `ida::event`: Phase 28 covers the stable post-change families used by change trackers and metadata synchronization. Pre-change and SDK-internal pointer-heavy notifications remain intentionally curated rather than exposed as raw payloads.
- Real-world example-port status is consolidated in `docs/port_gap_audit_examples.md`.
- No unblocked wrapper-surface delta remains in the audited bounded example ports. Phases 38-47 extend the Symless boundary through depth-bounded resolved direct and database-derived indirect-call arguments/returns, declarative allocator-wrapper/fixed-root discovery including fixed-pointer wrappers, exact constructor/vtable roots, direct-first/two-pointer-RTTI/data-alias table loads, statically seeded non-import virtual methods, exact shifted propagated arguments, exact local forward replacement, persistent informational references, source-ordered exact operand struct-offset paths, and explicit register/stack microcode-root selection with before/after injection. Runtime/object-dependent dispatch is not implemented by the audited upstream and is classified as a novel analysis, not a parity gap. Auto Enum's embedded corpus is representative, while idapcode's external Sleigh runtime and language-selection policy remain intentional integration boundaries rather than a native `ida::pcode` namespace.
