# Fork 演进提案索引

- **项目类型**: app —— 本 fork 的交付物是 `idax` 命令行工具，命令行接口即用户界面。
  重命名一个子命令或参数属于破坏性的用户可见变更。

本目录只记录**本 fork 特有**的演进决策。上游 `19h/idax` 自己的路线图与决策记录在
`.agents/` 与 `agents.md` 中，两者不得混写——详见 `../README.md`。

提案在落地到长期共享分支的那次提交里才分配编号，在此之前以 `draft-<slug>.md` 命名并按 slug
引用。

| # | 标题 | 状态 |
|---|------|------|
| draft | [收缩为上游之上的命令行工具](draft-shrink-fork-to-cli.md) | Draft |
