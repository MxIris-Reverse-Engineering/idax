name: Bindings CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

env:
  IDA_SDK_REPO: HexRaysSA/ida-sdk
  IDA_SDK_REF: 6929db6868a524496eb66e76e4ec6c9d720a0594 # IDA 9.4.0 release patch
  BUILD_TYPE: Release

jobs:
  build-node:
    name: Node.js Bindings - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            arch: x64
            msvc_arch: ''
          - os: windows-latest
            platform: win32
            arch: x64
            msvc_arch: x64
          - os: macos-14
            platform: darwin
            arch: arm64
            msvc_arch: ''

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          submodules: recursive

      - name: Checkout IDA SDK
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: ${{ env.IDA_SDK_REPO }}
          ref: ${{ env.IDA_SDK_REF }}
          path: ida-sdk
          submodules: recursive

      - name: Setup uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.28"
          github-token: ${{ github.token }}

      - name: Install IDA Pro
        env:
          HCLI_API_KEY: ${{ secrets.HCLI_KEY }}
        shell: bash
        run: |
          LICENSE_OUTPUT=$(uvx --from ida-hcli hcli --disable-updates license list)
          LICENSE_ID=$(printf '%s\n' "$LICENSE_OUTPUT" | python3 scripts/select_hcli_license.py)
          echo "::add-mask::$LICENSE_ID"
          echo "Selected active named IDA product license"
          
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64win.exe"
          elif [[ "$RUNNER_OS" == "Linux" ]]; then
            ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64linux.run"
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            if [[ "$(uname -m)" == "arm64" ]]; then
              ASSET_KEY="release/9.4/ida-pro/ida-pro_94_armmac.app.zip"
            else
              ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64mac.app.zip"
            fi
          fi
          
          mkdir -p ./ida-installer
          uvx --from ida-hcli hcli --disable-updates download --output-dir ./ida-installer "$ASSET_KEY"

          INSTALLER_FILE=$(python3 -c "import glob; files=sorted(glob.glob('ida-installer/*')); print(files[0] if files else '')")
          if [[ -z "$INSTALLER_FILE" ]]; then
            echo "error: no installer file found in ./ida-installer"
            ls -la ./ida-installer
            exit 1
          fi
          echo "Using installer file: $INSTALLER_FILE"
          uvx --from ida-hcli hcli --disable-updates ida install --set-default --accept-eula --yes --license-id "$LICENSE_ID" "$INSTALLER_FILE"

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '20'
          package-manager-cache: false

      - name: Resolve IDASDK and IDADIR paths
        shell: bash
        run: |
          SDK_ROOT="${GITHUB_WORKSPACE}/ida-sdk"
          if [[ -f "${SDK_ROOT}/ida-cmake/bootstrap.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/src/cmake/bootstrap.cmake" || -f "${SDK_ROOT}/src/cmake/idasdkConfig.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}/src" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/src/ida-cmake/bootstrap.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}/src" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/cmake/bootstrap.cmake" || -f "${SDK_ROOT}/cmake/idasdkConfig.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}" >> "$GITHUB_ENV"
          else
            echo "error: unable to locate SDK CMake entry point in SDK checkout"
            exit 1
          fi

          IDADIR=$(python3 -c "import json, os; p = os.path.expanduser('~/.idapro/ida-config.json') if os.name != 'nt' else os.path.expandvars('%APPDATA%/Hex-Rays/IDA Pro/ida-config.json'); print(json.loads(open(p).read())['Paths']['ida-install-dir']) if os.path.exists(p) else print('')")
          if [[ "$RUNNER_OS" == "macOS" && -f "$IDADIR/Contents/MacOS/libida.dylib" ]]; then
            IDADIR="$IDADIR/Contents/MacOS"
          fi
          echo "IDADIR=$IDADIR" >> "$GITHUB_ENV"
          
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            echo "$IDADIR" >> "$GITHUB_PATH"
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            echo "DYLD_LIBRARY_PATH=$IDADIR:$DYLD_LIBRARY_PATH" >> "$GITHUB_ENV"
            echo "DYLD_FALLBACK_LIBRARY_PATH=$IDADIR:$DYLD_FALLBACK_LIBRARY_PATH" >> "$GITHUB_ENV"
          else
            echo "LD_LIBRARY_PATH=$IDADIR:$LD_LIBRARY_PATH" >> "$GITHUB_ENV"
          fi

      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ./.github/actions/setup-msvc
        with:
          arch: ${{ matrix.msvc_arch }}

      - name: Build idax static library
        shell: bash
        run: |
          mkdir build
          cd build
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            cmake -G Ninja -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} ..
          else
            cmake -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} ..
          fi
          cmake --build . --config ${{ env.BUILD_TYPE }}

      - name: Build Node Addon
        shell: bash
        working-directory: bindings/node
        run: |
          npm install --ignore-scripts
          npx cmake-js compile -v "$(node -v | sed 's/v//')"

      - name: Run Node unit tests
        shell: bash
        working-directory: bindings/node
        run: |
          npm test

      - name: Run Node examples (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: bindings/node
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            export DYLD_LIBRARY_PATH="$IDADIR:$DYLD_LIBRARY_PATH"
          else
            export LD_LIBRARY_PATH="$IDADIR:$LD_LIBRARY_PATH"
          fi
          npm install ts-node typescript @types/node
          cp /bin/ls ./test_bin
          TEST_BIN="./test_bin"
          
          echo "Running idalib_dump_port..."
          npx ts-node examples/idalib_dump_port.ts "$TEST_BIN" --list

          echo "Running complexity_metrics..."
          npx ts-node examples/complexity_metrics.ts "$TEST_BIN"

          echo "Running class_reconstructor..."
          npx ts-node examples/class_reconstructor.ts "$TEST_BIN"

          echo "Running binary_forensics..."
          npx ts-node examples/binary_forensics.ts "$TEST_BIN"

      - name: Run Node integration tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: bindings/node
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            export DYLD_LIBRARY_PATH="$IDADIR:$DYLD_LIBRARY_PATH"
          else
            export LD_LIBRARY_PATH="$IDADIR:$LD_LIBRARY_PATH"
          fi
          echo "Running Node.js integration tests..."
          node test/integration.test.js ../../tests/fixtures/simple_appcall_linux64
          node test/registers.integration.test.js ../../tests/fixtures/register_tracking_aarch64

      - name: Run Node examples (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        working-directory: bindings/node
        run: |
          Write-Host "Skipping Node examples on Windows (headless instability under CI runtime)."

  build-rust:
    name: Rust Bindings - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            msvc_arch: ''
          - os: windows-latest
            msvc_arch: x64
          - os: macos-14
            msvc_arch: ''

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          submodules: recursive

      - name: Checkout IDA SDK
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: ${{ env.IDA_SDK_REPO }}
          ref: ${{ env.IDA_SDK_REF }}
          path: ida-sdk
          submodules: recursive

      - name: Setup uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.28"
          github-token: ${{ github.token }}

      - name: Install IDA Pro
        env:
          HCLI_API_KEY: ${{ secrets.HCLI_KEY }}
        shell: bash
        run: |
          LICENSE_OUTPUT=$(uvx --from ida-hcli hcli --disable-updates license list)
          LICENSE_ID=$(printf '%s\n' "$LICENSE_OUTPUT" | python3 scripts/select_hcli_license.py)
          echo "::add-mask::$LICENSE_ID"
          echo "Selected active named IDA product license"
          
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64win.exe"
          elif [[ "$RUNNER_OS" == "Linux" ]]; then
            ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64linux.run"
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            if [[ "$(uname -m)" == "arm64" ]]; then
              ASSET_KEY="release/9.4/ida-pro/ida-pro_94_armmac.app.zip"
            else
              ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64mac.app.zip"
            fi
          fi
          
          mkdir -p ./ida-installer
          uvx --from ida-hcli hcli --disable-updates download --output-dir ./ida-installer "$ASSET_KEY"

          INSTALLER_FILE=$(python3 -c "import glob; files=sorted(glob.glob('ida-installer/*')); print(files[0] if files else '')")
          if [[ -z "$INSTALLER_FILE" ]]; then
            echo "error: no installer file found in ./ida-installer"
            ls -la ./ida-installer
            exit 1
          fi
          echo "Using installer file: $INSTALLER_FILE"
          uvx --from ida-hcli hcli --disable-updates ida install --set-default --accept-eula --yes --license-id "$LICENSE_ID" "$INSTALLER_FILE"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable action snapshot

      - name: Resolve IDASDK and IDADIR paths
        shell: bash
        run: |
          SDK_ROOT="${GITHUB_WORKSPACE}/ida-sdk"
          if [[ -f "${SDK_ROOT}/ida-cmake/bootstrap.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/src/cmake/bootstrap.cmake" || -f "${SDK_ROOT}/src/cmake/idasdkConfig.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}/src" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/src/ida-cmake/bootstrap.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}/src" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/cmake/bootstrap.cmake" || -f "${SDK_ROOT}/cmake/idasdkConfig.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}" >> "$GITHUB_ENV"
          else
            echo "error: unable to locate SDK CMake entry point in SDK checkout"
            exit 1
          fi

          IDADIR=$(python3 -c "import json, os; p = os.path.expanduser('~/.idapro/ida-config.json') if os.name != 'nt' else os.path.expandvars('%APPDATA%/Hex-Rays/IDA Pro/ida-config.json'); print(json.loads(open(p).read())['Paths']['ida-install-dir']) if os.path.exists(p) else print('')")
          if [[ "$RUNNER_OS" == "macOS" && -f "$IDADIR/Contents/MacOS/libida.dylib" ]]; then
            IDADIR="$IDADIR/Contents/MacOS"
          fi
          echo "IDADIR=$IDADIR" >> "$GITHUB_ENV"
          
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            echo "$IDADIR" >> "$GITHUB_PATH"
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            echo "DYLD_LIBRARY_PATH=$IDADIR:$DYLD_LIBRARY_PATH" >> "$GITHUB_ENV"
            echo "DYLD_FALLBACK_LIBRARY_PATH=$IDADIR:$DYLD_FALLBACK_LIBRARY_PATH" >> "$GITHUB_ENV"
          else
            echo "LD_LIBRARY_PATH=$IDADIR:$LD_LIBRARY_PATH" >> "$GITHUB_ENV"
          fi

      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ./.github/actions/setup-msvc
        with:
          arch: ${{ matrix.msvc_arch }}

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake clang libclang-dev

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          XCODE_LIBCLANG="$(xcode-select -p)/Toolchains/XcodeDefault.xctoolchain/usr/lib"
          if [[ ! -f "$XCODE_LIBCLANG/libclang.dylib" ]]; then
            echo "error: Xcode libclang.dylib was not found"
            exit 1
          fi
          echo "LIBCLANG_PATH=$XCODE_LIBCLANG" >> "$GITHUB_ENV"

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          choco install llvm
          printf '%s\n' 'LIBCLANG_PATH=C:\Program Files\LLVM\bin' >> "$GITHUB_ENV"

      - name: Build Rust bindings (Unix)
        if: runner.os != 'Windows'
        working-directory: bindings/rust
        shell: bash
        run: |
          cargo build --verbose
          cargo build --examples --verbose

      - name: Build Rust bindings (Windows)
        if: runner.os == 'Windows'
        working-directory: bindings/rust
        shell: pwsh
        run: |
          cargo build --release --verbose
          cargo build --release --examples --verbose

      - name: Run Rust unit tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: bindings/rust
        run: |
          cargo test --lib --verbose -- --test-threads=1

      - name: Run Rust unit tests (Windows)
        if: runner.os == 'Windows'
        working-directory: bindings/rust
        shell: pwsh
        run: |
          cargo test --release --lib --verbose -- --test-threads=1

      - name: Run Rust examples (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: bindings/rust
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            export DYLD_LIBRARY_PATH="$IDADIR:$DYLD_LIBRARY_PATH"
          else
            export LD_LIBRARY_PATH="$IDADIR:$LD_LIBRARY_PATH"
          fi
          
          cp /bin/ls ./test_bin
          TEST_BIN="./test_bin"
          
          echo "Running idalib_dump_port..."
          cargo run --example idalib_dump_port -- "$TEST_BIN" --list

          echo "Running ida2py_port..."
          cargo run --example ida2py_port -- "$TEST_BIN"

      - name: Run Rust integration tests (Unix)
        if: runner.os == 'macOS'
        shell: bash
        working-directory: bindings/rust
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            export DYLD_LIBRARY_PATH="$IDADIR:$DYLD_LIBRARY_PATH"
          else
            export LD_LIBRARY_PATH="$IDADIR:$LD_LIBRARY_PATH"
          fi
          echo "Building Rust integration tests (headless runtime is covered by examples in CI)..."
          cargo test --test integration --no-run

      - name: Run Rust referent metadata runtime test (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: bindings/rust
        env:
          IDAX_DIAPHORA_REFERENT_RUNTIME_FIXTURE: ${{ github.workspace }}/tests/fixtures/simple_appcall_linux64
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            export DYLD_LIBRARY_PATH="$IDADIR:$DYLD_LIBRARY_PATH"
          else
            export LD_LIBRARY_PATH="$IDADIR:$LD_LIBRARY_PATH"
          fi
          cargo test --test diaphora_referent_runtime

      - name: Run Rust register-tracking runtime test (Unix)
        if: runner.os != 'Windows'
        shell: bash
        working-directory: bindings/rust
        env:
          IDAX_REGISTERS_RUNTIME_FIXTURE: ${{ github.workspace }}/tests/fixtures/register_tracking_aarch64
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            export DYLD_LIBRARY_PATH="$IDADIR:$DYLD_LIBRARY_PATH"
          else
            export LD_LIBRARY_PATH="$IDADIR:$LD_LIBRARY_PATH"
          fi
          cargo test --test registers_runtime

      - name: Run Rust integration tests (Windows)
        if: runner.os == 'Windows'
        working-directory: bindings/rust
        shell: pwsh
        run: |
          $env:PATH = "$env:IDADIR;$env:PATH"
          $env:IDAUSR = "$pwd\idax_empty_user"
          New-Item -ItemType Directory -Force -Path $env:IDAUSR | Out-Null
          Write-Host "Building Rust integration tests (runtime executes on Unix; Windows coverage is compile-only)..."
          cargo test --release --test integration --no-run
          cargo test --release --test diaphora_referent_runtime --no-run
          cargo test --release --test registers_runtime --no-run

  build-python:
    name: Python Bindings - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            msvc_arch: ''
          - os: windows-latest
            msvc_arch: x64
          - os: macos-14
            msvc_arch: ''

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          submodules: recursive

      - name: Checkout IDA SDK
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: ${{ env.IDA_SDK_REPO }}
          ref: ${{ env.IDA_SDK_REF }}
          path: ida-sdk
          submodules: recursive

      - name: Setup uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.28"
          github-token: ${{ github.token }}

      - name: Install IDA Pro
        env:
          HCLI_API_KEY: ${{ secrets.HCLI_KEY }}
        shell: bash
        run: |
          LICENSE_OUTPUT=$(uvx --from ida-hcli hcli --disable-updates license list)
          LICENSE_ID=$(printf '%s\n' "$LICENSE_OUTPUT" | python3 scripts/select_hcli_license.py)
          echo "::add-mask::$LICENSE_ID"
          echo "Selected active named IDA product license"

          if [[ "$RUNNER_OS" == "Windows" ]]; then
            ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64win.exe"
          elif [[ "$RUNNER_OS" == "Linux" ]]; then
            ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64linux.run"
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            if [[ "$(uname -m)" == "arm64" ]]; then
              ASSET_KEY="release/9.4/ida-pro/ida-pro_94_armmac.app.zip"
            else
              ASSET_KEY="release/9.4/ida-pro/ida-pro_94_x64mac.app.zip"
            fi
          fi

          mkdir -p ./ida-installer
          uvx --from ida-hcli hcli --disable-updates download --output-dir ./ida-installer "$ASSET_KEY"

          INSTALLER_FILE=$(python3 -c "import glob; files=sorted(glob.glob('ida-installer/*')); print(files[0] if files else '')")
          if [[ -z "$INSTALLER_FILE" ]]; then
            echo "error: no installer file found in ./ida-installer"
            ls -la ./ida-installer
            exit 1
          fi
          echo "Using installer file: $INSTALLER_FILE"
          uvx --from ida-hcli hcli --disable-updates ida install --set-default --accept-eula --yes --license-id "$LICENSE_ID" "$INSTALLER_FILE"

      - name: Resolve IDASDK and IDADIR paths
        shell: bash
        run: |
          SDK_ROOT="${GITHUB_WORKSPACE}/ida-sdk"
          if [[ -f "${SDK_ROOT}/ida-cmake/bootstrap.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/src/cmake/bootstrap.cmake" || -f "${SDK_ROOT}/src/cmake/idasdkConfig.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}/src" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/src/ida-cmake/bootstrap.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}/src" >> "$GITHUB_ENV"
          elif [[ -f "${SDK_ROOT}/cmake/bootstrap.cmake" || -f "${SDK_ROOT}/cmake/idasdkConfig.cmake" ]]; then
            echo "IDASDK=${SDK_ROOT}" >> "$GITHUB_ENV"
          else
            echo "error: unable to locate SDK CMake entry point in SDK checkout"
            exit 1
          fi

          IDADIR=$(python3 -c "import json, os; p = os.path.expanduser('~/.idapro/ida-config.json') if os.name != 'nt' else os.path.expandvars('%APPDATA%/Hex-Rays/IDA Pro/ida-config.json'); print(json.loads(open(p).read())['Paths']['ida-install-dir']) if os.path.exists(p) else print('')")
          if [[ "$RUNNER_OS" == "macOS" && -f "$IDADIR/Contents/MacOS/libida.dylib" ]]; then
            IDADIR="$IDADIR/Contents/MacOS"
          fi
          echo "IDADIR=$IDADIR" >> "$GITHUB_ENV"

          if [[ "$RUNNER_OS" == "Windows" ]]; then
            echo "$IDADIR" >> "$GITHUB_PATH"
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            echo "DYLD_LIBRARY_PATH=$IDADIR:$DYLD_LIBRARY_PATH" >> "$GITHUB_ENV"
            echo "DYLD_FALLBACK_LIBRARY_PATH=$IDADIR:$DYLD_FALLBACK_LIBRARY_PATH" >> "$GITHUB_ENV"
          else
            echo "LD_LIBRARY_PATH=$IDADIR:$LD_LIBRARY_PATH" >> "$GITHUB_ENV"
          fi

      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ./.github/actions/setup-msvc
        with:
          arch: ${{ matrix.msvc_arch }}

      - name: Build editable Python package
        shell: bash
        run: |
          uv sync --extra test --no-install-project
          uv pip install --reinstall -e .

      - name: Run Python pure, type, and declaration gates
        shell: bash
        run: |
          uv run pytest -q -m "not ida_runtime"
          uv run mypy
          uv run python scripts/check_python_api_manifest.py
          uv run python -m py_compile bindings/python/examples/*.py scripts/check_python_distribution.py

      - name: Build and inspect Python distributions
        shell: bash
        run: |
          uv build
          uv run python scripts/check_python_distribution.py

      - name: Run initialized IDA Python tests (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          RUNTIME_DIR="$RUNNER_TEMP/idax-python-runtime"
          mkdir -p "$RUNTIME_DIR"
          cp tests/fixtures/simple_appcall_linux64 "$RUNTIME_DIR/fixture"
          (
            cd "$RUNTIME_DIR"
            "$IDADIR/idat" -A -B fixture >/dev/null
          )
          IDAX_PYTHON_REQUIRE_DECOMPILER=1 \
          IDAX_PYTHON_RUNTIME_FIXTURE="$RUNTIME_DIR/fixture" \
          IDAX_PYTHON_REGISTERS_RUNTIME_FIXTURE="${{ github.workspace }}/tests/fixtures/register_tracking_aarch64" \
            uv run pytest -q -m ida_runtime

      - name: Record Windows initialized-host boundary
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Write-Host "Windows Python native build, pure tests, typing, declaration audit, and archive inspection pass; initialized idalib execution remains disabled under the established nondiagnostic Windows headless boundary."
